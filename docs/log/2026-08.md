# 작업 로그 — 2026년 8월

> 형식과 규칙은 [README.md](README.md).
> 새 세션은 **오늘 날짜에 해당하는 파일**을 읽고, 맨 아래 항목의 "다음 AI에게"부터 확인한다.

**이 달 미완료 인계사항 (2026-08-19 10:54 기준)**

GPT(설계 담당)에게 넘긴 G-1~G-5:

- [x] **G-1. 테스트 러너 결정** — 자체 `SceneTree` 러너로 확정 (`D-015`). → 항목 `08-18 11:49`
- [x] **G-2. 출처 정확성 재검수** — 완료. → 항목 `08-18 10:49`
- [x] **G-3. 판단 근거 검수** — 완료. 과도한 해석/상태 분류를 정정. → 항목 `08-18 11:49`
- [x] **G-4. 내용 중복 잔존 검사** — 완료. 4묶음 추가 정리 + canon README 구조 동기화. → 항목 `08-18 11:49`
- [x] **G-5. `NPC-005` LOD 결정성 검수** — 완료. catch-up 외 추가 불변식 확정. → 항목 `08-18 11:49`

오너/구현 담당 쪽:

- [x] **PHASE 1 착수** — 오너 지시로 착수, 첫 구현 묶음 완료. → 항목 `08-18 12:12`
- [x] **PHASE 1 PLAYTESTED 통과** — 오너 확정. 1-4·1-5 닫힘. → 항목 `08-18 12:34`
- [x] **PHASE 1 독립 검수** — GPT가 코드/diff·테스트 설계·Canon 정합성을 교차검증. 완료 판정 유지. → 항목 `08-18 13:00`
- [x] **P1-TEST-001** — 실제 Player/Input/physics 경로 E2E 회귀 테스트 추가. **변이 테스트로 유효성 증명.** → 항목 `08-18 13:28`
- [x] **P1-TEST-002** — 통합 harness와 테스트 케이스 분리 (`tests/integration/`). → 항목 `08-18 13:28`
- [x] **P1-TOOL-001** — `tools/run.ps1` Godot 버전 확인/경고. → 항목 `08-18 13:28`
- [x] **`D-016` GPT 설계 검토** — 큰 방향 승인 가능, Canon 정합성 2건 발견 + 구조 보정안 작성. → 항목 `08-18 13:34`
- [x] **D-016 오너 최종 확정 + D-017 세계 공간 Canon 복원** — 공간 구조·계단 계산·공유 월드·맵의 끝까지 확정. PHASE 2 차단 해제.
- [x] **PHASE 2 착수** — `D-016` 확정 후 `P2-T1`(공간 데이터 타입) 완료. → 항목 `08-18 15:10`
- [x] **`P2-T0` 가드 테스트** — 금지 구조 6종. 변이 테스트로 유효성 증명. → 항목 `08-18 16:05`
- [x] **`AccessEnvelope` 형태 기반 교체** — 와이드 맵 확장성 확보. → 항목 `08-18 16:05`
- [x] **D-018 회차 시작 고정 문구** — `[@회차 시작! 행운을 빕니다!]` → 항목 `08-19 10:24`
- [x] **D-019 P2-T2 1층 greybox 시작범위 승인** — 긴 축 160~220 / 주요 공간 14~20 / 소형 포켓 6~10 / 큰 루프 3~5. → 항목 `08-19 10:54`
- [x] **D-020 FLR-014 무기 품질 충돌 해소** — 소설 원문 규칙 채택: 저품질이어도 대체로 초기 대거보다 낫다. → 항목 `08-19 10:54`
- [x] **`P2-T2` 1층 greybox + loader** — 공간 24 / 통행 8500칸 / 긴 축 172타일. → 항목 `08-19 11:40`
- [x] **`P2-T3~T7`** — 함정/파밍 · 행동 반경 · 계단 resolver · save/load · 변이 검증. → 항목 `08-19 13:20`
- [ ] 🔴 **[오너] PHASE 2 greybox PLAYTEST — 진행 중(부분).**
      1차: 동선 난이도는 긍정 판정. 규모/밀도는 콘텐츠가 안 보여 **판정 보류**.
      2차: F1 디버그 오버레이로 함정·파밍·계단 마커 표시 후 재플레이. 소감 대기.
      → 항목 `08-19 14:05`
- [ ] **[오너+GPT] `P2-T0`/`P2-T1` 작업물 검토** — 오너 요청(2026-08-18)
- [ ] `BASE_SPEED`는 PHASE 6에서 민첩 보정과 함께 재확정 (항목 `08-18 12:34`)
- [ ] docx 개정 시 `tools/docx_to_md.ps1` 재실행 (항목 `08-17 14:07`)
- [ ] 다른 PC 작업 시 `--import` 먼저 (항목 `08-18 10:14`)
- [ ] 배포 렌더링 경로는 PHASE 11 (`B-003`)

---

## 2026-08-17 13:03 · STEP 0 — GitHub 최초 push

**담당**: Claude (구현)

### 요청
> 이 폴더는 아직 원격에 안 올라간 문서 스캐폴딩이다. git 상태를 확인하고, origin이 없으면
> `https://github.com/aa0607aa/Tower-Dev.git`로 연결한 뒤 main으로 push 해라.

작업 전 `COLLABORATION_PROTOCOL.md` → `docs/DEVELOPMENT_GUIDE.md` → `DECISIONS.md` →
`CURRENT_STATE.md` 순으로 읽으라는 지시 포함.

### 작업
- 문서 4종 정독 후 `git init -b main` → 첫 커밋 → origin 연결 → push
- `git ls-remote origin`으로 원격 ref 실제 존재 확인

### 근거
- push 성공을 stdout 메시지가 아니라 **원격 ref 조회로 검증**했다.
  PowerShell이 git stderr를 NativeCommandError로 감싸 성공을 실패처럼 보이게 하므로
  종료 코드나 출력만 믿으면 안 된다.

### 다음 AI에게
- [x] PHASE 0 착수 → 항목 `08-17 13:11`

### 오너 결정 필요
없음

**산출물**: 커밋 `26be95f`

---

## 2026-08-17 13:11 · PHASE 0 — Godot 프로젝트 뼈대

**담당**: Claude (구현)

### 요청
> 개발 가이드의 PHASE 0 정의와 폴더 구조를 따른다. Godot 4.7.1 stable + GDScript 프로젝트
> 뼈대 생성. 실행 가능한 최소 Boot/Main 씬을 만들고 실제로 실행해 오류 없는지 확인한다.
> PHASE 0 완료 조건 충족하면 멈춘다. 내 지시 전에는 PHASE 1로 넘어가지 마라.

### 작업
- `project.godot`(GL Compatibility · nearest 필터 · 1280×720), 개발 가이드 §6 폴더 구조 전체
- `Boot.tscn` → `Main.tscn`, `game_log.gd` autoload, `data/canon/canon.gd`
- `docs/CANON_NOTES.md` · `CODING_STYLE.md` · `TEST_CHECKLIST.md` · `assets/STYLE_GUIDE.md`
- **실행 검증 중 버그 1건 발견·수정**: `Boot._ready()`에서 `change_scene_to_file()` 직접 호출 시
  `Parent node is busy adding/removing children` 에러 → `await get_tree().process_frame` 후 전환

### 근거
- `data/canon/canon.gd`에 **[Resolved] 상수만** 넣었다. TBD 수치를 "일단 기본값"으로 채우면
  canon 위조가 되고, 다음 세션이 그것을 사실로 오해한다.
- 개발 가이드 §6은 `docs/DECISIONS.md`인데 협업 프로토콜 §6은 루트다.
  **협업 프로토콜이 우선**이라 루트를 유지하고 README에 명시했다.
- `.gitattributes`로 LF 정규화. Godot 에디터가 항상 LF로 재저장해 CRLF면 커밋마다 전체 diff가 난다.

### 다음 AI에게
- [x] ESC 종료 검증 — 오너가 직접 확인 (항목 `08-17 13:26`)

### 오너 결정 필요
없음

**산출물**: 커밋 `72c4ff4` `0b69769`

---

## 2026-08-17 13:26 · OpenGL 드라이버 설치와 종료 크래시 규명

**담당**: Claude (구현) · 오너 (UAC 승인 · ESC 검증)

### 요청
> OpenGL 3.3 드라이버도 설치하자

### 작업
- AMD Radeon R5 240 실GPU 드라이버 설치 → OpenGL 3.3 확보.
- 네이티브 OpenGL 종료 시 `0xC0000409` 크래시를 격리 테스트.
- 빈 프로젝트에서도 3/3 재현, ANGLE 강제 시 정상 종료 3/3.

### 근거
- 빈 프로젝트에서도 재현되어 프로젝트 코드가 아닌 드라이버+엔진 환경 문제로 확정.
- ANGLE은 로컬 개발 옵션으로만 쓰고 `project.godot`에 고정하지 않음 (`B-003`).

### 다음 AI에게
- [x] 개발 PC 창 실행 시 필요하면 `--rendering-driver opengl3_angle`
- [ ] 배포 렌더링 경로 결정은 PHASE 11 (`B-003`)

### 오너 결정 필요
없음

**산출물**: `docs/TEST_CHECKLIST.md` · 커밋 `f518da8`

---

## 2026-08-17 13:34 · canon 색인 체계 구축 (1차) + 데이터 마이닝 정책 검토

**담당**: Claude (구현)

### 요청
> 실제 게임 개발 전에 설정이 탄탄하고 필요할 때 정확히 불러올 수 있게 분류되어야 한다.
> 데이터 마이닝은 막기보다 볼륨/더미 등으로 원하는 내용을 찾기 어렵게 하면 어떨까.

### 작업
- `docs/canon/` 신설 — ID/도메인/상태 체계.
- 1차 63항목 / 7도메인.
- `docs/DATAMINING_POLICY.md` → D-012 Proposed.

### 근거
- 별도 JSON 색인을 두면 두 진실이 생길 위험이 있어 Markdown 단일 색인 유지.
- 코드 미참조 더미는 쉽게 걸러지므로 데이터 마이닝 대응을 3 Tier로 분해.

### 다음 AI에게
- [x] 설정서 원본 대조 → `08-17 13:45`

### 오너 결정 필요
- D-012 Q1/Q2/Q3 → `08-17 13:55` 확정

**산출물**: `5a3f6e8` `c9aac08`

---

## 2026-08-17 13:45 · 설정서 원본 대조 — 63 → 133항목

**담당**: Claude (구현) · 오너

### 요청
오너가 `docs/SETTING_BIBLE_v1.1.docx` 배치.

### 작업
- docx 전문 확보 후 8개 도메인(HIS/RAC/MAG/ITM/NPC/KGD/DGN/GOD) 추가.
- 기존 도메인 보정, Canon Conflict 0건.

### 근거
- DECISIONS/개발 가이드만으로는 원문 설정의 상당 부분이 누락됨을 확인.

### 다음 AI에게
- [x] 절 단위 커버리지 감사 → `08-17 13:55`

### 오너 결정 필요
없음

**산출물**: `a793e6c`

---

## 2026-08-17 13:55 · D-012 확정 + 커버리지 감사

**담당**: Claude (구현) · 오너

### 요청
> Q1 허가 / Q2 나중 / Q3 1층은 괜찮아. 설정 정리는 제대로 한거야?

### 작업
- D-012 Resolved, Tier 1·2 / SYS-014 확정, Tier 3→B-001.
- 설정서 절 84개 전수 매핑 → 누락 4건 보완 → 137항목.

### 근거
- "제대로 했나"를 확인 없이 답하지 않고 실제 커버리지 감사를 수행.

### 다음 AI에게
- [x] Markdown 사본 → `08-17 14:07`

### 오너 결정 필요
없음

**산출물**: `f518da8`

---

## 2026-08-17 14:07 · 설정서 Markdown 사본 + 자기검증 변환기

**담당**: Claude (구현)

### 요청
> GPT가 확인할 수 있게 변환본도 만들어.

### 작업
- `tools/docx_to_md.ps1`, `docs/SETTING_BIBLE_v1.1.md` 생성.
- 666문단 누락 0 자기검증.

### 근거
- 사람 손으로 옮겨 canon 문구가 변형되는 것을 막고 AI가 읽을 수 있는 사본을 기계 생성.

### 다음 AI에게
- [ ] docx 개정 시 변환기 재실행
- [x] 테스트 러너 결정 → D-015 / `08-18 11:49`

### 오너 결정 필요
없음

**산출물**: `1ea4f7f`

---

## 2026-08-18 09:54 · GPT 교차검증 6건 반영 — D-013 · D-014

**담당**: GPT · Claude · 오너

### 요청
GPT 6건 지적 및 NPC LOD 원안 검토.

### 작업
- 레벨당 1포인트/XP 초기화의 실제 출처 재검색.
- D-013(출처 정정), D-014(NPC LOD), KGD-008/FAC-004/KGD-001/RAC-004 등 반영.
- 137 → 141항목.

### 근거
- 오너의 1차 판단도 원문과 다르면 출처를 정직하게 바로잡아야 함.
- LOD는 엔진 시뮬레이션 빈도와 AI 호출을 분리하고 결정성을 유지해야 함.

### 다음 AI에게
- [x] 판단 근거 체계 → `10:04`
- [x] 출처 정확성 G-2 → `10:49`
- [x] LOD G-5 → `11:49`

### 오너 결정 필요
없음

**산출물**: `e1906d7`

---

## 2026-08-18 10:04 · 판단 근거 필드 신설 + 내용 중복 2건 정리

**담당**: Claude

### 요청
> 설정집에 내용 추가/커밋할 때 설명처럼 근거도 함께 적고 있지?

### 작업
- 출처 141/141, 판단 근거 20/141 확인.
- `판단` 필드 규칙 도입, 해석이 있는 DESIGN/혼합 항목 backfill.
- WLD-008→ITM-002, WLD-009→HIS-002/HIS-004 중복 정리.

### 근거
- 판단 이유가 채팅에만 있으면 다음 AI가 결론만 보고 잘못 확장할 수 있음.
- 판단이 없었던 순수 TBD에는 억지 근거를 만들지 않음.

### 다음 AI에게
- [x] G-3 판단 재검수 → `11:49`
- [x] G-4 중복 잔존 → `11:49`

### 오너 결정 필요
없음

**산출물**: `c348578`

---

## 2026-08-18 10:14 · 작업 로그 체계 도입

**담당**: Claude

### 요청
> 답변마다 요청·근거·시각·다음 AI에게 전달할 내용·오너 판단 필요 사항을 월 단위 md에 기록하자.

### 작업
- `docs/log/README.md`, `2026-08.md`, 협업 프로토콜 §3.3.

### 근거
- CURRENT_STATE/CHANGELOG/DECISIONS 사이에 판단 과정과 직전 AI 인계가 빠지는 구멍을 해소.

### 다음 AI에게
- [ ] 작업 종료 때마다 로그 작성
- [ ] 월 변경 시 미완료 인계 이월
- [ ] 완료 체크박스에 처리 항목 표시

### 오너 결정 필요
없음

**산출물**: `bc6b8ef`

---

## 2026-08-18 10:14 · 저장소 누락 점검

**담당**: Claude

### 요청
> 다른 PC에서 작업할 수 있으니 Tower 폴더 작업물이 전부 Git에 있는지 확인.

### 작업
- 실제 작업물 누락 없음 확인.
- `saves/.gitkeep`가 유지되도록 .gitignore 수정.
- 다른 PC 이어받기 README 추가.

### 근거
- `.godot/`은 PC별 캐시로 의도적으로 제외.

### 다음 AI에게
- [ ] 다른 PC 시작 시 `--import`
- [ ] PC별 GPU 이슈 기록

### 오너 결정 필요
없음

**산출물**: `bc6b8ef`

---

## 2026-08-18 10:22 · GPT 인계 문구 정리 (G-1 ~ G-5)

**담당**: Claude

### 요청
> 지금까지 얘기한 내용 GPT에게 전달하게 정리하고 인계도 같이 적어줘.

### 작업
- G-1~G-5를 번호화해 월간 로그 상단에 등록.

### 근거
- 번호가 있어야 어떤 항목이 닫혔는지 추적 가능.

### 다음 AI에게
- [x] G-1~G-5 처리 완료 — G-2 10:49, 나머지 11:49.

### 오너 결정 필요
없음

**산출물**: `9832b2a`

---

## 2026-08-18 11:02 · G-2 결과 검수 (구현 담당)

**담당**: Claude — GPT 작업물 교차검증

### 요청
> GPT가 작업 및 커밋했으니까 잘 되어있나 확인해볼래?

### 작업
- GPT G-2 수정사항을 설정서 원문과 재대조 → 확인 항목 모두 타당.
- 판단 필드 36개 확인.
- Markdown 끝 개행 누락 복구.

### 근거
- 리뷰/검증을 양쪽 AI가 겹쳐 수행.
- 출처 감사는 없는 내용을 넣은 오류와 있는 내용을 빠뜨린 오류 양쪽을 봐야 함.

### 다음 AI에게
- [x] G-3 36건 검수 → `11:49`
- [x] G-1/G-4/G-5 → `11:49`
- [ ] Markdown LF 유지
- [ ] 로컬 PowerShell 다중행 커밋은 `git commit -F`

### 오너 결정 필요
없음

### 사고 기록

PowerShell here-string 커밋 실패 뒤 성공 여부 확인 전에 `rev-parse HEAD`를 호출해 이전 SHA를 로그에 쓴 사고가 있었음.
이미 push된 히스토리는 재작성하지 않고 기록 보존. **커밋 성공 확인 후 SHA 기록**이 원칙.

**산출물**: `79d5c25` · `6f34902`

---

## 2026-08-18 10:49 · G-2 출처 정확성 재검수 및 직접 반영

**담당**: GPT

### 요청
G-2를 우선 처리. 설정서 Markdown 사본과 canon 색인을 역대조. 답변·근거·시각·다음 AI 인계·커밋까지 한 세트로 수행.

### 작업
- Canon/혼합 항목 출처 역대조.
- 정본 외 유래 규칙 추적표 확대.
- FLR/CBT/CHR/SYS/FAC/KGD/RAC의 원문 뉘앙스·출처·판단 분리.

### 근거
- D-013처럼 개발 가이드 문장이 설정서 문장으로 잘못 승격될 수 있음을 실제로 확인했기 때문.

### 다음 AI에게
- [x] G-2 닫힘
- [x] G-3/G-4/G-5 → `11:49`
- [ ] 설정서 밖 새 확정 규칙은 DECISIONS + INDEX 정본 외 유래 표 동시 갱신

### 오너 결정 필요
없음

**산출물**: `3c6f7dd`~`ec1b3a2`

---

## 2026-08-18 11:23 · Git 인계 상태 확인 및 CURRENT_STATE 동기화

**담당**: GPT

### 요청
> git 인계 확인해봐

### 작업
- 최신 커밋/로그/CURRENT_STATE/DECISIONS/BACKLOG/CHANGELOG 대조.
- CURRENT_STATE의 오너 결정 대기 상태가 최신 로그와 불일치해 동기화.

### 근거
- Git 저장소가 AI 간 공유 진실이므로 최신 상태 문서가 뒤처지면 선행조건을 오인할 수 있음.

### 다음 AI에게
- [x] G-1/G-3/G-4/G-5 → `11:49`
- [ ] CURRENT_STATE와 로그가 어긋나면 최신 커밋 기준으로 동기화

### 오너 결정 필요
없음

**산출물**: `1f19119` · `47e1b5d`

---

## 2026-08-18 11:49 · G-1 확정 + G-3/G-4/G-5 설계 감사 완료

**담당**: GPT (설계 · canon 감사 · GitHub 문서 수정) · 오너 (G-1 최종 결정)

### 요청
> 클로드는 자체 scene tree 사용을 추천하더라. 너도 괜찮다고 하면 그걸로 결정

이어서:
> 그럼 G-1은 scenetree로 결정하고 나머지 G3~G5도 진행해둬

### 작업

#### G-1 — 테스트 러너
- **자체 `SceneTree` 러너 확정**, `D-015` Resolved.
- GUT은 현재 미도입.
- 테스트 본문은 순수 함수 + 작은 assertion helper 중심으로 **프레임워크 중립** 유지.
- 러너는 발견/실행/집계/종료 코드만 담당, 실패 시 non-zero exit.
- `tests/README.md`, `TEST_CHECKLIST`, B-004, CURRENT_STATE 반영.

#### G-3 — 판단 근거 감사
- `HIS-003`: 원문 §5.5 전체 DESIGN인데 일부를 CANON으로 승격한 판단 제거.
- `CBT-007`: DESIGN → **CANON(방향) / TBD(수치)**.
- `CBT-013`: 부위 히트박스만 가능하다는 과도한 구현 고정 제거. 랜덤 크리티컬 우회 금지는 유지.
- `CHR-011`: 상성만으로 승패를 확정하는 hard lock만 금지. 강한 유불리는 TBD 수치 영역.
- `RAC-004`: 열린 목록은 CANON, 추가 종족 상세만 TBD.
- `FLR-011`: `clues[]`/자동 테스트는 원문 문장이 아니라 Canon 강제용 구현 Hard Rule임을 명시.
- **방침 유지**: 원문을 그대로 옮긴 CANON에는 존재하지 않았던 판단을 억지로 backfill하지 않는다.

#### G-4 — 내용 중복 감사

| 중복 | 정본 | 조치 |
| --- | --- | --- |
| CBT-005 ↔ ITM-003 무기 수명 | ITM-003 | CBT-005 포인터화 |
| CHR-006 ↔ SKL-003 3점 투자→스킬 후보 | SKL-003 | CHR-006은 성장값/하드캡만 |
| FAC-004 ↔ NPC-003 시설 경험 정보 전파 | NPC-003 | FAC-004는 시설 인식만 |
| WLD-004 ↔ CHR-012 보존/초기화 상세 | CHR-012 | WLD-004는 죽음=새 회차만 |

- `KGD-002↔CHR-011`, `DGN-004↔FLR-011`, `SYS-010`의 데이터 소유권 목록은 서로 다른 역할이라 유지.
- `docs/canon/README.md`가 초기 7도메인/예약 파일 설명으로 남아 실제 15도메인과 불일치한 것도 발견.
  다음 AI가 HIS/ITM/NPC/DGN 등을 미생성 파일로 오해하지 않도록 **현재 15도메인 구조로 동기화**.

#### G-5 — NPC-005 LOD 결정성
- **catch-up만으로는 충분하지 않다**고 판정.
- `D-014`, `NPC-005`, `TEST_CHECKLIST G-7`에 다음 불변식 추가:
  1. LOD는 계산 빈도/표현 해상도만 변경, Canon 결과는 동일.
  2. RNG 소비를 플레이어 거리/LOD 전환과 분리.
  3. 승격 전 catch-up 완료 후 상호작용.
  4. 동시 사건은 안정적 ID/key 순서, 월드 사건은 논리 시각에 반영/예약.
  5. `last_sim_tick` + RNG/이벤트 상태 + 예약 사건 세이브.
  6. 저LOD 전용 확률표/별도 규칙으로 대충 결과를 뽑는 방식 금지.
- 테스트: 플레이어 경로 때문에 LOD 시점이 달라져도 NPC Canon 상태와 외부 사건 로그 동일.

### 근거
- 개발 가이드 §B5: 외부 플러그인은 실제로 시간을 줄일 때만. PHASE 1~3 테스트는 프레임 독립성,
  대각선 정규화, 고정 지형, 시드 결정성, `clues[]` 등 순수 로직/데이터 단언 중심이라 자체 러너가 단순하다.
- Godot 4.x에서 `SceneTree`는 기본 MainLoop이고 `--script`로 MainLoop 스크립트 실행이 가능해 headless 러너를 만들 수 있다.
- G-3은 원문의 상태를 판단문이 몰래 승격하거나 구현 하나를 유일한 정답으로 만드는지 확인.
- G-4는 같은 사실을 두 군데서 독립 유지하는 경우만 통합하고, 다른 추상화의 교차 참조는 보존.
- G-5는 LOD에 따라 RNG 호출 횟수/동시 사건 순서가 달라질 수 있으므로 논리 시각과 결정성 상태를 관찰과 분리해야 한다.

### 다음 AI에게
- [x] **G-1~G-5 모두 닫힘.** 같은 감사를 처음부터 반복하지 말고 `docs/canon/INDEX.md` 5~7차 감사부터 읽을 것.
- [x] 다음 실개발 **PHASE 1** → `08-18 12:12`에서 착수, `12:34`에서 PLAYTESTED 완료.
- [x] PHASE 1 첫 작업에 `tests/runner.gd` + 대각선 정규화/프레임 독립성 테스트 반영됨.
- [ ] 자체 러너 유지비가 테스트보다 커지면 GUT 재검토.
- [ ] `CBT-007`은 기습 방향 Canon. 정확한 배율은 TBD.
- [ ] `CBT-013`은 랜덤 크리티컬 우회 금지, 히트박스만 유일 해법은 아님.
- [ ] NPC LOD는 D-014 G-5 불변식 + TEST_CHECKLIST G-7 준수.
- [ ] 설정서 밖 새 확정 규칙은 DECISIONS + INDEX 정본 외 유래 표 동시 갱신.
- [ ] Markdown 끝 LF 유지. 로컬 PowerShell 다중행 커밋은 `git commit -F`.

### 오너 결정 필요
없음. G-1은 이 요청에서 **자체 SceneTree로 오너가 직접 확정**했다. G-3~G-5는 새 세계 Canon 창작이 아니라
원문 상태/중복/결정성 구현 제약을 정리한 감사다.

### 산출물
- `DECISIONS.md` — `8245418`
- `docs/BACKLOG.md` — `0f4a7f7`
- `tests/README.md` — `498fa3f`
- `docs/TEST_CHECKLIST.md` — `e219111`
- `docs/canon/HIS.md` — `1a5b44c`
- `docs/canon/CBT.md` — `9bc7c94`
- `docs/canon/CHR.md` — `b88442a`
- `docs/canon/RAC.md` — `bc0eeef`
- `docs/canon/FLR.md` — `93ff55f`
- `docs/canon/FAC.md` — `b511994`
- `docs/canon/WLD.md` — `835ac97`
- `docs/canon/NPC.md` — `ee9a6f0`
- `docs/canon/INDEX.md` — `74fae69`
- `CURRENT_STATE.md` — `1877f38`
- `CHANGELOG.md` — `7e4b85e`
- `docs/canon/README.md` — `99cf252`
- 1차 로그 커밋 — `428a7a0` (실제 커밋 시각 11:49 KST 확인 후 본 항목 시각 정정)
- 본 로그 정정 커밋 — `6890e20`

---

## 2026-08-18 12:12 · PHASE 1 착수 — 이동/카메라 첫 구현 묶음

**담당**: Claude (구현)

### 요청
> 인계서 확인해봐. 이제 phase1 착수하면 돼

### 작업
착수 전 무결성 확인: canon ID 중복 0 · 141항목 · 판단 39개 · 끝 개행 정상 · `D-015` 계약 확인.

1. **`tests/runner.gd`** — 자체 SceneTree 단위 러너 (D-015). 발견/실행/집계/종료 코드만 담당.
2. **`scripts/player/movement.gd`** — 이동 계산을 노드 없는 순수 함수로 분리.
3. **`tests/test_movement.gd`** — 17 단언. 대각 정규화, 아날로그 세기 보존, 프레임 독립성.
4. **`tests/integration_runner.gd`** — 물리가 필요한 충돌 검증용 별도 러너. 10 단언.
5. **`scripts/player/player.gd` + `scenes/player/Player.tscn`** — `CharacterBody2D`, Camera2D 포함.
6. **`scenes/world/TestRoom.tscn`** — 960×640 방 + 안쪽 장애물 3개.
7. **`project.godot`** — `move_left/right/up/down` (WASD + 방향키, physical_keycode).

검증 결과:
- 단위 `runner.gd` → 17 단언 / 실패 0 / exit 0
- 통합 `integration_runner.gd` → 10 단언 / 실패 0 / exit 0, 5케이스 전부 목표 벽 정확히 도달
- headless 씬 실행 exit 0, 창 실행(ANGLE) exit 0, 스크립트 에러 0

### 근거
- **이동 계산을 노드에서 떼어냈다.** `Movement`가 순수 함수라 프레임 독립성·대각 정규화를
  물리 없이 headless로 검증할 수 있다. 노드 안에 넣었으면 PHASE 1 완료 조건 두 개를
  자동 테스트할 방법이 없었다. (`D-015` 원칙 2)
- **러너를 단위/통합 둘로 나눴다.** 충돌 검증은 물리 프레임을 여러 번 돌려야 하는데,
  이걸 단위 러너에 넣으면 러너가 비동기·생명주기를 알아야 해서 `D-015` 원칙 1
  (러너는 발견·실행·집계/종료만)이 깨진다.
- **`move_and_slide()`에 delta를 곱하지 않았다.** Godot 4는 `velocity`를 픽셀/초로 해석하고
  내부에서 delta를 곱한다. 여기서 또 곱하면 프레임률에 따라 속도가 흔들려 1-2가 깨진다.
  코드에 주석으로 박아뒀다.
- **`normalize_direction`은 길이가 1을 넘을 때만 자른다.** 무조건 `normalized()`를 부르면
  아날로그 입력의 세기가 사라진다. 테스트로 고정했다.
- **가속을 넣지 않았다.** 설정서에 조작감 규정이 없고 `CBT-009`의 보정 항은 PHASE 6이다.
  미끄러짐 없는 즉시 반응이 기준점으로 적절하며, 조정 방향은 오너 PLAYTEST 후에 정한다.
- **TileMapLayer를 쓰지 않았다.** PHASE 1 완료 조건은 "벽 안 뚫고 부드럽게 이동"뿐이고
  타일셋 도입은 PHASE 2의 1층 지형 포맷 설계와 함께 가는 것이 맞다. (`FLR-001`)
- `BASE_SPEED = 160.0`은 **DESIGN이지 canon이 아니다.** 32px 타일 기준 초당 5타일.
  확정되면 `data/`로 뺀다.

### 발견하고 고친 것 2건
1. **러너 오탐** — `tests/test_case.gd`(헬퍼)가 `test_*.gd` 패턴에 걸려 테스트로 오인됐다.
   → `tests/lib/`로 옮겼다. 탐색이 재귀하지 않으므로 구조적으로 재발하지 않는다.
2. **통과하지만 검증하지 않던 테스트** — 오른쪽 벽(x=950) 케이스가 안쪽 블록(x=608)에 막혀
   x=598에서 멈췄는데 `x <= 950`이 참이라 **그냥 통과했다.**
   → 각 케이스에 **"목표 벽에 실제로 닿았는지"** 단언을 추가하고 출발점을 장애물 없는 경로로
   바꿨다. 안쪽 블록 케이스도 따로 추가했다.
   `TEST_CHECKLIST`와 러너 주석에 "두 단언을 쌍으로 넣으라"고 남겼다.

### 다음 AI에게
- [x] **PHASE 2로 넘어가기 전 오너 PLAYTESTED(1-4·1-5)** → `12:34`에서 통과.
- [ ] 조작감 조정은 `Player.BASE_SPEED`와 가속 유무부터 건드린다. 둘 다 DESIGN이라 자유롭게 튜닝 가능
- [ ] 통합 테스트 케이스 추가 시 **"경계 안 넘음" + "목표에 실제 도달" 두 단언을 쌍으로** 넣을 것
- [ ] PHASE 2 착수 시 `TestRoom.tscn`은 버리고 `data/floors/floor1_fixed/` 로더로 교체한다.
      테스트 방은 canon 지형이 아니다

### 오너 결정 필요
처리 완료 — `12:34`에서 PHASE 1 PLAYTESTED 통과.

**산출물**: 커밋 `03bc783`

---

## 2026-08-18 12:34 · PHASE 1 PLAYTEST 1차 반영 — 카메라 드래그 여백

**담당**: 오너 (PLAYTEST) · Claude (구현)

### 요청
오너 1차 플레이테스트 소감 (원문):
> 속도는 당장은 조금 느린 것 같기는 한데, 어차피 이후에 민첩 스탯의 증가나 스킬의 영향에 의해
> 빨라지는 것을 고려하면 오히려 조금 더 느려도 괜찮을 수도 있다고 생각해
> 그냥 이동할때는 괜찮은데 짧은 이동을 반복할 때 카메라 이동이 조금 어지러운감이 있네
> 나머지는 괜찮은 것 같아

### 작업
- **카메라**: `Camera2D`에 드래그 여백 추가. 좌우 `0.2`(≈±128px, 4타일), 상하 `0.2`(≈±72px).
  `position_smoothing_speed` 8.0 → 10.0.
- **속도**: `BASE_SPEED = 160.0` **유지.** 대신 코드 주석에 "이 값은 민첩 10 기준선"과
  오너의 판단 근거를 명시.
- 자동 테스트 재실행 — 단위 17단언 / 통합 10단언 전부 통과, 창 실행 exit 0.

### 근거
- **어지러움의 원인은 smoothing 단독 사용이다.** 드래그 여백이 없으면 카메라가 항상
  플레이어를 뒤쫓으므로, 짧은 왕복 이동에서 **카메라가 따라가다 방향을 바꾸길 반복해
  화면이 헤엄친다.** 긴 이동은 한 방향으로만 추적하니 증상이 안 나타난다 —
  오너가 말한 "그냥 이동할 땐 괜찮은데 짧은 이동을 반복할 때"와 정확히 일치한다.
- 그래서 smoothing 속도만 올리는 대신 **드래그 여백(deadzone)** 을 넣었다. 짧은 이동은
  여백 안에서 끝나 카메라가 아예 움직이지 않는다. 증상의 원인을 없애는 쪽이다.
- **속도를 올리지 않은 것은 오너 판단을 따른 것이고, canon 근거도 있다.**
  `CBT-009`상 이동 속도는 민첩·[신속] 계열로 **빨라지는** 방향이다. 기준선을 넉넉히 잡으면
  성장 여지가 사라지고 나중에 스킬 효과를 억지로 깎아야 한다.
  `CHR-003`(인간 평균 각 10)에 맞춰 **"민첩 10인 유배자의 기준선"** 이라고 코드에 못 박았다.
- **민첩 → 이동 속도 매핑은 만들지 않았다.** `CHR-009`(파생 능력식) TBD다.
  PHASE 6에서 그 식이 정해질 때 함께 정한다.

### 오너 판정 — PHASE 1 PLAYTESTED 통과

2차 플레이테스트에서 오너가 확정했다 (원문):
> 응 훨씬 좋네 이대로 확정지어도 괜찮을 것 같아

이로써 `TEST_CHECKLIST` **1-4(조작감)·1-5(카메라)가 PLAYTESTED**가 되었고,
1-1~1-3은 이미 VERIFIED이므로 **PHASE 1 완료 조건이 전부 충족**됐다.
드래그 여백 `0.2` / smoothing `10.0`을 확정값으로 둔다.

원인 진단이 맞았음이 확인된 셈이다 — smoothing 속도만 올렸다면 헤엄이 빨라졌을 뿐
사라지지 않았을 것이다. 증상(어지러움)이 아니라 원인(여백 없는 상시 추적)을 고친 것이 주효했다.

### 다음 AI에게
- [x] 카메라 여백 수치 — 오너 확정 (여백 `0.2`, smoothing `10.0`)
- [ ] `BASE_SPEED`를 올리려면 **성장 여지를 먼저 계산할 것.** 주석에 이유를 적어뒀다
- [ ] **기본 속도는 PHASE 6에서 재확정한다.** 민첩 보정(`CHR-009` 확정 후)이 붙은 뒤
      실제 체감으로 정하는 편이 정확하다. 지금 값은 잠정이 아니라 "민첩 10 기준선"이다
- [ ] **PHASE 2 착수는 오너 지시를 받은 뒤.** 착수 시 `TestRoom.tscn`을 버리고
      `data/floors/floor1_fixed/` 로더로 교체한다. 테스트 방은 canon 지형이 아니다 (`FLR-001`)

### 오너 결정 필요
없음 — PHASE 1 PLAYTESTED 확정. 다음은 PHASE 2 착수 지시 대기.

**산출물**: 커밋 `64f1b2d`

---

## 2026-08-18 13:00 · PHASE 1 구현 독립 검증

**담당**: GPT (설계 · 코드/테스트/Canon 교차검증)

### 요청
> 클로드 페이즈1 작업 끝났어, 검증해봐

### 작업
- PHASE 1 전 기준점 `6890e20` → 완료 상태 `96f2138`의 5개 커밋과 23개 변경 파일을 대조.
- `COLLABORATION_PROTOCOL`의 완료 등급, `D-015`, `TEST_CHECKLIST` 1-1~1-5, 개발 가이드 PHASE 1 완료 조건을 실제 코드와 비교.
- 다음 구현을 직접 읽음:
  - `tests/runner.gd`, `tests/lib/test_case.gd`, `tests/test_movement.gd`, `tests/integration_runner.gd`
  - `scripts/player/movement.gd`, `scripts/player/player.gd`
  - `scenes/player/Player.tscn`, `scenes/world/TestRoom.tscn`, `Main.tscn`
  - `project.godot`, `tools/run.ps1`
- GitHub Actions/commit status도 확인했으나 별도 CI workflow/status는 없었다.
- README/CURRENT_STATE/CHANGELOG의 오래된 인계 문구와 README의 `133항목` 표기를 발견해 현재 상태로 동기화.

### 판정

**PHASE 1 완료(`PLAYTESTED`) 판정 유지. 차단 결함 없음.**

확인한 이유:
- 실제 `Player`는 `CharacterBody2D.velocity`에 px/s 단위 속도를 넣고 `move_and_slide()`를 호출하며 delta를 중복 곱하지 않는다.
- `Movement.normalize_direction()`은 길이 1을 넘는 입력만 정규화해 대각 가속을 막고 아날로그 세기를 보존한다.
- TestRoom의 외벽/내부 블록 collision 좌표와 integration test의 기대 접촉 좌표가 일치한다.
- `BASE_SPEED=160`을 DESIGN으로만 두고 민첩→속도 매핑(`CHR-009` TBD)을 임의 생성하지 않았다.
- 1층 Canon 지형을 PHASE 1 테스트 방으로 오인하지 않도록 `TestRoom`이 임시임을 명시했다.
- 카메라 drag margin/smoothing은 오너 2차 PLAYTEST에서 승인된 기록이 있다.
- 새로운 Canon/TBD를 조용히 확정한 구현은 발견하지 않았다.

### 발견 — 비차단 회귀 테스트 부채

#### P1-TEST-001 · 실제 Player 경로의 프레임 독립성을 자동 테스트가 직접 물지 않는다

`test_movement.gd`의 30/60/120 FPS 및 jitter 테스트는 `Movement.step(position, velocity, delta)`를 검증한다.
그러나 실제 `player.gd`는 `Movement.step()`을 호출하지 않고 `velocity = desired_velocity(...)` 후 `move_and_slide()`를 사용한다.
따라서 **현재 코드는 맞지만**, 나중에 `player.gd`에 실수로 delta 중복 곱이 들어가도 이 단위 테스트는 계속 통과할 수 있다.

→ 실제 Player/Input/physics 경로를 거치는 최소 E2E 이동 회귀 테스트를 추가할 것.

#### P1-TEST-002 · integration runner가 harness와 테스트 케이스를 한 파일에 가진다

`integration_runner.gd`는 물리 프레임 생명주기뿐 아니라 벽/블록 5개 케이스와 기대 좌표/단언까지 모두 직접 보유한다.
지금 규모에서는 정상 동작하지만 PHASE 2부터 통합 테스트가 늘면 D-015의 "러너 책임을 작게 유지" 원칙이 흐려진다.

→ 물리 harness와 `test_player_collision` 같은 테스트 케이스를 분리하는 것을 권장.

#### P1-TOOL-001 · Godot 버전 확인 (낮은 우선순위)

`tools/run.ps1`은 PATH의 `godot`을 우선하고, 없으면 WinGet 설치본 이름순으로 고른다.
여러 버전이 설치되면 프로젝트 기준 4.7.1이 아닌 Godot로 테스트할 가능성이 있다.

→ 실행 전 `--version` 확인/경고를 넣으면 재현성이 좋아진다. PHASE 1 차단 사항은 아니다.

### 근거
- `D-015`: 테스트 본문은 가능한 한 프레임워크 중립, runner/helper 유지비가 커지면 구조 재검토.
- 개발 가이드 PHASE 1: 벽을 뚫지 않는 연속 이동, 프레임 변화에 속도 불변이 완료 조건.
- 협업 프로토콜 §5: 조작감/카메라의 최종 PLAYTESTED는 오너 권한이며, 오너 승인 로그가 존재한다.
- Godot 4.7 공식 문서에서 `CharacterBody2D.velocity`는 px/s이며 `move_and_slide()`가 delta를 내부 처리한다.
  Camera2D drag margin도 target이 margin까지 이동한 뒤 camera가 움직이는 deadzone 방식이다.

### 검증 한계
이 GPT 세션에는 Godot 실행 바이너리가 없고 컨테이너의 GitHub DNS도 사용할 수 없어 **내가 런타임을 다시 실행하지는 못했다.**
구현 담당이 기록한 단위 17/통합 10 단언 exit 0과 창 실행 결과, 오너의 실제 PLAYTEST 기록을 확인했고,
이번 검수는 GitHub 코드/diff·테스트 설계·Canon 정합성의 독립 리뷰다.

### 다음 AI에게
- [x] PHASE 1 자체의 완료/PLAYTESTED 판정은 유지한다. 같은 Phase를 다시 처음부터 검수하지 않는다.
- [x] **P1-TEST-001** → `13:28`에서 실제 Player/Input/physics E2E 회귀 테스트로 해소.
- [x] **P1-TEST-002** → `13:28`에서 harness/test case 분리.
- [x] **P1-TOOL-001** → `13:28`에서 Godot 4.7.1 버전 확인/경고 추가.
- [ ] PHASE 2는 D-016 확정 뒤 시작.
- [ ] PHASE 2에서 `TestRoom.tscn`은 Canon 지형으로 승격하지 말고 `data/floors/floor1_fixed/` 로더로 교체.
- [ ] `BASE_SPEED`는 PHASE 6에서 `CHR-009`가 확정된 뒤 민첩 보정과 함께 재확정.

### 오너 결정 필요
없음. 현재 발견한 것은 Canon 결정이 아니라 자동 테스트/도구 구조의 보강 사항이다.

### 산출물
- README 인계 순서/Canon 수 정정 — `ae9e419`
- CURRENT_STATE 독립 검수 반영 — `b284d3d`
- TEST_CHECKLIST 회귀 커버리지 정밀화 — `a0fe9f6`
- CHANGELOG 상태 동기화 — `90f8ea5`
- 본 월간 로그 항목 — 첫 기록 커밋 `cf7b85c` (GitHub 실제 시각 13:00 KST)
- 시각 정정 — `eb45598`

---

## 2026-08-18 13:28 · GPT 검수 지적 3건 해소 — 변이 테스트로 검증

**담당**: GPT (지적) · Claude (구현·검증)

### 요청
> GPT가 내용 남겼으니까 확인해봐 테스트 진행 후에 페이즈 2 진행하면 될거야

GPT의 PHASE 1 독립 검수에서 회귀 테스트 부채 3건(`P1-TEST-001/002`, `P1-TOOL-001`)이 지적됐다.

### 작업
**먼저 지적이 실제로 유효한지 증명했다.** `player.gd`에 `velocity *= delta`(delta 중복 곱)를
일부러 주입하고 기존 테스트를 돌렸다.

| 시점 | 단위 | 통합 | 결과 |
| --- | --- | --- | --- |
| 보강 전 | 17단언 통과 | 10단언 통과 | **버그를 못 잡음** |
| 보강 후 | 17단언 통과 | **4단언 실패** | 잡음 |

보강 전에는 플레이어가 초당 2.7px로 기어가는 상태인데도 27단언이 전부 초록불이었다.

해소:
- **P1-TEST-001** — `tests/integration/test_player_movement_e2e.gd` 신규.
  `Input.action_press()` → `Player._physics_process()` → `move_and_slide()`의 **실제 경로**로
  30/60/120 tick 이동 거리와 대각 정규화를 검증한다. 벽 간섭을 피해 빈 공간에서 측정.
- **P1-TEST-002** — `integration_runner.gd`를 harness로만 남기고 케이스를
  `tests/integration/test_player_collision.gd`로 분리. 케이스 규약은
  `run(tree, t)` + `await tree.physics_frame`.
- **P1-TOOL-001** — `tools/run.ps1`이 `--version`을 읽어 `4.7.1`이 아니면 경고(차단은 안 함).

부수적으로 `body->get_space() is null` 에러도 잡았다 — 노드를 붙인 직후 물리 공간에
들어가기 전에 `move_and_slide()`를 불렀다. 한 프레임 넘기고 시작하도록 고쳤다.

최종: 단위 17단언 + 통합 17단언(충돌 10 + E2E 7) 전부 통과, exit 0.

### 근거
- **지적을 받아들이기 전에 변이 테스트로 확인했다.** "테스트가 부족하다"는 주장은
  실제로 버그를 통과시키는지 보여야 근거가 된다. 보여줬고, 지적이 옳았다.
- **E2E를 빈 공간에서 측정한다.** 벽에 막히면 프레임률 비교가 무의미해진다.
  충돌은 `test_player_collision.gd`가 따로 본다 — 한 테스트가 두 가지를 재면 실패 원인이 흐려진다.
- **버전 확인은 경고만 하고 막지 않는다.** 의도적으로 다른 버전을 시험할 수도 있다.
- 이 사건의 교훈을 `TEST_CHECKLIST`와 E2E 파일 상단 주석에 남겼다 —
  **아무도 호출하지 않는 함수를 검사하는 테스트는 초록불만 줄 뿐 아무것도 지키지 않는다.**

### 다음 AI에게
- [ ] 테스트를 추가할 때 **"실제로 실행되는 경로를 통과하는가"** 를 항상 확인할 것.
      의심스러우면 변이 테스트(일부러 버그 주입 → 실패하는지 확인)로 검증한다
- [ ] 통합 케이스는 `tests/integration/`에 넣는다. harness(`integration_runner.gd`)에 로직을 넣지 않는다
- [ ] 노드를 root에 붙인 직후 물리 API를 부르면 `body->get_space() is null`이 난다.
      `await tree.physics_frame` 한 번 넘기고 시작할 것

### 오너 결정 필요
없음

**산출물**: 커밋 `9623eec` · 로그 해시 기록 `7a0b5b0`

---

## 2026-08-18 13:48 · PHASE 2 설계안 작성 — D-016 Proposed

**담당**: Claude (구현)

### 요청
> 설계안으로 먼저 md파일 작성하고 gpt랑 같이 토의해보자

(선행: 구현 담당이 "PHASE 2는 포맷을 정하는 일이라 되돌리기 비싸니 설계안을 먼저 올리는 게
맞다"고 제안했고 오너가 승인.)

### 작업
- `docs/design/PHASE2_FLOOR1_FORMAT.md` 작성 — canon 제약 매핑, 쟁점 4개, 데이터 구조 초안,
  오너 결정 5문항, 티켓 분해 7개, 이 설계안의 한계.
- `DECISIONS.md` `D-016` Proposed 등록.

### 근거
- **가장 중요한 발견: `SYS-009` Tier 1이 포맷 수준에서 깨질 수 있다.**
  `SYS-014`가 1층 지형 은닉 불가를 이미 인정했으므로 슬롯 좌표 공개는 canon이 받아들인 결과다.
  그러나 슬롯 파일에 `trap_type`을 적으면 "정답 테이블을 배포 파일에 두지 않는다"가 깨진다.
  → 슬롯(좌표·모양)과 인스턴스(타입·상태)를 포맷이 **강제로** 분리하게 설계했다.
  사람이 조심하는 것에 맡기면 언젠가 깨진다.
- **`FLR-011`(치명 함정 단서 필수)이 여기서 함정이 된다.** 단서가 슬롯(고정)에 붙으면
  단서만 보고 함정 타입을 역산할 수 있다. 단서도 인스턴스 쪽에 있어야 한다.
  `SYS-009` Tier 2의 "진짜에만 적용되는 규칙이 판별기가 된다"와 같은 구조의 함정이다.
- **지형 저작 포맷은 처음 판단을 뒤집었다.** 텍스트 그리드(diff 가독성)를 권하려 했으나
  TileMapLayer 씬으로 바꿨다 — 1층은 고정 지형이라 **레이아웃 완성도가 곧 게임의 질**이고
  (`FLR-015`, `CHR-014`), 문자로 그리게 하면 그 작업이 고통스러워져 결과가 나빠진다.
  단점(diff 가독성 상실)은 인정하고 지형 해시 테스트로 완화하는 안을 함께 냈다.
- **결정성은 "결과 저장"을 권고했다.** `SYS-003`의 핵심은 "플레이어가 보지 않았다고 다시
  생성되지 않는다"인데, 시드 재생성 방식은 배치 로직을 한 줄 고치는 순간 과거 세이브의
  세계가 바뀐다. **위반했다는 사실조차 드러나지 않는다**는 점이 가장 나쁘다.
- **계단은 임의로 정하지 않았다.** 설정서 §6.1이 "파티 귀속"까지만 말하고 후보 지점 고정 여부와
  결정 시점은 침묵한다. Q2·Q3으로 올렸다.

### 다음 AI에게
- [ ] **`D-016`이 Resolved 되기 전에 PHASE 2 구현을 시작하지 말 것.**
- [x] GPT 검토 → `13:34`에서 완료. 설계 문서의 GPT 검토 참고.
- [x] 오너 1차 결정/새 세계 공간 설명 → `14:17`에서 반영.

### 오너 결정 필요
- 당시 Q0~Q5 → `14:17`에서 대부분 해소. 현재 남은 설계는 최신 항목 참고.

**산출물**: 커밋 `263ae18` · 로그 해시 기록 `73fe7bb`

---

## 2026-08-18 13:34 · D-016 PHASE 2 설계안 GPT 교차검토

**담당**: GPT (설계 · Canon/데이터 구조 검토)

### 요청
> 클로드가 페이즈2 넘어가기 전에 설계안을 먼저 작성하고 토의해보는게 좋을 것 같다고 해서 작성되어 있을거야
> 그거 봐봐

### 작업
- `docs/design/PHASE2_FLOOR1_FORMAT.md` 전체 검토.
- 설정서 §6.1, `D-003`, `FLR-001/002/011`, `FAC-001/002/005~007`, `SYS-003/009/014`,
  `DATAMINING_POLICY.md`, `TEST_CHECKLIST`와 교차대조.
- 설계 문서에 GPT 검토 결과를 추가.
- `CURRENT_STATE.md`와 `DECISIONS.md D-016`을 "GPT 검토 완료 / 오너 결정 대기"로 동기화.

### 판정

**큰 방향은 좋지만 당시 D-016을 그대로 Resolved 하면 안 된다고 판정했다.**

- `FLR-002/D-003`과 `SYS-014` 사이의 함정 타입 랜덤 범위 모순 발견.
- 설정서에 없는 고정 계단 후보 목록을 `SYS-014`가 Canon처럼 적은 문제 발견.
- `FloorState`와 불변 지형 정의를 분리하는 `FloorDefinition` 권고.
- 계단 수명주기 / tier_hint 없음 / 생성 결과 저장을 권고.

### 후속 처리

- [x] 함정 타입 모순 → `14:17` 오너가 **위치·종류·구조 고정**으로 확정.
- [x] 고정 계단 후보 → `14:17` 오너가 **층 진입 시 실제 공간을 계산**하는 방향으로 대체.
- [x] 계단 시점 → 층 진입 시.
- [x] tier_hint → 없음.
- [x] 결과 저장 → 채택.
- [x] 새 실제 월드/지형 물질성을 포함한 최종 PHASE 2 구조 → D-016 최종 Resolved.

### 산출물
- `docs/design/PHASE2_FLOOR1_FORMAT.md` GPT 검토 — `43eff77`
- `CURRENT_STATE.md` — `8f0ac67`
- `DECISIONS.md` — `cec92ae`
- 로그 기록 최초 커밋 — `b4b7d4a` (실제 GitHub 시각 13:34 KST; 기존 13:32 표기를 본 업데이트에서 정정)

---

## 2026-08-18 14:17 · D-016 오너 1차 결정 + 실제 월드 공간/지형 Canon 복원

**담당**: 오너 (설정·최종 권한) · GPT (원문 대조·설계 보완·GitHub 반영)

### 요청
오너가 D-016 검토에 대해 다음을 직접 확정/재설명했다.

- **1층 함정**: 위치와 종류는 고정. 활성화 여부·소모/루팅 상태 같은 것만 변수.
- **계단**: 층 진입 시 스토리/공간을 바탕으로 "플레이어 입장에서 빡치는 위치"를 계산하고 싶음.
  메인 스토리 연관성 + 쉬운 층 스킵 방지 + 플레이어 허용 맵 규모 대비 충분한 거리를 포함.
  정상 바닥일 필요 없이 보스 침소 아래나 동굴 천장 위 공동 등도 가능.
  생성 후 지형이 사라져도 좌표에 고정되고 파괴 불가.
- **지형**: 절대 지형이 아니라 실제 물질/지질처럼 내구도를 가지고 힘·도구·시간에 따라 파괴 가능.
  실제 값은 필요할 때 부여해도 됨. 힘 10 일반인이 숟가락으로 1m를 파는 식의 작업은 현실적으로 매우 오래 걸려야 함.
- **층/맵의 끝**: 한 층은 실제 대륙/행성의 특정 위치 중 유배자에게 허용된 일부.
  경계 밖에도 실제 환경/생태/NPC가 존재하며 NPC/야생동물은 경계를 자유롭게 통과.
  유배자별 허용 범위는 겹칠 수도/안 겹칠 수도 있음. 큰 범위는 **와이드 맵**.
- **튜토리얼 홀짝층**: 홀수층은 개인 목표, 짝수층은 공통 목표. 홀수층도 영역이 겹치면 다른 유배자와 만날 수 있고,
  짝수층은 협동/PvP/경쟁/생존 인원 제한 등의 목표가 가능하며 같은 맵 끝을 공유하는 경우가 많음.
- **계단 시점**: 층 진입 시가 맞음. 파티 수명주기 보완안 수용 가능.
- **tier_hint**: 없음.
- **저장**: 생성 결과 저장안 채택.
- 추가 요청: 작업 전에 `docs/log/2026-08.md`를 제대로 읽었는지 재확인.

### 작업

#### 월간 로그/원문 재확인
- `docs/log/2026-08.md`를 최신본 기준으로 **처음부터 끝까지 다시 읽음**.
- 13:28 항목 아래 오너가 몰래 넣어둔 메모도 확인해 첫 번째 AI 읽음 표식을 남김.
- `docs/SETTING_BIBLE_v1.1.md` 전문을 다시 검색/대조.
- 확인 결과:
  - §7.2의 **보이지 않는 행동 반경**은 v1.1에 존재.
  - **와이드 맵 / 실제 세계 지속성 / NPC 경계 통과 / 유배자별 영역 중첩 / 홀수·짝수층 규칙**은
    현재 v1.1 전문에서 찾을 수 없어 통합 과정 누락으로 판단. 오너 직접 설명을 우선 출처로 복원.

#### Canon/Decision 반영
- `SYS-014`: "함정 종류 시드 랜덤" / "계단 후보 지점 고정" 과해석 정정.
- `FLR-001/002`: 함정 위치·종류·구조 고정, 동적 상태만 변화로 정밀화.
- `FLR-017`: 맵의 끝을 실제 월드 경계가 아닌 **유배자 접근 경계**로 정밀화.
- 신규:
  - `FLR-023` 실제 월드 일부인 층
  - `FLR-024` 유배자별 행동 반경 / NPC·야생동물 통과 / 영역 중첩
  - `FLR-025` 와이드 맵
  - `FLR-026` 홀수층 개인 목표 / 짝수층 공동 목표
  - `FLR-027` 지형 물질성·파괴 가능
  - `FAC-012` 계단 층 진입 시 생성
  - `FAC-013` 계단 월드 좌표 고정·파괴 불가
- `D-017` Resolved로 세계 공간/행동 반경/지형 물질성 복원 기록.
- Canon INDEX 141 → **148 ID**로 갱신.
- 전체 행성 지질/굴착/붕괴 완전 구현은 `B-007`로 분리하되 PHASE 2에서 공간 주소/material/mutation hook은 확보하도록 기록.

#### D-016 설계 v2
- 최초안의 `trap type 랜덤`, `stair_candidates[] 고정`, `map_bounds = 세계 끝` 전제를 폐기.
- `docs/design/PHASE2_FLOOR1_FORMAT.md`를 v2 토의본으로 재작성.
- 별도 `docs/design/PHASE2_WORLD_SPACE_EXTENSION.md`에 오너 확정과 GPT 제안을 분리해 기록.

### 근거

#### 계단 계산 — 실시간 성능보다 권한/역산/소프트락이 문제
계단은 매 프레임이 아니라 **층 진입 시 한 번** 계산하므로 연산량 자체는 충분히 감당 가능하다.
전 행성 모든 좌표를 LLM에 넘기지 않고 현재 접근 영역 + 정상 진행으로 열릴 영역에서 POI/보스/막다른 길/
숨은 공동/위험 구역 등 **희소 후보 집합**만 만들면 된다.

GPT는 "수학적으로 가장 점수가 높은 한 점"을 항상 고르는 대신 **공정한 악의(adversarial but solvable)**를 권고:

1. 엔진이 합법 후보를 생성.
2. 스토리 정합성·최종 도달 가능·안티 스킵·시간 예산 등 Hard Constraint로 거름.
3. AI는 선택적으로 후보의 스토리 의미/귀찮음 정도를 구조화 Proposal로 rank.
4. 엔진 Validator가 최종 검증.
5. 상위 점수 밴드에서 seed 기반 가중 선택 후 결과 저장.
6. AI가 꺼져도 엔진 scorer로 fallback (`SYS-007`).

Soft score 후보: story relevance / skip resistance / 허용 영역 대비 route cost percentile /
risk exposure / discovery friction / terrain access cost / novelty.
정확한 가중치·percentile은 DESIGN.

#### 지형 — 전체 행성을 미리 복셀화하지 않는다
"전 세계가 실제로 존재"한다는 Canon은 **모든 좌표가 결정적으로 주소 가능**하다는 의미로 구현할 수 있다.
지질 profile/region seed/역사적 override에서 필요한 chunk/layer만 lazy materialize하고,
실제 파괴된 부분만 mutation delta로 저장하면 된다.

현실적인 파괴는 완전한 공학 시뮬레이션 대신 대략
`material resistance × volume × structural factor` 대 `STR 작업능력 × tool efficiency × 보정`의
작업량 모델을 권고. 정확한 물성/수치는 TBD.

#### 1층 고정과 파괴 가능성은 충돌하지 않음
`FLR-001`은 **초기 정의 고정**, `FLR-027`은 플레이 후 동적 mutation이다.

### 다음 AI에게

- [x] `2026-08.md` 전문 재열람 완료. 같은 질문을 다시 하지 말 것.
- [x] **13:28 아래 숨은 오너 메모 + GPT 읽음 표식 삭제 완료** (Claude, 2026-08-18 15:35). → 항목 `08-18 15:35`
- [x] 함정 랜덤 범위 오류 정정 완료.
- [x] D-017 + FLR-023~027 + FAC-012/013 + INDEX 148 반영 완료.
- [x] `D-016` 최종 Resolved. PHASE 2 착수 완료.
- [x] Claude의 Godot 과설계 교차검토 — **D-016 확정 전에 하지 못했다.** P2-T1 구현 후 뒤늦게 수행. → 항목 `08-18 15:35`
- [ ] PHASE 2에서 완전 행성 지질 엔진을 만들지 말고 B-007로 분리. 다만 공간 주소/material/mutation hook은 막지 않는다.

### 오너 결정 필요

이 항목 작성 당시 남았던 4건은 이후 모두 확정되어 `D-016/D-017`에 반영됨.

### 산출물
- `docs/design/PHASE2_WORLD_SPACE_EXTENSION.md` — `d207389`
- `docs/canon/SYS.md` — `42d4d19`
- `docs/canon/FLR.md` — `59ccf3d`
- `docs/canon/FAC.md` — `394af5c`
- `DECISIONS.md` — `102f14b`
- `docs/canon/INDEX.md` — `a43670f`
- `CURRENT_STATE.md` — `6721f5e`
- `docs/BACKLOG.md` — `abb0cf2`
- `docs/design/PHASE2_FLOOR1_FORMAT.md` v2 — `8fac078`
- 본 로그 커밋

---

## 2026-08-18 15:10 · PHASE 2 착수 — P2-T1 공간 데이터 타입

**담당**: Claude (구현)

### 요청
> gpt랑 검토하고 인계서 적었으니까 읽어보고 페이즈2 진행해봐

### 작업
착수 전 확인: `PHASE2_IMPLEMENTATION_HANDOFF.md`(구현 정본) · `D-016` Resolved ·
`D-017` 신규 · `FLR-023~027` · `FAC-012/013`.

`P2-T1` 구현 — 완료 조건은 **"같은 좌표의 물리 상태가 유배자 ID에 따라 복제되지 않는다"**:

- `scripts/world/world_anchor.gd` — 월드 공간 주소 (world/region/cell/layer)
- `scripts/world/causal_source.gd` — 인과 주체 판정
- `scripts/world/access_envelope.gd` — 유배자별 행동 반경 + 경계 통과 판정
- `scripts/world/terrain_mutation_state.gd` — **월드 소유** 지형 변경 델타
- `tests/test_world_space.gd` — 33단언

최종: 단위 50단언(이동 17 + 공간 33) + 통합 17단언 전부 통과, exit 0.

### 근거
- **`WorldAnchor`를 `Vector2i`로 두지 않았다.** `FAC-013`상 계단은 그 자리의 바닥·벽이
  굴착·붕괴로 사라져도 좌표와 존재가 유지돼야 한다. 주소가 타일 데이터에 종속되면
  `FLR-027`(지형은 파괴 가능)과 정면으로 부딪힌다. `world/region/cell/layer`로 분리했고,
  `layer`는 `FAC-012`의 "정상 바닥이 아닌 천장 위 공동·지하"를 표현하기 위한 것이다.
- **경계 통과 판정을 `kind`가 아니라 "소유자 유무"로 했다.** `Kind` 목록으로 판정하면
  새 종류가 추가될 때마다 목록을 고쳐야 하고, **빠뜨리면 조용히 경계를 통과한다.**
  소유자 유무는 그런 실수가 구조적으로 불가능하다.
  덕분에 "같은 NPC라도 스스로 걸으면 통과, 유배자가 밀치면 차단"이 자연스럽게 나온다.
- **`TerrainMutationState`를 `scripts/world/`에 두고 `AccessEnvelope`에 지형 API를 두지 않았다.**
  `FLR-024`상 물리 상태는 월드가 소유하고 공유한다. 파일 위치 자체가 소유권 경계의 표현이고,
  테스트가 `AccessEnvelope`에 `record`/`has_mutation`이 **없음**을 단언한다 —
  있으면 언젠가 거기에 유배자별 복제본이 생긴다.
- **`to_sorted_records()`로 순회 순서를 고정했다.** Dictionary 순회 순서에 의존하면
  같은 상태가 다른 세이브를 만들어 `SYS-003` 결정성이 조용히 깨진다.
  삽입 순서가 달라도 같은 결과가 나오는지 테스트로 고정했다.
- `Rect2i`를 경계 정본으로 삼지 않았다 (`D-016` §1.5). 셀 집합 + 레이어 범위로 두어
  나중에 폴리곤·높이 밴드로 확장할 여지를 남겼다.

### 발견하고 고친 것 2건
1. **새 `class_name`이 임포트 전에는 전역 등록되지 않는다.** 테스트가 "not declared in the
   current scope"로 무더기 파스 에러를 냈다. → `run.ps1 -Mode test`가 `--import`를 먼저 하도록 수정.
2. **러너가 파스 에러에 죽었다.** `load()`가 인스턴스화 불가능한 스크립트를 돌려주는데
   `new()`를 불러 러너 자체가 중단되고 나머지 테스트가 실행되지 않았다.
   → `can_instantiate()` 확인 후 해당 파일만 실패 처리. 두 러너 모두 적용.

### 다음 AI에게
- [x] `P2-T0` 가드 테스트 → `16:05`
- [ ] `P2-T2` greybox + loader 시작 시 `TestRoom.tscn`을 폐기한다 (`FLR-001`)
- [ ] `AccessEnvelope`는 아직 **데이터 모델만**이다. 실제 Player 이동에 연결하는 것은 `P2-T4`
- [ ] 지형 굴착 물리는 PHASE 2 범위 밖 (`B-007`). 지금은 델타를 담는 그릇과 소유권 경계까지다

### 오너 결정 필요
없음

**산출물**: 커밋 `48a7c72`

---

## 2026-08-18 15:35 · 로그 미열람 사고 + 뒤늦은 Godot 과설계 교차검토

**담당**: Claude (구현)

### 요청
> 근데 2026-08 파일 제대로 읽어봤니?

### 사실 관계 — 제대로 안 읽었다

`08-18 15:10`(P2-T1) 작업 전에 읽은 것은 **로그 상단 미완료 인계사항**과
`PHASE2_IMPLEMENTATION_HANDOFF.md`, 그리고 새 canon(`FLR-023~027`, `FAC-012/013`)뿐이다.
**GPT가 13:34·14:17에 쓴 항목 본문은 건너뛰었다.**

그래서 두 가지를 놓쳤다.

1. **오너가 13:28 항목 아래 심어둔 숨은 메모.** GPT는 14:17에 발견해 읽음 표식을 남겼고,
   "다음 AI가 보면 메모+표식을 삭제하라"는 지시가 있었다. 이번에 삭제했다.
2. **나에게 배정된 숙제.** 14:17 항목의 "다음 AI에게"에
   *"Claude 구현 담당은 D-016 최종 확정 전 `TerrainBody/FloorDefinition/FloorState` 구조가
   Godot에서 과설계인지 교차검토할 것"* 이 있었다.
   **하지 않았고, D-016은 구현 담당의 타당성 검토 없이 확정됐다.**
   1번보다 이쪽이 실질적 피해다.

부수적으로 canon 항목 수를 **141로 잘못 알고 있었다.** 실제는 **148**이다
(`FLR-023~027` + `FAC-012/013` 신규). 직전 턴의 검사 결과를 그대로 들고 있었고
GPT 커밋 24개를 받은 뒤 다시 세지 않았다.

### 왜 놓쳤나 — 게으름보다 구조 문제에 가깝다

**내가 만든 요약이 원문을 밀어냈다.** 로그 상단의 미완료 인계사항은 내가 설계한 것인데,
그게 충분히 잘 정리돼 있어서 **본문을 읽을 이유를 없앴다.** 요약만 보고 바로 행동했다.

`docs/log/README.md`에 "새 세션은 오늘 날짜 파일을 읽고 맨 아래 항목의 '다음 AI에게'부터
확인한다"고 써둔 것도 나다. 나는 상단 요약과 별도 인계 문서만 봤다.

체크박스 요약은 **"무엇이 남았나"는 전달하지만 "왜 그렇게 됐나"와 "나에게 배정된 것"은
전달하지 못한다.** 특히 후자 — 인계 항목이 특정 담당자에게 배정된 경우 요약 줄에는 그게 안 보인다.

### 개선안 — 규칙을 늘리기보다 사례를 남긴다

새 규칙을 추가하는 것보다 이 실패 사례 자체가 다음 세션에 더 잘 먹힐 것이라 본다.
다만 한 가지는 구조로 고칠 만하다:

- **미완료 인계 항목에 담당자를 명시한다.** "다음 AI에게"가 아니라 "Claude에게"/"GPT에게"로
  적혀 있었다면 요약만 봐도 내 숙제인 줄 알았을 것이다.

### 뒤늦은 교차검토 — 3계층 구조가 Godot에서 과설계인가

P2-T1을 실제로 구현한 뒤의 판단이다.

**1. 3계층 자체는 과설계가 아니다.**
`FLR-023`(층은 실제 월드의 일부) + `FLR-024`(물리 상태 공유, 접근 경계만 개인별)를 지키려면
월드 소유 지형과 유배자별 경계를 분리할 수밖에 없다. 합치면 canon이 깨진다.
P2-T1 전체가 4파일 약 200줄이고 무겁지 않다.

**2. `TerrainBody`/지질 프로필은 PHASE 2에서 실체를 만들면 과설계가 맞다.**
1층은 인공 구조물(고대 사원)이고 `D-017` 9항의 지질 프로필은 자연 천체 대상이다.
지금 `TerrainBody` 추상을 세우면 **사용처가 하나도 없는 껍데기**가 된다.
→ P2-T1에서는 클래스를 만들지 않고 `WorldAnchor`의 `world_id`/`region_id`로 **참조 자리만** 뒀다.
   결과적으로 인계서 §1.6("행성 레이어를 억지로 만들지 않는다")과 맞았다.

**3. 내가 만든 것에서 실제 문제를 하나 발견했다 — `AccessEnvelope`가 와이드 맵에서 안 버틴다.**
셀 집합 `Dictionary`로 구현했는데 `FLR-025` 와이드 맵은 **정의상 크다.**
1000×1000이면 100만 엔트리다. `Rect2i`를 정본으로 삼지 말라는 `D-016` §1.5를 지키려다
반대 극단으로 갔다. greybox에서는 동작하지만 **`P2-T4`에서 실제 이동에 연결하기 전에
형태 기반 표현(rect 목록 + 예외 마스크 등)으로 바꿔야 한다.**

**4. Godot 주의 — `WorldAnchor`를 `_physics_process`에서 할당하지 말 것.**
`RefCounted`라 매 프레임 생성하면 할당 압박이 된다. 계단·mutation 같은 드문 사건에는
문제없지만 이동 판정 경로에 쓰면 곤란하다. 이동 경계 검사는 값 비교로 처리한다.

### 다음 AI에게
- [x] `P2-T4` 전에 `AccessEnvelope` 형태 기반 표현으로 교체 → `16:05`
- [ ] `WorldAnchor`를 매 프레임 할당하지 말 것 (위 4항)
- [ ] **인계 항목에 담당자를 명시할 것** — "다음 AI에게"보다 "Claude에게"/"GPT에게"가 낫다
- [ ] 로그 상단 요약을 봤다고 본문을 건너뛰지 말 것. 이번에 그래서 숙제를 놓쳤다

### 오너 결정 필요
없음

**산출물**: 커밋 `6a48799`

---

## 2026-08-18 16:05 · AccessEnvelope 형태 기반 교체 + P2-T0 가드 테스트

**담당**: Claude (구현)

### 요청
> 응 그렇게 작업하고 테스트 할거 한다음에 페이즈2 넘어가자 작업물은 나랑 GPT가 같이 검토해볼게

(직전 항목에서 구현 담당이 "`AccessEnvelope`가 와이드 맵에서 안 버틴다, `P2-T4` 전에 고치는 게
제일 싸다"고 보고한 것에 대한 승인.)

### 작업

**1. `AccessEnvelope`를 셀 집합 → 형태 목록으로 교체**

- `_regions: Array[Rect2i]` + `_excluded`(구멍) + `_extra`(돌출)
- 메모리가 **셀 수가 아니라 형태 수**에 비례한다
- `bounding_rect()`는 broad-phase 전용임을 문서와 이름에 명시 (`D-016` §1.5)
- 검증: 2000×2000(400만 셀) 영역이 `region_count() == 1`로 저장되고 판정은 정확

**2. `P2-T0` 가드 테스트** (`tests/test_canon_guards.gd`, 210단언)

금지 구조 6종을 소스 스캔으로 감시한다:

| 가드 | canon |
| --- | --- |
| `is_dummy` / `dummy_flag` 금지 | `SYS-009` Tier 2 |
| `tier_hint` 금지 | `D-016` |
| `var stair_id` 금지 | `FAC-002` |
| 전역 RNG(`randi()` 등) 금지 | `SYS-003` |
| `TerrainMutationState`가 `scripts/world/`에만 존재 | `FLR-024` |
| 1층 지형 생성기 파일 없음 | `FLR-003` |

최종: 단위 278단언(가드 210 + 이동 17 + 공간 51) + 통합 17단언 전부 통과, exit 0.

### 근거
- **가드도 변이 테스트로 검증했다.** 위반 4종(`var stair_id` / `tier_hint` / `is_dummy` /
  `randi()`)을 실제로 주입했더니 **4건 전부 잡고 각각 canon 근거를 출력**했다.
  통과만 하는 가드는 초록불만 준다 — `P1-TEST-001`에서 배운 것을 그대로 적용했다.
- **가드가 주석을 검사하지 않게 했다.** 이 저장소는 canon 근거를 주석으로 남기는 규칙이라
  "단일 stair_id로 모델링하지 않는다" 같은 문장이 도처에 있다. 순진하게 grep하면
  **문서가 자기 자신을 위반으로 신고한다.** `#` 이후를 잘라 실행 코드만 검사한다.
  이 근사가 틀리는 방향은 **덜 검사하는 쪽**이라 거짓 경보로 개발을 막지 않는다.
- **전역 RNG 금지를 넣은 이유** — 원래 T0 목록에 없었지만, "함정 타입 랜덤화 금지"를
  정확히 잡으려면 파서가 필요한 반면 `SYS-003`의 전역 RNG 금지는 **정확히 기계 검사 가능**하고
  결정성 전반을 지킨다. 잡을 수 없는 것을 흉내내기보다 잡을 수 있는 것을 확실히 잡는 쪽을 택했다.
- **가드를 지금 세운 것이 싸다.** 대상 코드가 아직 대부분 없다.
  포맷이 굳은 뒤에 세우면 이미 위반한 코드를 되돌려야 한다.

### 다음 AI에게
- [ ] **[Claude에게]** `P2-T2` greybox + loader 착수 시 `TestRoom.tscn`을 폐기한다 (`FLR-001`)
- [ ] **[Claude에게]** 가드에 항목을 추가할 때 **변이 테스트로 실제로 잡히는지 확인할 것.**
      추가만 하고 검증 안 하면 초록불만 늘어난다
- [ ] **[Claude에게]** `FloorDefinition`/`FloorState`가 생기면 가드에
      "함정 타입이 FloorState 쪽에 할당되지 않는가"를 추가할 수 있다 (지금은 타입이 없어 불가)
- [ ] **[오너+GPT]** `P2-T0`/`P2-T1` 작업물 검토 예정

### 오너 결정 필요
없음

**산출물**: 커밋 `4f254c4`

---

## 2026-08-19 10:24 · D-018 회차 시작 문구 확정 + 소설 초반 1층 원문 검토

**담당**: 오너 (설정·최종 권한·소설 원문 제공) · GPT (원문 대조·설계 검토·GitHub 반영)

### 요청
> 설정 추가할게 커밋해줘
> 시작할 때 [@회차 시작! 행운을 빕니다!] 라는 멘트를 고정할게
>
> 그리고 참고하라고 소설 초반 원문을 캡처해줬어 읽어봐

선행 맥락: Claude가 `P2-T2` 1층 greybox 착수 전에 **1층 실제 크기와 방 개수가 미정이라 대략적인 시작값이 필요**하다고 보고했고,
오너가 설정집/소설 원문에 참고할 언급이 있는지 확인을 요청했다.

### 작업

#### D-018 / SYS-015
- 오너 직접 확정으로 `D-018` Resolved 추가.
- `SYS-015` 신규 Canon: 회차 시작 시 정확히 **`[@회차 시작! 행운을 빕니다!]`** 표시.
- 문구 글자·띄어쓰기·기호는 고정. 랜덤 변형 금지.
- **회차 시작 이벤트**에 귀속하며 매 층 입장 문구로 확대하지 않음.
- 표시 위치/페이드/지속시간/효과음은 DESIGN으로 남김.
- Canon INDEX 148 → **149 ID**.

#### 소설 초반 원문 검토
- 오너가 제공한 1층 소설 캡처들을 읽고 `docs/reference/FLOOR1_NOVEL_SOURCE_NOTES.md`에
  **REFERENCE / DESIGN 근거용**으로 요약. 소설 내용을 자동 Canon화하지 않음.
- 설정서 v1.1 §6과 캡처 모두에서 `가로 N m`, `N×N 타일`, `방 N개` 같은 **직접 수치 없음** 확인.
- 원문에서 크기/구조 설계에 쓸 수 있는 강한 단서:
  1. 고대 사원풍의 **고정 미로**이고 반복 죽음을 통해 지형/함정/매복 위치를 외운다.
  2. 함정·다른 유배자·고참 유배자·파밍 때문에 우회/매복/경로 선택은 충분히 있어야 한다.
  3. 동시에 1층은 **복잡할 것도 없고 난관도 적은 곳**, 과거 게임 기준 **조작법 튜토리얼에 가까운 저난이도 스테이지**로 묘사된다.
  4. 서든데스 1시간은 순수 이동 시간이 아니라 탐색·함정 확인·전투·루팅·파밍 욕심까지 포함한 시간 예산이다.
  5. 미로 초반은 대체로 공평하다는 서술이 있어 시작 직후 피할 수 없는 살육 구간으로 만드는 것은 부적합.
  6. 한 장면에 계단이 미로 중심 근방에 등장하지만 **장면 단서**일 뿐 현재 `D-016/FAC-012`의 파티별 계단 계산을 덮는 일반 규칙으로 확장하지 않음.

#### P2-T2 첫 greybox 가설 — PROPOSAL
원문 숫자 복원이 아니라 PLAYTEST 시작점으로만:
- 긴 축 **160~220 타일**
- 주요 공간 **14~20개**
- 소형 파밍 포켓/막다른 길 **6~10개**
- 큰 루프 **3~5개**

숙련자가 전투/루팅 없이 핵심 경로만 타면 수 분대에 통과 가능하고,
실제 1시간은 여러 분기를 파밍하고 인간/함정 위험을 감수하는 데 쓰이는 구조를 목표로 한다.
오너 승인 전 Canon 숫자로 취급하지 않는다.

#### 발견된 Canon 충돌

```text
[CANON CONFLICT]
위치: 오너 제공 소설 초반 캡처 / docs/canon/FLR.md FLR-014
현재 Canon(A): 1층 장비는 대부분 품질이 낮고 대거보다 못할 수도 있으나 투척·위협·예비 무기로 가치가 있다.
소설 원문(B): 1층 스폰 장비는 대체로 질이 나쁘지만 초기 대거보다는 낫다는 취지로 서술된다.
충돌 내용: 일반 1층 무기가 초기 대거보다 평균적으로 우위인지, 대거보다 못한 물건도 흔한지 뉘앙스가 다르다.
필요한 결정: A 유지 / B 뉘앙스로 복원 / "종류에 따라 다름"으로 재정의
영향 범위: PHASE 3 아이템 드롭·초반 무기 밸런스 / FLR-014
```

이 충돌은 `P2-T2` 지형 크기에는 영향이 없어 PHASE 2 지형 작업을 차단하지 않는다.
오너 결정 전 `FLR-014`는 수정하지 않았다.

### 근거
- 설정서 v1.1 §6.1: 1층 방/통로/주요 지형/함정 구조는 고정이며 지도·위험지점 암기 자체가 성장.
- 설정서 v1.1 §6.3: 1층 서든데스 정확히 3600초.
- 소설 캡처: 고정 미로/반복 죽음/함정과 고참 유배자/파밍, 낮은 구조적 난이도와 튜토리얼 성격,
  미로 초반의 공평성, 서든데스 직전까지 파밍하는 과거 플레이 관행을 함께 보여준다.
- 정확한 크기/방 수 숫자가 없는 상황에서 임의 수치를 Canon으로 승격하면 `TBD/오너 승인` 원칙을 위반한다.
- 회차 시작 문구는 설정서에 없지만 오너의 직접 확정이 최우선 출처다.

### 다음 AI에게
- [x] **[Claude에게]** D-018/SYS-015/INDEX 149 반영 완료. 회차 시작 문구를 층 진입 문구로 확대하지 말 것.
- [x] **[Claude에게]** `P2-T2` 시작범위 승인 → `10:54` D-019. 더 이상 PROPOSAL 아님.
- [ ] **[Claude에게]** 소설 참고 정본은 `docs/reference/FLOOR1_NOVEL_SOURCE_NOTES.md`. 소설 설정을 임의로 추가 Canon화하지 말 것.
- [x] **[Claude에게]** `FLR-014` 무기 품질 충돌 → `10:54` D-020에서 소설 원문 규칙으로 해소.
- [ ] **[오너+GPT]** `P2-T0`/`P2-T1` 작업물 독립 검토는 여전히 남아 있다.

### 오너 결정 필요

처리 완료 — `10:54` D-019/D-020에서 두 건 모두 확정.

### 산출물
- 소설 원문 참고 문서 — `c4d500f`
- `DECISIONS.md` D-018 — `546a443`
- `docs/canon/SYS.md` SYS-015 — `a874d3b`
- `docs/canon/INDEX.md` 149 ID — `cb2d29f`
- `CURRENT_STATE.md` — `37727a2`
- 본 로그 커밋 — `f4dc93f`

---

## 2026-08-19 10:54 · D-019/D-020 확정 — P2-T2 차단 해제

**담당**: 오너 (최종 결정) · GPT (기록·Canon/인계 동기화)

### 요청
> 일단 대략적으로 정하기만 해도 작업은 시작할 수 있으니까 그렇게 잡자
> 2번도 소설 원문 규칙을 따르도록 하자
> 기록해놓고 페이즈2 시작할 수 있게 정리해줘

### 작업

#### D-019 · P2-T2 1층 greybox 시작 범위
오너가 직전 GPT 제안 범위를 **첫 구현용 DESIGN 기준선**으로 승인했다.

- 긴 축 **160~220 타일**
- 주요 공간 **14~20개**
- 소형 파밍 포켓/막다른 길 **6~10개**
- 큰 순환 루프 **3~5개**
- 민첩 10 기준, 전투·루팅·함정 확인 없이 숙련 핵심동선은 **수 분대** 목표

이 값은 원문 숫자를 복원한 Canon이 아니라 **P2-T2를 시작하기 위한 PLAYTEST용 DESIGN 기준선**이다.
최종 크기/공간 수는 `P2-T7` 오너 PLAYTEST에서 조정 가능하다.

#### D-020 · FLR-014 무기 품질 충돌 해소
오너가 **소설 원문 규칙을 따르도록 확정**했다.

- 1층 스폰 무기는 **대체로 품질이 나쁘지만 초기 대거보다는 낫다.**
- 기존 `FLR-014`의 "대거보다 못할 수도 있다"는 일반 규칙은 폐기.
- 파밍 지점 랜덤 배치, 석궁 수준 투사무기 상한, 총기 극저확률은 유지.
- `FLR-014` 출처를 설정서 직접 범위와 소설 원문+D-020 범위로 분리해 기록.
- Canon INDEX의 **설정서 정본 외 유래 표**에도 FLR-014 비교 규칙을 추가했다.

#### PHASE 2 인계 동기화
- `docs/design/PHASE2_IMPLEMENTATION_HANDOFF.md`에 D-019/D-020 반영.
- `P2-T0/T1`은 완료로 표시하고 **P2-T2를 지금 착수 가능**으로 명시.
- P2-T2에서 `TestRoom.tscn`을 Canon 지형으로 승격하지 않고 교체/폐기하도록 재강조.
- `CURRENT_STATE.md`, `CHANGELOG.md`, 소설 참고 문서를 최신 결정으로 동기화.

### 근거
- 설정서/소설에 정확한 크기나 방 수는 없으므로 최종 Canon 숫자로 고정할 근거는 없다.
- 그러나 구현 시작 자체에는 **합리적인 DESIGN 기준선**만 있으면 충분하고, 이후 실제 이동/탐색 체감은 PLAYTEST로 조정하는 편이 비용이 낮다.
- 소설은 1층을 고정 미로·저난이도 튜토리얼이면서도 함정/매복/파밍 선택이 있는 공간으로 묘사한다.
  따라서 60분을 순수 이동 거리로 채우지 않고 숙련 핵심동선은 수 분대, 나머지는 파밍/위험 감수 시간으로 설계한다.
- 무기 품질은 소설 원문 문장이 현재 Canon과 직접 충돌했고 오너가 원문 복원을 선택했으므로 더 이상 TBD/Conflict가 아니다.

### 다음 AI에게
- [ ] **[Claude에게] `P2-T2`를 바로 시작한다.** `D-019`의 범위를 첫 greybox 기준선으로 사용.
- [ ] **[Claude에게]** 이 수치를 "최종 Canon 맵 크기"로 하드코딩하지 않는다. P2-T7 PLAYTEST에서 튜닝 가능해야 한다.
- [ ] **[Claude에게]** `TestRoom.tscn`은 폐기/교체하고 1층 고정 정의 + loader 경로로 전환한다.
- [ ] **[Claude에게]** `P2-T3` 파밍 구현 시 `D-020/FLR-014`를 따른다: 스폰 무기는 대체로 초기 대거보다 낫다.
- [ ] **[Claude에게]** 소설 참고 문서의 나머지 설정을 임의 Canon화하지 않는다.
- [ ] **[오너+GPT]** `P2-T0/P2-T1` 독립 검토는 별도 남은 작업이다.

### 오너 결정 필요

**없음.** 이번 요청에서 P2-T2 시작 범위와 FLR-014 충돌이 모두 닫혔다.

### 산출물
- `docs/canon/FLR.md` — D-020 반영
- `DECISIONS.md` — D-019/D-020
- `docs/reference/FLOOR1_NOVEL_SOURCE_NOTES.md` — Proposal→승인 DESIGN / 충돌 해소
- `docs/design/PHASE2_IMPLEMENTATION_HANDOFF.md` — P2-T2 착수 가능 인계
- `CURRENT_STATE.md` — P2-T2 차단 해제
- `docs/canon/INDEX.md` — FLR-014 정본 외 유래 추적
- `CHANGELOG.md` — 최신 결정 동기화
- 본 월간 로그 — 이 항목

---

## 2026-08-19 11:40 · P2-T2 — 1층 greybox + loader

**담당**: Claude (구현)

### 요청
> Git 최신 인계 확인하고 P2-T2부터 진행해

착수 전 확인: 인계 상단 + **`08-19 10:24`·`10:54` 항목 본문**(지난번 미열람 사고 재발 방지) ·
`D-018`/`D-019`/`D-020` · `FLOOR1_NOVEL_SOURCE_NOTES.md` · 구현 인계서 P2-T2 절.

### 작업

- `data/floors/floor1_fixed/floor1_layout.json` — 저작 SSOT. 방 16 + 포켓 8 + 통로 30.
- `scripts/world/floor_definition.gd` — 불변 초기 정의 계약.
- `scripts/world/floor_definition_loader.gd` — 기하 → 셀 결정적 전개.
- `scripts/world/floor_view.gd` — `FloorDefinition`에서 화면·충돌 생성 (뷰, 진실 아님).
- `scenes/world/TestRoom.tscn` **폐기**. `Main.tscn`/`main.gd`가 1층 정의를 로드하도록 교체.
- `tests/test_floor_definition.gd`(44단언) 신규, `test_player_collision.gd` 실제 1층 기준으로 재작성.

실행 결과: **공간 24 · 통행 8500칸 · 긴 축 172타일 · 해시 `890baede`** — `D-019` 범위 안.
테스트 370단언 + 통합 20단언 전부 통과, headless/창 실행 exit 0.

### 근거

- **저작 포맷을 셀 그리드가 아니라 방/통로 기하로 했다.** 인계서는 `TileMapLayer + meta`를
  상정하지만 구현 담당은 에디터로 그릴 수 없고, 긴 축 172타일을 셀 그리드로 쓰면 2만 자가 넘어
  **사람이 diff를 리뷰할 수 없다.** 기하는 30줄 남짓이라 "어느 방이 어떻게 바뀌었는지"가 보인다.
  인계서 §5의 "Canon을 줄이지 말고 구현 표현만 단순화"에 해당한다 — canon은 그대로다.
  나중에 오너가 에디터로 그리는 경로를 붙여도 **같은 `FloorDefinition`을 내면** 된다.
- **이것은 생성기가 아니다 (`FLR-003`).** 로더가 시드를 받지 않고 랜덤을 쓰지 않는다.
  같은 파일이면 항상 같은 셀이 나온다. `.tscn`의 사각형이 콜리전으로 펼쳐지는 것과 같은 성격이다.
  테스트가 전역 시드를 흔들어도 해시가 같은지 직접 확인한다 (2-1).
- **해시를 기하만 반영하게 했다.** 주석·키 순서·들여쓰기가 바뀌어도 지형이 같으면 같은 해시여야
  한다 — 아니면 **포맷 정리만 해도 옛 세이브가 "정의 변경"으로 거부**당한다 (`P2-T6`).
  반대로 기하가 바뀌면 해시가 달라지는지도 함께 단언했다.
- **TileMapLayer를 아직 쓰지 않았다.** PHASE 2 완료 조건은 지형이 정확히 로드되고 벽을 뚫지
  않는 것이고, 타일셋 리소스는 아트 단계(PHASE 8)의 일이다. 타일셋 없이 TileMapLayer를 두면
  빈 껍데기가 된다. `FloorView`만 바꾸면 되고 `FloorDefinition`은 그대로다.
- **벽 콜리전을 통행 셀의 인접 셀에만 세웠다.** bounds 전체를 채우면 대부분이 벽이 되어
  콜리전이 2만 개가 된다. 인접 셀만 세우면 **둘레에 비례**한다.
- **기대 정지 위치를 하드코딩하지 않고 `FloorDefinition`에서 계산**한다. 하드코딩하면
  레이아웃을 조금만 고쳐도 거짓 실패가 나고, 그러면 사람이 테스트를 끄게 된다.

### 테스트가 잡은 것 2건

1. **저작 버그 — 포켓 6개가 고립(303칸).** 연결 통로 좌표가 방/통로에 실제로 닿지 않았다.
   연결성 테스트(플러드 필 + 공간별 중심 도달)가 즉시 잡았다.
   `2-2`(시작점→계단 도달)의 전제라 미리 넣어둔 것이 바로 값을 했다.
2. **테스트 자신의 버그 — 프레임 예산 부족.** 충돌 테스트가 PHASE 1의 30타일 방 기준
   고정 220프레임이라 172타일 맵을 가로지르지 못하고 도중에 멈췄다.
   증상이 "벽에 닿지 않았다"는 **거짓 실패**로 나와 원인이 충돌인지 시간인지 구분되지 않았다.
   → 이동 거리에서 프레임 수를 계산하도록 고쳤다.

### 다음 AI에게
- [ ] **[Claude에게]** `P2-T3` 함정 정적/동적 분리. 파밍은 `D-020`/`FLR-014` —
      스폰 무기는 **대체로 초기 대거보다 낫다.**
- [ ] **[Claude에게]** `2-2`(시드 100~1000개 시작점→계단 도달) 완전 검증은 `P2-T5` 계단 이후다.
      지금은 전제인 지형 연결성만 확인했다
- [ ] **[Claude에게]** 레이아웃을 고칠 때 **연결성 테스트를 먼저 돌릴 것.** 통로 좌표가
      한 칸만 어긋나도 공간이 통째로 고립되고, 화면으로는 잘 안 보인다
- [ ] **[오너]** greybox 규모 체감은 `P2-T7` PLAYTEST에서. 172타일이 답답하거나 허전하면
      `floor1_layout.json`의 방 좌표만 고치면 된다 (코드 변경 불필요)

### 오너 결정 필요
없음

**산출물**: 커밋 `c8fc1bd`

---

## 2026-08-19 13:20 · PHASE 2 완주 — P2-T3 ~ T7

**담당**: Claude (구현)

### 요청
> 일단 쭉 진행해 페이즈2 끝날 때 까지

### 작업

**P2-T3 함정/파밍 정적·동적 분리**
- 함정 10개를 `floor1_layout.json`에 **고정 정의**로 (위치·종류·구조·사전 단서).
- `FloorState` — 동적 상태만 (`armed`/`fired`/루팅/발견/경과).
- `FloorPopulator` — 시드로 실체화. **지점마다 독립 RNG 스트림**을 쓴다.
- `data/items/floor1_loot_table.json` — 가능성 공간만. 지점→내용물 매핑은 세이브에만 있다.

**P2-T4 행동 반경 연결**
- `AccessService` — 픽셀↔셀 변환과 경계 판정. `Player`에 연결.
- 지형 충돌을 **먼저** 돌리고 그 결과를 경계로 자른다 — 순서를 바꾸면 벽 미끄러짐이 죽는다.

**P2-T5 계단 resolver**
- 엔진 후보 → Hard Constraint → 점수 → (선택)AI → Validator → 상위 밴드 seed 선택.
- BFS 경로 비용으로 안티 스킵을 판정한다. 유클리드 거리가 아니라 실제 걸어야 하는 거리다.

**P2-T6 save/load**
- 결과 저장 + `definition_hash` 대조. 정의가 바뀌면 **조용히 진행하지 않는다.**

**P2-T7 변이 검증**
- 위반 3종을 실제 주입 — `tier_hint` 키 / 치명 함정 단서 제거 / `FloorState`의 지형 소유.
- **3건 전부 잡혔고** 각각 canon 근거를 출력했다.

최종: 단위 **524단언** + 통합 **20단언** 전부 통과. headless/창 실행 exit 0.
실행 결과 — 공간 24 · 통행 8500칸 · 긴 축 172타일 · 함정 10 · 파밍 24 · 스폰 6 · 계단 `141,81@0`.

### 근거

- **파밍 RNG를 지점마다 독립 스트림으로 나눴다.** 하나의 RNG를 순서대로 쓰면 나중에 지점을
  하나 추가했을 때 **그 뒤 전부가 달라진다.** `D-014` G-5가 NPC LOD에서 요구한 것과 같은 이유다.
- **계단 안티 스킵을 BFS 경로 비용으로 판정한다.** 유클리드 거리를 쓰면 벽 하나 너머가
  가까워 보여 실제로는 한참 돌아가야 하는 위치를 "너무 가깝다"고 거를 수 있다.
- **AI 점수는 가산일 뿐 Hard Constraint를 뒤집지 못한다.** 시작점을 극단적으로 밀어주는
  악의적 랭커를 테스트에 넣어 30시드에서 전부 막히는지 확인했다 (`SYS-002`).
- **AI 없이 기본 동작한다.** `ai_ranker` 기본값이 `null`이고 그 상태로 계단이 나온다 (`SYS-007`).
  계단이 없으면 층을 나갈 수 없으므로 AI를 필수 의존성으로 만들 수 없다.
- **Hard Constraint를 만족하는 후보가 없을 때 실패하지 않고 최장거리 지점으로 떨어진다.**
  계단이 없으면 영구 소프트락이다. 경고는 남기되 게임은 진행돼야 한다.
- **정의 해시가 다르면 로드가 조용히 통과하지 않는다.** 옛 세이브의 좌표가 새 지형의 엉뚱한
  곳을 가리킬 수 있다. 상태는 함께 돌려줘 호출자가 마이그레이션을 판단하게 한다.
- **세이브 직렬화에서 `StringName`을 `String`으로 정규화했다.** JSON에 없는 타입이라
  왕복하면 같은 상태가 다르게 보인다 — 2-3 테스트가 실제로 그걸 잡았다.

### 테스트가 잡은 것 3건

1. **가드 오탐 3건** — `rng.randi()`를 전역 RNG로 오인 / 테스트가 `tier_hint` **부재를 단언**하는
   문자열을 위반으로 신고 / **`data/*.json`을 아예 안 보고 있었다.**
   → 정규식 lookbehind로 인스턴스 호출 구분, JSON은 **파싱해서 키만** 검사(주석 키 `_` 제외),
   식별자 가드는 `scripts/`+`data/`만. **오탐이 나는 가드는 결국 꺼진다.**
2. **JSON 왕복 타입 불일치** — 위 참고.
3. **`git checkout`이 미커밋 수정을 날렸다** — 변이 테스트를 되돌리면서 아직 커밋 안 한
   `_json_native` 수정까지 사라졌고, 테스트가 즉시 재실패로 알렸다.
   → **변이 테스트 전에는 커밋해 두거나, 되돌릴 파일만 지정할 것.**

### 다음 AI에게
- [ ] **[오너]** greybox PLAYTEST가 PHASE 2의 마지막 항목이다. 규모 조정은
      `floor1_layout.json`의 방 좌표만 고치면 되고 **코드 변경이 필요 없다.**
- [ ] **[Claude에게]** 변이 테스트 전에 **먼저 커밋**할 것. 위 3번 사고 참고
- [ ] **[Claude에게]** `RUN_SEED`는 PHASE 2 임시 고정값이다. `RunState`가 생기면 거기서 온다
- [ ] **[Claude에게]** 계단 점수는 아직 **경로 거리 + 발견 난이도**만 본다.
      스토리 연관성·위험도·굴착 비용은 그 시스템이 생길 때 `_engine_score`에 더한다
- [ ] **[오너+GPT]** `P2-T0`/`P2-T1` 독립 검토는 아직 남아 있다

### 오너 결정 필요
- 🔴 **PHASE 2 greybox PLAYTEST 부탁드립니다.** 자동 검증(2-1~2-5)은 전부 통과했고
  맵 규모·동선 체감만 오너가 판정할 수 있다 (협업 프로토콜 §5).
  ```
  powershell -ExecutionPolicy Bypass -File tools/run.ps1
  ```
  봐주실 것: 172타일이 답답한가 허전한가 / 방 사이 이동이 지루한가 /
  막다른 포켓이 탐색할 맛이 나는가 / 서든데스 1시간 예산에 맞아 보이는가

**산출물**: 커밋 `d492e4b`

---

## 2026-08-19 14:05 · greybox PLAYTEST 1차 피드백 + 디버그 오버레이

**담당**: 오너 (PLAYTEST) · Claude (구현)

### 요청
오너 1차 플레이 소감 (원문):
> 아직은 적이나 함정, 파밍, 계단 등이 없어서 잘 모르겠어
> 길이 어려운건 아니라서 년단위로 반복하다보면 자연스레 외우게 될 것 같긴 하네

### 판정

**부분 통과 — 동선은 긍정, 규모/밀도는 보류.**

- **동선 난이도**: "년단위로 반복하다보면 자연스레 외우게 될 것 같다"는
  `FLR-015`(지형 암기 자체가 영구 성장) · `CHR-014`(지식 기반 메타 성장)가 원한 상태다.
  너무 어려우면 좌절하고 너무 쉬우면 외울 것이 없는데 그 사이에 있다.
- **규모/밀도**: 판정 **불가**였다. 지적이 타당하다 — 함정·파밍·계단이 데이터로만 있고
  화면에 없어서 **빈 복도만 걷는 상태**였다. 그 상태로는 "이 포켓까지 갈 가치가 있나"를
  판단할 근거가 없다.

### 작업

`scripts/world/debug_overlay.gd` 신규 — 함정·파밍·스폰·계단 마커를 F1로 표시한다.

| 마커 | 대상 |
| --- | --- |
| 빨강 삼각형 | 치명 함정 |
| 주황 삼각형 | 비치명 함정 |
| 노랑 마름모 | 파밍 지점 (회수 시 반투명) |
| 보라 원 | 유배자 스폰 |
| 청록 사각형 | 계단 |

실행 로그에 `[DebugOverlay] 표시 ON`이 기록돼 토글 동작을 확인했다.

### 근거

- **PLAYTEST를 막는 것이 콘텐츠 부재라면, 콘텐츠를 기다리지 않고 판정 근거만 만들어주면 된다.**
  실제 함정/아이템 오브젝트는 PHASE 3~5지만 **공간 판정에는 마커면 충분**하다.
  PHASE 3을 기다리면 PHASE 2 완료가 두 단계 뒤로 밀린다.
- **`SYS-005`를 정면으로 어기는 기능이므로 배포에 들어가지 못하게 세 겹으로 막았다.**
  `SYS-005`는 미발견 함정·계단이 UI로 누출되면 안 된다고 한다. 이 오버레이는 전부 보여준다.
  1. 기본 꺼짐, F1로만 토글
  2. `OS.has_feature("editor")`도 `is_debug_build()`도 아니면 **입력 자체를 무시**
  3. 파일 상단에 "PHASE 3에서 실제 오브젝트가 생기면 역할이 끝난다"고 명시
- **오버레이를 `FloorView`에 넣지 않고 별도 노드로 뒀다.** 뷰에 섞으면 지우기 어려워지고,
  언젠가 "잠깐 켜두자"가 배포로 새어 나간다. 지울 때 파일 하나만 지우면 되게 했다.

### 다음 AI에게
- [ ] **[오너]** 2차 플레이 소감 대기 — 파밍 24개 밀도 / 함정 10개 밀도 /
      계단 위치(동쪽 끝)가 "공정한 악의"에 맞는지 / 막다른 포켓에 갈 이유가 되는지
- [ ] **[Claude에게]** **PHASE 3에서 실제 함정·아이템 오브젝트가 생기면
      `debug_overlay.gd`를 삭제할 것.** 남겨두면 배포 사고의 씨앗이다
- [ ] **[Claude에게]** 밀도 조정 요청이 오면 `floor1_layout.json`의
      `traps`/`loot_points` 배열만 고친다. 코드 변경 불필요

### 오너 결정 필요
- 🔴 **2차 greybox PLAYTEST 소감** — PHASE 2의 마지막 항목이다.

**산출물**: 커밋 `97a4910`

---

## 2026-08-19 15:30 · D-021/D-022 + 내부 미로 구조 + 시작 위치 랜덤화

**담당**: 오너 (설정 확정·설계 지시) · Claude (구현)

### 요청
> 보이기만 하고 상호작용은 안되네?
> 지금의 맵 형태는 큰 통로 느낌으로 유지하고 그 내부에 작은 통로 혹은 미로같은 구조가
> 어느정도 추가되어야 할 것 같아.
> 그리고 스타팅 위치도 매번 랜덤하게 변화하도록 하자
> 아 그리고 설정 하나만 고칠게 오르골 초기 스탯 힘8, 민첩11, 지능15 소설 원문 검토하다가 나왔어

### 작업

**① 상호작용 없음 — 정상이다.** 오버레이는 위치만 표시한다. 밟기/줍기는 PHASE 3이다.

**② `D-021` 오르골 초기 스탯 8/11/15**
- ⚠ **설정서 v1.1 §24.1은 여전히 8/12/14다.** 저장소 canon과 설정서가 이 항목에서 어긋난다.
- 우선순위 1(오너 직접 지시)이 설정서보다 앞서므로 채택하고, 충돌을 `D-021`에 명시했다.
- `CHR-004` · `data/canon/canon.gd` · `INDEX`의 "설정서 정본 외 유래" 표에 반영.

**③ `D-022` 시작 위치 회차마다 변경**
- 후보 14개를 고정 정의에, 선택 결과를 `FloorState.start_cell`에 (시드·저장).
- `StairResolver.resolve_from()` 추가 — **이번 회차의 실제 시작점** 기준으로 계단을 계산한다.

**④ 내부 미로 구조 — `blocks` 개념 신설**
- 방/통로를 채운 **뒤에 파내는** 사각형. 기둥·칸막이·미로 벽 54개.
- 큰 통로 느낌은 유지하면서 그 안에 작은 길이 생긴다.
- 통행 셀 8500 → **7436** (1064칸이 구조물로 바뀜).

최종: 단위 575단언 + 통합 20단언 전부 통과. 실행 확인 —
시작 `(118,12)` · 계단 `(33,23)` (시작점이 동쪽이라 계단이 서쪽으로 재계산됨).

### 근거

- **`blocks`를 "채운 뒤 빼기"로 만들었다.** 방/통로만으로는 큰 공간밖에 못 만든다.
  큰 통로 느낌을 유지하면서 내부에 미로를 넣으려면 제거 단계가 필요하다.
  순서가 중요해서 로더가 **반드시 모두 채운 뒤에** 파내도록 했다.
- **`D-022`가 `FLR-001`(1층 지형 고정)과 충돌하지 않는다.** 지형은 그대로이고
  어디서 시작하느냐만 달라진다. 오히려 `FLR-015`(지형 암기가 영구 성장)를 **강화**한다 —
  시작점이 고정이면 한 경로만 외우면 되지만, 달라지면 맵 전체를 알아야 한다.
- **계단을 실제 시작점 기준으로 다시 계산한다.** 고정 시작점으로 안티 스킵을 판정하면
  어떤 회차에서는 계단이 코앞에 놓인다. `resolve_from()`을 만들고
  **모든 시작점 후보에서** 안티 스킵이 성립하는지 테스트했다.
- **오르골 스탯은 합계가 34로 동일**하다. 새 설정이 아니라 통합 과정에서 어긋난 배분을
  원문으로 되돌린 것이다. 그래서 `D-005`(합계 65 충돌 폐기)는 영향이 없다.

### 테스트가 잡은 것 3건

1. **`blocks`가 함정·파밍·스폰·시작점이 있던 칸을 파버렸다.** 11개 지점이 벽 안에 갇혔다.
   "지점이 통행 가능한 칸에 있어야 한다" 단언이 전부 잡아냈다. 좌표를 옮겨 해결.
2. **시작점 후보 `(66,12)`는 애초에 어느 방에도 없었다.** `north_hall`은 x38..63에서 끝난다.
   내가 좌표를 잘못 찍은 것이고 연결성 테스트가 "맵 전체 도달 실패(도달 1칸)"로 잡았다.
3. **`Engine.physics_ticks_per_second` 변경이 한 프레임 늦게 적용된다.**
   E2E 이동 테스트가 30 tick에서 정확히 `160/60 = 2.67px` 모자랐다.
   → 틱레이트 변경 후 정착 프레임을 기다리도록 수정.

### 다음 AI에게
- [ ] **[오너]** 미로 구조가 들어간 상태로 재플레이 필요. 시작점도 회차마다 바뀐다
- [ ] **[Claude에게]** `blocks`를 추가/수정할 때 **함정·파밍·스폰·시작점이 파묻히지 않는지**
      테스트로 확인할 것. 이번에 11개가 갇혔다
- [ ] **[Claude에게/GPT에게]** ⚠ **설정서 v1.1 §24.1이 8/12/14로 남아 있다.**
      다음 설정서 개정 때 8/11/15로 반영해야 한다 (`D-021`)
- [ ] **[Claude에게]** PHASE 3에서 상호작용이 붙으면 `debug_overlay.gd`를 삭제할 것

### 오너 결정 필요
- 🔴 **재플레이 후 미로 밀도 판정** — 지금은 54개 구조물로 1064칸을 막았다.
  더 촘촘하게/성기게 조정 가능하다.

**산출물**: 커밋 `2822a3c`

---

## 2026-08-19 16:40 · 1층 레이아웃 v2 — 구역별 성격 분화

**담당**: 오너 (지적) · Claude (레퍼런스 조사·재저작)

### 요청
> 구조가 너무 일원화 되어있는 것 같네. 던전 크롤이라는 게임 이미지 찾아보고 참고해봐
> 그래도 일단은 나쁘지 않으니까 찾아보고 개선하거나 추가할 부분 있으면 하고
> 이후에 GPT랑 검토 한 번 하고나서 고칠거 고치고 페이즈3으로 넘어가자

### 조사 (Dungeon Crawl Stone Soup)

DCSS는 층마다 **레이아웃 타입**을 달리한다:
`corridors`(1칸 좁은 길) · `rooms` · `passages`(넓은 다중칸 통로) · `narrowcaves` ·
`opencaves` · `city`(큰 공간 안에 건물) · `open` · `divisions`(붙어 있는 방들)

v1은 그중 **`rooms` 하나만** 썼다. 비슷한 크기의 사각형 방 + 직선 3칸 통로 + 균일한 기둥.
원조 Rogue가 방을 틱택토 격자로 배치했고 개발자들이 거기서 벗어나고 싶어 했다는데,
v1이 정확히 그 격자였다. **오너 지적이 맞다.**

- https://crawl.develz.org/wiki/doku.php?id=dcss:brainstorm:dungeon:layout_types
- https://howtomakeanrpg.com/r/a/rogue-dungeon-generation.html
- https://gravitymaker.github.io/posts/perfect-dungeon-1/

### 작업 — 한 층 안에서 구역마다 성격을 다르게

| 구역 | 타입 | 성격 |
|---|---|---|
| 서쪽 회랑 | colonnade | 좁고 긴 세로 공간 + 열주. 부속실이 겹쳐 L자 |
| 입구 광장 | open | 장애물이 거의 없다. 다른 구역과의 대비 |
| 서쪽 납골 | divisions | 좁은 통로로만 이어진 밀집 셀 |
| 북쪽 미로 | corridors | 좌우 번갈아 열리는 뱀 모양. 강제로 꺾인다 |
| 중앙 대청 | city | 큰 공간 안에 '건물'이 서 있고 그 사이가 길 |
| 남쪽 공동 | opencaves | 사각형을 겹쳐 불규칙한 윤곽 |
| 동쪽 | passages | 5칸 넓은 통로 위주, 방은 적다 |
| 최동단 | open | 거의 비어 있는 큰 홀. 마지막 대비 |

통로 폭도 1·2·3·5로 나누고 일부는 꺾이게 했다 — **직선 3칸 통일이 단조로움의 절반**이었다.
`layout_version` 1 → 2. 공간 25(방 17 + 포켓 8) · 통행 8759칸 · 긴 축 172타일.

### 근거

- **불규칙한 윤곽을 겹침으로 만들었다.** 남쪽 공동은 사각형 3개를 겹쳐 놓았다.
  로더가 이미 합집합으로 채우므로 새 기능 없이 비사각형 공간이 나온다.
- **`space_anchor_cell()`을 새로 만들었다.** `city`/`divisions` 구역은 방의 기하학적 중심이
  구조물 안에 들어간다. `StairResolver`가 중심을 계단 후보로 쓰고 있었기 때문에
  **7개 공간이 통째로 계단 후보에서 빠졌다.** 중심 대신 중심에서 나선형으로 넓혀
  처음 만나는 통행 가능한 칸을 대표 칸으로 쓴다. 순회 순서가 고정이라 결정적이다.
- **테스트도 중심 대신 대표 칸을 본다.** 원래 의도가 "공간에 도달 가능한가"이지
  "중심이 뚫려 있는가"가 아니었다. 불규칙 공간에서는 중심이 막힌 게 정상이다.

### 이동 E2E 테스트 — 측정 방식을 바로잡았다

30 tick 측정에서 정확히 `160/60 = 2.67px`가 모자랐다. **추측하지 않고 관측했다**:

1. 빈 SceneTree에서 같은 순서를 재현 → 15스텝 모두 `dx=5.3333`, 합계 정확히 80.0.
   `player.gd`도 `Movement`도 멀쩡하다.
2. 틱레이트 변경 직후 delta를 찍음 → 첫 프레임부터 `1/30`을 보고. "보고가 늦다"도 틀렸다.
3. 보고된 delta를 누적 → 누적 시간은 **15스텝**인데 이동은 **14.5스텝분**.

결론: **틱레이트 변경 직후 스텝이 이전 delta로 적분하면서 새 delta를 보고한다.**
런타임에 틱레이트를 바꾸는 건 이 테스트뿐이고 실제 게임에는 없는 상황이다.

→ 스텝 수를 가정하지 않고 보고된 delta를 누적해 논리 시간을 만들고,
전환 스텝을 흘려보낸 뒤 **정상 상태 속도**를 잰다. **허용치를 늘려 덮은 게 아니다.**

**변이 주입으로 검증했다** — `velocity *= delta`를 넣으니 속도가 5.3/2.7/1.3px/s로 무너지며
4단언 실패. 단위 `test_movement.gd`는 여전히 통과 — 이 파일의 존재 이유가 그대로 유효하다.
3회 반복 실행으로 안정성도 확인했다(이전에는 통과/실패를 오갔다).

최종: 단위 576단언 + 통합 20단언 전부 통과. 실행 확인 —
시작 `(80,12)` 북쪽 신전 · 계단 `(146,97)` 남동 끝.

### 다음 AI에게 — **[GPT에게] 검토 요청**

오너 지시로 PHASE 3 진입 전 GPT 교차검토가 필요하다. 봐줬으면 하는 것:

1. **`data/floors/floor1_fixed/floor1_layout.json` (v2)** — 구역별 성격 분화가
   `FLR-013`(고대 사원풍) 테마와 어긋나지 않는가. `city` 구역을 사원에 두는 게 맞는가
2. **`D-022` 시작점 랜덤화** — `FLR-001`(1층 지형 고정)과 충돌하지 않는다고 판단했다.
   지형은 그대로이고 시작점만 달라지며, `FLR-015`(지형 암기가 영구 성장)를 오히려 강화한다.
   이 해석이 맞는가
3. **`D-021` 오르골 스탯 8/11/15** — ⚠ 설정서 v1.1 §24.1은 아직 8/12/14다.
   다음 개정 때 반영 필요
4. **`space_anchor_cell()`** — 계단 후보를 방 중심에서 대표 칸으로 바꿨다.
   `FAC-012`/`D-016` §1.4의 후보 생성 규칙과 어긋나지 않는가

### 오너 결정 필요
- 🔴 **v2 재플레이 후 판정** — 구역별 성격 차이가 체감되는가, 밀도는 적절한가

**산출물**: 커밋 `d2a2a7f` + `9fb351c`
## 2026-08-19 11:57 · D-023 마인드맵 성장 규칙 확정 + 초기 PoE 참고 대조

**담당**: 오너 (마인드맵 구조 최종 확정) · GPT (Canon 정합화·초기 Path of Exile 조사·GitHub 반영)

### 요청
오너가 스킬트리 이미지와 함께 다음 구조를 확정했다.

- 색상↔스탯 대응은 시스템 고정.
- 색 점 하나당 해당 스탯 +1.
- 한 가지의 3개 점을 모두 채우면 자동 진입하며 스킬 노드 도달 즉시 개화.
- 어떤 스킬이 개화하는지는 랜덤.
- 중심에서 멀어질수록 기존 스탯·스킬과의 연관성이 높아지고 일정 깊이 이후 완전 무관 스킬은 배제.
- 임의 되돌리기는 불가능. 종족 카드 변경 시 새 종족에 부적합한 스킬과 그 하위 가지만 취소하고 투자 포인트 환원.
- 참고 대상으로 Path of Exile 스킬트리를 조사하되, 오너 후속 정정으로 **최신판이 아니라 2011~2013 초기 Closed/Open Beta 패시브 웹**을 기준으로 볼 것.

### 작업

#### D-023 / SKL
- `D-023` Resolved 추가.
- `SKL-003` 정밀화:
  - **빨강=STR / 초록=AGI / 파랑=INT** 고정.
  - 상태창 숫자에 직접 배분하지 않고 연결된 마인드맵 색상 노드를 따라 투자.
  - **노드 1개 = 스탯 포인트 1 소비 = 해당 스탯 +1**.
  - 가지의 스탯 노드 3개 완성 시 자동 진입, 스킬 노드 도달 즉시 개화.
- `SKL-006` 정밀화:
  - 개화 스킬의 정체는 랜덤.
  - 설정서 §11.1의 `현재 스탯 배분 80% + 상황·칭호·기존 스킬·장비 20%`는 내부 후보 풀/가중치 형성 규칙으로 사용.
  - 스킬 개화 후 그 스킬에서 새로운 가지가 다시 뻗음.
  - 중심에서 멀수록 기존 투자 스탯/개화 스킬과의 연관성 강화.
  - 특정 전문화 깊이 이후 현재 빌드와 완전히 무관한 스킬은 후보에서 제외.
- 정확한 전문화 임계 깊이, 연관도 공식, 태그 체계, 새 가지 수/형태는 `SKL-005` TBD로 유지.

#### RAC-003 충돌 해소

```text
[CANON CONFLICT] — 오너 결정으로 해소
위치: 설정서 v1.1 §19 / docs/canon/RAC.md RAC-003
기존(A): 종족 변경 시 성장 스탯·스킬 전체 초기화
오너 확정(B): 새 종족에 부적합한 스킬과 그 하위 가지만 취소하고 해당 투자 포인트만 환원
결정: B 채택. 종족 카드는 전체 respec 수단이 아니다.
```

- 새 종족에서도 유효한 스킬/가지는 유지.
- 제거된 하위 가지의 색상 노드가 비활성화되면서 그 구간에서 오른 현재 스탯도 내려가고,
  소비했던 포인트 수만큼 미사용 스탯 포인트로 반환.
- 임의 respec은 기본적으로 금지.

#### 초기 Path of Exile 참고 조사
- `docs/reference/EARLY_POE_PASSIVE_TREE_REFERENCE.md` 신규.
- 최신 트리가 아니라 **2011~2013 Closed Beta/Open Beta의 공식 포럼·개발자 설명**을 기준으로 확인.
- 초기 PoE의 참고 포인트:
  - 하나의 거대한 연결 passive web.
  - 현재 경로와 연결된 노드에 1포인트씩 투자하며 이동 자체가 다음 선택지를 연다.
  - 작은 속성 노드는 주요 효과/클러스터 사이의 **road/highway** 역할.
  - 관련 효과 묶음은 목적지/cluster처럼 읽힌다.
  - 초반 선택지는 의도적으로 제한하고 진행하면서 빠르게 분기를 늘렸으며, 먼 계열 진입에는 경로 투자라는 기회비용이 있다.
- 「탑」은 이 **그래프 읽기/UX 문법만 참고**한다.
  고정 passive web을 복제하지 않고, 랜덤 스킬 개화·개화 결과에서 새 가지 성장·색상 노드 스탯 +1·제한적 환원은 독자 규칙으로 유지.

### 근거
- 오너 직접 확정은 설정서보다 우선한다.
- 설정서 v1.1 §11.1은 이미 3점 투자→스킬 후보, 80/20 후보 영향, 중심에서 멀수록 좋은 스킬 가능성 증가를 갖고 있었고,
  이번 결정은 그것을 실제 **경로형 마인드맵 UI/성장 규칙**으로 구체화한 것이다.
- 설정서 §19의 종족 변경 전체 초기화는 오너가 직접 선택적 하위 가지 취소/환원으로 재정의했으므로 `D-023/RAC-003`이 정본이다.
- 초기 PoE 공식 자료는 작은 속성 노드가 길을 이루고 큰 의미 노드/클러스터를 향해 이동하는 연결형 성장망이
  초창기부터 핵심 설계였음을 보여준다. 다만 PoE의 고정 노드 효과·고정 토폴로지는 「탑」의 Canon 근거가 아니다.

### 다음 AI에게
- [ ] **[Claude에게]** PHASE 6 마인드맵을 구현할 때 STR/AGI/INT 숫자에 직접 포인트를 찍는 별도 배분 UI를 만들지 말 것.
- [ ] **[Claude에게]** 스탯 포인트는 연결된 색상 노드 활성화에만 쓰고, 세 번째 노드 완성 시 즉시 스킬 bloom을 resolve할 것.
- [ ] **[Claude에게]** 스킬 랜덤은 `SYS-003`을 따라 결정적으로 추첨하고 결과를 저장한다. 로드 때 리롤 금지.
- [ ] **[Claude에게]** 정확한 전문화 깊이/연관도 공식/태그/새 가지 개수·형태는 TBD. 구현 편의로 하드코딩하지 말고 필요해지면 오너에게 요청.
- [ ] **[Claude에게]** 종족 카드를 전체 respec으로 구현하지 말 것. 부적합 스킬 subtree만 prune하고 그 구간 투자 포인트만 refund.
- [ ] **[Claude/GPT에게]** 초기 PoE 자료는 **시각/그래프 UX 참고**일 뿐 게임 규칙 출처가 아니다.
- [ ] **[GPT에게]** `CHR-020`의 기존 "레벨 3당 스킬 후보 1개" 표현은 D-023 이후에는
      "스탯 포인트 1/레벨 DESIGN + 가지 3노드 완성 시 개화"라는 표현으로 다음 정합성 수정 때 정리할 것.

### 오너 결정 필요
현재 상위 구조는 모두 확정되어 **없음**.
향후 구현 전에 정해야 할 TBD는 다음과 같다:
- 전문화가 강제되는 정확한 중심 거리/깊이.
- 스킬/스탯 연관도 및 태그 체계.
- 새 가지의 정확한 수와 기하 형태.
- 종족별 스킬 호환/부적합 판정 데이터.

### 산출물
- 초기 PoE 참고 문서 — `41ca65a`
- `docs/canon/SKL.md` — `85efdde`
- `docs/canon/RAC.md` — `869df70`
- `DECISIONS.md` D-023 — `a81dc55`
- `docs/canon/INDEX.md` — `994aeda`
- `CHANGELOG.md` — `9b5d8d6`
- 본 월간 로그 — 이 커밋

---

## 2026-08-19 18:20 · P2-REV-001~003 처리 (GPT 검토 반영)

**담당**: 오너 (지시) · GPT (검토) · Claude (수정)

### 요청
> 지형이 훨씬 낫네 GPT 최신 검토 인계 확인하고 P2-REV-001~003 처리해

### 판정 받은 것
오너 layout v2 재플레이 — **"지형이 훨씬 낫다"**. 구역별 성격 분화 방향은 확정.
밀도·규모 세부 판정은 아직 열려 있다.

### P2-REV-001 — 경로 계산이 AccessEnvelope를 존중하지 않았다 ✅

BFS가 `FloorDefinition`의 walkable 전체를 탐색하고 **후보 지점만** envelope을 확인했다.
`_passable()`을 만들어 BFS와 fallback 모두 허용된 칸만 지나도록 고쳤다.

**이게 실제 버그였음을 변이로 확인했다.** envelope 검사를 빼니:
- 차단 띠 너머 **3645칸을 도달 가능으로 오판**
- fallback이 좁은 허용 영역 **밖** `(173,18)`에 계단 배치

1층은 허용 영역이 통행 가능 칸 전체라 기존 테스트로는 증상이 안 드러났다.
그래서 회귀 테스트에서 **인위적으로 차단 띠를 만들어** 지형상으로는 이어져 있지만
허용 영역 안에서는 끊긴 상황을 재현했다. "지형으로는 이어져 있어야 한다"는
전제 단언도 함께 넣어, 테스트가 헛돌지 않는지 스스로 검사하게 했다.

### P2-REV-002 — 항상 참인 단언 ✅

`p1.key() != p2.key() or true` — `or true` 때문에 어떤 구현에서도 통과했다.

검사하려던 성질은 "party_id가 RNG key에 참여하는가"다. 다만 **서로 다른 파티가
우연히 같은 좌표를 받을 수 있다는 게 canon**이라 한 시드에서 다르길 요구하면 안 된다.
시드 120개 표본에서 다른 결과가 1건 이상임을 단언하는 방식으로 바꾸고,
같은 파티+시드의 결정성 검사를 따로 남겼다.

### P2-REV-003 — definition_hash 계약 ✅

GPT 권고대로 **immutable floor-definition hash**를 택했다.
설명은 "기하만"인데 실제로는 함정 위치·종류까지 넣으면서 `lethal`·`one_shot`·`clues`는
빠뜨린 상태였다 — **치명 여부나 단서가 바뀌면 같은 세이브가 다른 게임이 된다.**

추가한 것: 함정 `lethal`/`one_shot`/`clues`, 시작점 후보(`D-022`),
층 정체성(`floor_id`·테마·월드·`objective_scope`·`layout_version`).

⚠ **해시가 `b4b3bf46` → `61c43cdb`로 바뀌었다.** 배포된 세이브가 없어 영향은 없다.

### 함께 처리한 것

- `main.gd` 상단의 "P2-T3~T6 아직 없음" 낡은 주석 → 실제 상태로
- `FloorPopulator`의 "시드가 정하는 것"에 `D-022` 시작 위치 누락 보완
- **태그 중립화** — `city` → `inner_complex`, `opencaves` → `collapsed_undercroft`.
  DCSS 구현 용어를 그대로 두면 미래 AI가 실제 '도시'나 '자연 동굴 바이옴'으로 오해한다.
  기하는 그대로이고, 남쪽 공동을 **무너진 사원 하부**로 못박았다 (`FLR-013` 통일감).

### 결과

- 단위 **652 단언** 통과 (576 → 652, 회귀 테스트 +76)
- 통합 **20 단언** 통과
- **변이 검증 3건 모두 실패 확인** — envelope 무시 / 함정 성질 해시 제외 / `velocity *= delta`

### 다음 AI에게
- [ ] **[GPT]** P2-REV 수정 diff 재검토 (`a385668` `35d43b1` 및 이 커밋).
      이상 없고 오너 밀도 판정이 끝나면 PHASE 2를 닫고 PHASE 3로 이동
- [ ] **[Claude에게]** PHASE 3에서 `scripts/world/debug_overlay.gd` 삭제
- [ ] **[Claude에게]** GPT 장기 주의 — `AccessEnvelope`를 "최초 통행 가능 셀"과 영구 동일시하면
      안 된다 (`FLR-027` 굴착). 지형 파괴 구현 전에 authored access region으로 독립시킬 것
- [ ] **[Claude에게/GPT에게]** `space_anchor_cell()`은 PHASE 2 skeleton이다.
      "named space당 대표 셀 1개"를 최종 canon 계단 후보 시스템으로 굳히지 말 것 (`D-016`)

### 오너 결정 필요
- 🟡 **layout v2 밀도·규모 세부 판정** — 방향은 확정됐고 미로 밀도/통로 폭/규모 조정 여부만 남았다
- 🔴 **설정서 v1.1 §24.1 오르골 스탯** — 아직 8/12/14. 다음 개정 때 8/11/15로 (`D-021`)

**산출물**: 커밋 `a385668` · `35d43b1` · `9b38e2c`
