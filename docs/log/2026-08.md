# 작업 로그 — 2026년 8월

> 형식과 규칙은 [README.md](README.md).
> 새 세션은 **오늘 날짜에 해당하는 파일**을 읽고, 맨 아래 항목의 "다음 AI에게"부터 확인한다.

**이 달 미완료 인계사항 (2026-08-18 10:14 기준)**

- [ ] **테스트 러너 결정** — GUT vs 자체 SceneTree 스크립트. PHASE 1 착수 전 필요.
      오너가 GPT에 의견 요청 중. (`docs/BACKLOG.md` B-004 · 항목 `08-17 14:07`)
- [ ] **GPT 3차 재검수** — 판단 근거 backfill 정확성, CANON 항목의 판단 누락,
      내용 중복 잔존 여부. (항목 `08-18 10:04`)
- [ ] **PHASE 1 착수 지시 대기** — 오너 지시 전 시작 금지.

---

## 2026-08-17 13:03 · STEP 0 — GitHub 최초 push

**담당**: Claude (구현)

### 요청
> 이 폴더는 아직 원격에 안 올라간 문서 스캐폴딩이다. git 상태를 확인하고, origin이 없으면
> `https://github.com/aa0607aa/Tower-Dev.git`로 연결한 뒤 main으로 push 해라.

작업 전 `COLLABORATION_PROTOCOL.md` → `docs/DEVELOPMENT_GUIDE.md` → `DECISIONS.md` →
`CURRENT_STATE.md` 순으로 읽으라는 지시 포함.

### 작업
- 문서 4종 정독 후 `git init -b main` → 첫 커밋 → origin 연결 → push
- `git ls-remote origin`으로 원격 ref 실제 존재 확인

### 근거
- push 성공을 stdout 메시지가 아니라 **원격 ref 조회로 검증**했다.
  PowerShell이 git stderr를 NativeCommandError로 감싸 성공을 실패처럼 보이게 하므로
  종료 코드나 출력만 믿으면 안 된다.

### 다음 AI에게
- [x] PHASE 0 착수 → 항목 `08-17 13:11`

### 오너 결정 필요
없음

**산출물**: 커밋 `26be95f`

---

## 2026-08-17 13:11 · PHASE 0 — Godot 프로젝트 뼈대

**담당**: Claude (구현)

### 요청
> 개발 가이드의 PHASE 0 정의와 폴더 구조를 따른다. Godot 4.7.1 stable + GDScript 프로젝트
> 뼈대 생성. 실행 가능한 최소 Boot/Main 씬을 만들고 실제로 실행해 오류 없는지 확인한다.
> PHASE 0 완료 조건 충족하면 멈춘다. 내 지시 전에는 PHASE 1로 넘어가지 마라.

### 작업
- `project.godot`(GL Compatibility · nearest 필터 · 1280×720), 개발 가이드 §6 폴더 구조 전체
- `Boot.tscn` → `Main.tscn`, `game_log.gd` autoload, `data/canon/canon.gd`
- `docs/CANON_NOTES.md` · `CODING_STYLE.md` · `TEST_CHECKLIST.md` · `assets/STYLE_GUIDE.md`
- **실행 검증 중 버그 1건 발견·수정**: `Boot._ready()`에서 `change_scene_to_file()` 직접 호출 시
  `Parent node is busy adding/removing children` 에러 → `await get_tree().process_frame` 후 전환

### 근거
- `data/canon/canon.gd`에 **[Resolved] 상수만** 넣었다. TBD 수치를 "일단 기본값"으로 채우면
  canon 위조가 되고, 다음 세션이 그것을 사실로 오해한다.
- 개발 가이드 §6은 `docs/DECISIONS.md`인데 협업 프로토콜 §6은 루트다.
  **협업 프로토콜이 우선**이라 루트를 유지하고 README에 명시했다.
- `.gitattributes`로 LF 정규화. Godot 에디터가 항상 LF로 재저장해 CRLF면 커밋마다 전체 diff가 난다.

### 다음 AI에게
- [x] ESC 종료 검증 — 오너가 직접 확인 (항목 `08-17 13:26`)

### 오너 결정 필요
없음

**산출물**: 커밋 `72c4ff4` `0b69769`

---

## 2026-08-17 13:26 · OpenGL 드라이버 설치와 종료 크래시 규명

**담당**: Claude (구현) · 오너 (UAC 승인 · ESC 검증)

### 요청
> OpenGL 3.3 드라이버도 설치하자

(선행 요청: "Godot 4.7.1 설치 상태 확인하고 없으면 안내해줘" → winget으로 설치 완료)

### 작업
- 진단: 실 하드웨어는 AMD Radeon R5 240(`PCI\VEN_1002&DEV_6611`)인데 드라이버 미설치로
  Microsoft 기본 디스플레이 어댑터로 동작 → OpenGL 3.3 불가
- 관리자 권한 승격(오너 UAC 승인) 후 Windows Update의 AMD 드라이버 설치 → OpenGL 3.3 확보
- **설치 후 새 문제**: 창 실행 시 종료 코드 `0xC0000409` 크래시
- 격리 테스트: headless exit 0 / 네이티브 GL 창 크래시 3/3 /
  **스크립트 없는 빈 프로젝트도 크래시 3/3** / Vulkan·D3D12 미지원 →
  **ANGLE 강제 시 exit 0, 3/3**

### 근거
- 빈 프로젝트에서 재현되므로 **우리 코드가 아니라 드라이버+엔진 문제**임을 확정했다.
  이 격리 없이는 PHASE 1에서 우리 코드를 의심하며 시간을 버렸을 것이다.
- **ANGLE을 `project.godot`에 넣지 않았다.** 프로젝트 설정으로 박으면 배포 시 모든 Windows
  플레이어가 ANGLE을 강제로 쓴다. 개발 PC 한 대의 낡은 드라이버로 배포 렌더링 경로를 정할 수 없다.
  → `docs/BACKLOG.md` B-003으로 분리
- 드라이버 설치는 관리자 권한이 필요해 **직접 하지 않고 오너 UAC 승인을 받았다.**

### 다음 AI에게
- [x] 개발 PC에서 창 실행 시 `--rendering-driver opengl3_angle` 사용 — README·TEST_CHECKLIST에 기록
- [ ] 배포 렌더링 경로 결정은 PHASE 11 (`B-003`)

### 오너 결정 필요
없음 (ESC 종료는 오너가 직접 눌러 검증 완료 → PHASE 0 완료 조건 전부 충족)

**산출물**: `docs/TEST_CHECKLIST.md` 환경 이슈 절 · 커밋 `f518da8`에 반영

---

## 2026-08-17 13:34 · canon 색인 체계 구축 (1차) + 데이터 마이닝 정책 검토

**담당**: Claude (구현)

### 요청
> 실제 게임 개발에 착수하기 전에 설정이 제대로 탄탄하게 잡히고 그렇게 잡힌 설정이 분류가
> 제대로 되서 필요할 때 딱딱 불러올 수 있어야 한다고 생각해.

> 이건 원본 게임에서 언급된 내용인데, 데이터 마이닝은 피할 수 없는 숙명이다, 라고 생각하고
> 데이터 마이닝 자체를 막는 것이 아니라 일반적인 방법으로는 다 확인할 수 없을 만큼 수 많은
> 데이터를 쌓고 더미 데이터를 섞어서 데이터 마이닝으로 원하는 내용을 찾지 못하게 만드는
> 방법을 택한다고 하거든? 우리도 그런 방식을 택하면 어떨까 싶은데.

### 작업
- `docs/canon/` 신설 — ID 체계(`FLR-004` 형태), 도메인 분류, 상태(CANON/DESIGN/TBD/PROPOSAL)
- 1차 등재 63항목 / 7도메인 (당시 설정서 원본이 저장소에 없어 DECISIONS + 개발 가이드에서만 유래)
- `docs/DATAMINING_POLICY.md` 작성 → `D-012` Proposed 등록

### 근거
- **JSON 색인을 두지 않았다.** 마크다운 본문과 JSON을 둘 다 유지하면 반드시 어긋나고,
  어긋나면 어느 쪽이 맞는지 아무도 모르게 되어 체계의 존재 이유가 사라진다.
- 데이터 마이닝 원안은 **방향은 옳지만 문자 그대로 구현하면 실패**한다고 판단했다.
  ① 마이너는 눈으로 안 보고 스크립트로 처리하므로 볼륨은 몇 분의 차이일 뿐이고
  ② **코드가 참조하지 않는 더미는 한 번의 필터로 사라진다.**
  ③ 결정적으로, 진짜에만 적용되는 검증 규칙(`FLR-011`의 `clues[]` 필수)이 곧 판별기가 된다.
  → 3 Tier로 분리 제안. **오너 제안이라도 그대로 승인하지 않고 검토 결과를 올렸다.**

### 다음 AI에게
- [x] 설정서 원본 입고 시 63항목 전수 대조 → 항목 `08-17 13:45`

### 오너 결정 필요
- **D-012 Q1/Q2/Q3** → 채팅으로 직접 질의함. 항목 `08-17 13:55`에서 확정

**산출물**: 커밋 `5a3f6e8` `c9aac08`

---

## 2026-08-17 13:45 · 설정서 원본 대조 — 63 → 133항목

**담당**: Claude (구현) · 오너 (원본 배치)

### 요청
오너가 `docs/SETTING_BIBLE_v1.1.docx`를 저장소에 배치.

### 작업
- docx 압축 해제 → `word/document.xml` 파싱해 전문(§0~§27) 확보
- **원문에만 있던 도메인 8개 신규 등재**: HIS · RAC · MAG · ITM · NPC · KGD · DGN · GOD
- 기존 도메인 보정 (FLR 11→21, CHR 12→17, SKL 5→9, CBT 9→13, FAC 7→11)
- `[CANON CONFLICT]` 0건 — 개발 가이드 v2.0과 설정서 v1.1은 정합

### 근거
- 1차 색인이 **설정서의 절반도 못 담고 있었다.** 종족 12종·신좌·마법/주술·왕국·리프트·
  메인던전·NPC 규칙이 통째로 빠져 있었다. DECISIONS와 개발 가이드만으로 canon을 재구성하려던
  1차 접근의 한계가 드러난 것.
- ID 재배정 1건(`FAC-006`→`KGD-007`, `FAC-007`→`FAC-011`). 원칙상 재사용 금지지만
  원본 입고 전 임시 색인 단계라 예외 처리하고 `FAC.md` 하단에 흔적을 남겼다.

### 다음 AI에게
- [x] 절 단위 커버리지 감사 → 항목 `08-17 13:55`

### 오너 결정 필요
없음

**산출물**: 커밋 `a793e6c`

---

## 2026-08-17 13:55 · D-012 확정 + 커버리지 감사 (누락 4건)

**담당**: Claude (구현) · 오너 (D-012 결정)

### 요청
> Q1. 허가 / Q2. 나중에 하자 / Q3. 1층은 괜찮아. 어차피 알더라도 큰 변화는 없어.
>
> 설정 정리는 제대로 한거야?

### 작업
- **D-012 Resolved** — Tier 1·2 → canon `SYS-009`, Q3 → canon `SYS-014`,
  Tier 3 → BACKLOG `B-001`
- `docs/BACKLOG.md` 신규 (개발 가이드 §11이 요구하는데 없었다)
- **"제대로 했나"에 답하기 위해 실제 감사 실행** — 설정서 절 84개를 뽑아 전수 매핑 확인
- **누락 4건 발견·보완** → 137항목:
  §0.2(→`SYS-013`) · §6.4 대거 특성(→`FLR-007`) · §10.1(→`CHR-018`) · §24.2(→`CHR-019`)

### 근거
- "제대로 했냐"는 질문에 **확인 없이 답하지 않았다.** 기계 감사를 돌렸고 4건이 나왔다.
- §6.4 누락이 가장 위험했다. 대거의 **짧은 리치**가 `CBT-007`(리치가 길수록 암습 보정 감소)과
  직결되므로, 빠진 채 PHASE 4에 갔으면 초기 무기 밸런스 근거를 잃었을 것이다.
- Q3(1층 은닉 불가)은 손실이 아니라고 판단했다. `FLR-015`가 지형 암기를 영구 성장으로,
  `CHR-014`가 지식 기반 메타 성장을 핵심 축으로 규정하므로 **1층 맵을 아는 것은 원래 보상**이다.

### 다음 AI에게
- [x] 마크다운 사본 필요성 검토 → 항목 `08-17 14:07`

### 오너 결정 필요
없음

**산출물**: 커밋 `f518da8`

---

## 2026-08-17 14:07 · 설정서 마크다운 사본 + 자기검증 변환기

**담당**: Claude (구현)

### 요청
> 지금 정리한 내용들 GPT가 확인할 수 있게 하려면 git에 연결해주고 확인하라고 하면 돼?
> → (걸림돌 설명 후) 응 변환본도 만들어.

### 작업
- `tools/docx_to_md.ps1` — Word 스타일 매핑(Heading/List/Table/Callout) 기계 변환
- `docs/SETTING_BIBLE_v1.1.md` 생성 (원문 666문단, 누락 0)
- 저장소 공개 여부 확인 → **public**이라 GPT 브라우징으로 접근 가능

### 근거
- **손으로 옮기지 않았다.** canon 문구를 사람이 옮기면 반드시 조금씩 바뀌고,
  그러면 원본과 사본이라는 두 개의 진실이 생긴다.
- **변환기에 자기 검증을 내장했다.** 원본 전 문단이 출력물에 있는지 확인하고 하나라도 빠지면
  오류로 중단한다. 조용히 한 문단이 빠지면 **그걸 읽는 AI가 "그런 규칙은 없다"고 판단**해버린다.
  없는 걸 지어내는 것보다 있는 걸 못 보는 쪽이 잡기 어렵다.
- 처음엔 사본을 거부했으나(두 개의 진실 우려), GPT가 docx 바이너리를 못 읽어
  **canon 원본 없이 검수하게 되는 문제**가 더 크다고 판단해 오너 승인을 받고 만들었다.
  `SETTING_BIBLE_v1.1_README.md`가 이미 "오너 승인 하에 가능"으로 예상해둔 경로다.

### 다음 AI에게
- [ ] **docx 개정 시 `tools/docx_to_md.ps1` 재실행 필수.** 안 하면 사본이 낡은 canon을 퍼뜨린다
- [ ] **테스트 러너 결정** (GUT vs 자체 스크립트) — PHASE 1 착수 전 필요. `B-004`

### 오너 결정 필요
- 테스트 러너 → 오너가 GPT에 의견 요청하기로 함

**산출물**: 커밋 `1ea4f7f`

---

## 2026-08-18 09:54 · GPT 교차검증 6건 반영 — D-013 · D-014

**담당**: GPT (설계 검수) · Claude (구현) · 오너 (결정)

### 요청
GPT가 6건 지적. 오너 판단:
> 1번에 레벨당 1포인트는 원문에 포함되어있으니 패스.
> 2번에 레벨, 경험치 초기화 맞아. 3번은 문화 충돌 설정은 추가. 4번도 인식 전 NPC 행동
> 보강하는게 좋아보여 (…) 5번도 적용하면 좋을 것 같고 6번도 적용하자

4번에 대한 오너의 설계 원안 (원문 유지 — 수치와 구조가 판단에 직접 영향):
> 플레이어의 행동턴이 1이라고 가정하면, 주변에 있거나 중요 NPC의 경우에는 1대1의 비율로
> AI가 조종하고 멀리 있거나 중요하지 않은 NPC의 행동은 5턴마다 한 번 간략하게만 진행하고
> 해당 내용이 플레이어와 관련이 될 경우 살을 붙이는 형식으로 하기로 했었어.
> 우린 지금 반실시간이긴한데, 적용할 수 있다면 이 방향이 좋다고 생각해

### 작업
- **1번을 "패스"하지 않고 원문 재검색으로 확인 → GPT가 옳았다.**
  `grep`으로 설정서 전문 확인: "레벨당 스탯 포인트 1"은 docx에 **없다.**
  원문은 §11.1 "3점 투자마다 스킬 후보", §9.1 "레벨업 등으로 증가"까지.
  그 비율은 `DEVELOPMENT_GUIDE.md` §3.1 문장이다. 경험치도 §8.2 초기화 목록에 없다.
- `D-013` 신설 — 규칙은 유지하되 **출처를 오너 결정으로 정정**. `CHR-020` 분리(DESIGN)
- `D-014` 신설 — NPC 시뮬레이션 LOD (`NPC-005`)
- `KGD-008`(직업 문화 충돌) · `FAC-004` 보강 · `KGD-001` 복원 · `RAC-001` 제목 + `RAC-004` 분리
- 137 → 141항목

### 근거
- **오너가 "패스"라고 한 항목을 그대로 넘기지 않았다.** 원문에 없는데 있다고 기록하면
  다음 대조에서 같은 지적이 또 나오고, 그때는 신뢰가 깨진다.
  규칙 자체는 오너 권한(우선순위 1)이므로 유지하되 **출처만 정정**하는 것이 정답이었다.
- LOD 원안을 반실시간으로 옮기며 **두 제약을 추가**했다. 원안을 그대로 옮기면 canon이 깨진다:
  1. **"AI가 조종한다"를 문자 그대로 옮기면 `SYS-007` 위반.** 텍스트 RPG는 AI가 NPC를 굴렸지만
     「탑」은 엔진이 굴리고 AI는 대화·비정형 행동만 한다. LOD는 **엔진 시뮬레이션 빈도**이고
     AI 호출 예산은 별개 축이다. 묶으면 API 끊길 때 NPC가 멈춘다.
  2. **저해상도 시뮬레이션도 결정적이어야 `SYS-003` 유지.** 플레이어 위치에 따라 NPC 결과가
     달라지면 "보지 않았다고 다시 생성되지 않는다"가 깨진다. 승격 시 소급 계산(catch-up) 필요.
     **LOD 구현에서 가장 흔히 깨지는 지점.**

### 다음 AI에게
- [x] 판단 근거 기록 체계 → 항목 `08-18 10:04`
- [ ] **`D-013` 유형의 오류가 더 있는지 GPT 재검수** — 출처가 잘못 달린 항목.
      커버리지는 기계로 잡히지만 **출처 정확성은 제3자 대조가 필요**하다는 게 이번에 드러났다

### 오너 결정 필요
없음 (D-013 · D-014 확정)

**산출물**: 커밋 `e1906d7`

---

## 2026-08-18 10:04 · 판단 근거 필드 신설 + 내용 중복 2건 정리

**담당**: Claude (구현)

### 요청
> 설정집에 내용 추가해서 커밋할 때 나한테 설명하는 것 처럼 해당 내용에 대한 근거도 함께 적고있지?

### 작업
- **감사 결과: 절반만 하고 있었다.** 출처 141/141, **판단 근거 20/141**
- `docs/canon/README.md` 항목 형식에 **판단** 필드 추가 + 사용 기준 규정
- DESIGN·혼합 상태 항목 18개에 판단 backfill
- **감사가 드러낸 내용 중복 2건 정리**: `WLD-008`→`ITM-002`, `WLD-009`→`HIS-002`+`HIS-004`

### 근거
- 협업 프로토콜 §3이 "대화 내용을 인수인계 수단으로 쓰면 반드시 망가진다"고 하는데,
  **색인 자체가 그 규칙을 어기고 있었다.** 분류 판단을 채팅으로만 설명했다.
- 설정서 §26이 미정으로 남긴 순수 TBD 10건은 **면제**했다. 판단이 개입한 적이 없어서
  "원문이 TBD라 TBD"라고 쓰면 잡음만 늘고 진짜 판단을 찾기 어려워진다.
- 중복 2건은 2차에서 `HIS`·`ITM` 도메인을 신설하며 **재등재하고 원본을 안 지워서** 생겼다.
  **ID 중복은 기계로 잡히지만 내용 중복은 안 잡힌다** — 도메인 신설 시 사람이 확인해야 한다.

### 다음 AI에게
- [ ] **GPT 3차 재검수**: (a) backfill한 판단이 원문과 어긋나는지 (b) CANON 항목 중 판단이
      필요한데 비어 있는지 (c) 내용 중복 잔존 — 특히 WLD↔HIS/ITM/KGD, CHR↔RAC, CBT↔ITM
- [ ] CANON 항목의 판단 backfill은 **그 항목을 건드릴 때 채운다.** 지금 전수로 훑으면
      "판단이 없었는데 있었던 척" 적을 위험이 있다

### 오너 결정 필요
없음

**산출물**: 커밋 `c348578`

---

## 2026-08-18 10:14 · 작업 로그 체계 도입 (이 문서)

**담당**: Claude (구현)

### 요청
> 항상 답변 할때는 md파일 만들어서 다른 AI가 해당 부분의 대화 흐름을 놓칠 것을 대비해
> 그렇게 작성한 근거에 대해서 함께 첨부하고 내가 요청한 내용을 요약해서 작성 혹은 판단에
> 따라 원문을 그대로 작성하고 그에 대해서 어떻게 답변 했다 라고 시간을 함께 작성하고
> 자신이 아닌 다른 AI에게 해당 작업이 끝난 후에 전달해야 할 내용을 함께 적고, 일시를 명시하자.
> 이러한 작업은 월 단위로 md파일을 구분할거야. (…) 그 과정에서 내가 판단해주어야 하는 부분이
> 있으면 채팅에다가 직접 얘기하는거고.

### 작업
- `docs/log/README.md` — 형식·필드 규칙·다른 문서와의 관계 정의
- `docs/log/2026-08.md` — **이 세션 전체를 커밋 시각 기준으로 소급 작성** (10건)
- `COLLABORATION_PROTOCOL.md` §3.3 신설 — 작업 로그를 협업 규칙에 편입

### 근거
- **기존 문서에 실제로 구멍이 있었다.** `CURRENT_STATE.md`는 최신본만 남기고(덮어씀),
  `CHANGELOG.md`는 판단이 없고, `DECISIONS.md`는 작업 과정이 없다.
  **"어제 왜 그렇게 판단했는지"와 "직전 AI가 나에게 뭘 넘겼는지"가 어디에도 없었다.**
- 시각은 **커밋 타임스탬프를 사용**했다. 기억으로 시각을 지어내면 로그의 신뢰가 무너진다.
- 요청 인용은 **원문과 요약을 섞었다.** 설계 아이디어·수치·제약(예: LOD의 "5턴마다")은
  요약하면 정보를 잃으므로 원문을 남겼다.
- 파일을 **하나로 합치지 않고 `README`(규칙)와 `YYYY-MM`(기록)으로 나눴다.**
  규칙은 안 변하고 기록은 매달 늘어나므로 수명이 다르다.

### 다음 AI에게
- [ ] **작업이 끝날 때마다 이 파일에 항목을 추가한다.** 몰아 쓰면 근거를 잊는다
- [ ] **월이 바뀌면** `2026-09.md`를 만들고 미완료 인계사항을 새 파일 맨 위로 옮긴다
- [ ] 인계 항목이 처리되면 `[x]`로 바꾸고 **어느 항목에서 처리했는지** 적는다

### 오너 결정 필요
없음

**산출물**: 커밋 `bc6b8ef` · `COLLABORATION_PROTOCOL.md` §3.2~3.3

---

## 2026-08-18 10:14 · 저장소 누락 점검 (다른 PC 작업 대비)

**담당**: Claude (구현)

### 요청
> 지금 작업중인 컴퓨터 외 다른 컴퓨터에서 작업을 진행할 수도 있는데 git에 우리가 작업중인
> tower 폴더의 내용물이 전부 올라가있는지 확인하고 안올라가있으면 업로드도 해줘

### 작업
- `git status --untracked-files=all --ignored` + 디스크/추적 파일 수 대조
- 디스크 86개 / 추적 75개 → 차이 11개의 정체를 전부 확인:
  `.godot/` 8개(엔진 캐시) · `saves/.gitkeep` · 신규 로그 2개
- **결론: 실제 작업물은 전부 업로드돼 있었다.** 누락 없음
- **다만 문제 1건 발견·수정**: `.gitignore`의 `/saves/`가 폴더를 통째로 무시해
  **다른 PC에서 클론하면 `saves/` 폴더가 아예 생기지 않는다**
  → `/saves/*` + `!/saves/.gitkeep`로 변경
- `README.md`에 "다른 PC에서 이어받기" 절 추가 (clone → Godot 설치 → `--import`)

### 근거
- **`.godot/`는 일부러 올리지 않는다.** Godot이 프로젝트를 열 때 생성하는 캐시라서
  PC마다 내용이 달라 커밋하면 매번 충돌한다. `--import` 한 번으로 재생성된다.
- ANGLE 옵션(`--rendering-driver opengl3_angle`)은 **이 PC 전용 우회**임을 README에 명시했다.
  다른 PC에서 무조건 붙이면 멀쩡한 GPU에서도 D3D11 변환을 거쳐 성능만 손해다.
  먼저 옵션 없이 실행하고 종료 크래시가 날 때만 붙이도록 안내를 바꿨다.

### 다음 AI에게
- [ ] 다른 PC에서 작업 시작하면 **`--import` 먼저** 실행할 것 (`.godot/`가 없으므로)
- [ ] 그 PC의 GPU/드라이버에서 종료 크래시가 나는지 확인하고, 나면
      `docs/TEST_CHECKLIST.md` 환경 이슈 절에 **PC별로** 기록할 것

### 오너 결정 필요
없음

**산출물**: 커밋 `bc6b8ef`
