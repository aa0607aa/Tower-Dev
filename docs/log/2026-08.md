# 작업 로그 — 2026년 8월

> 형식과 규칙은 [README.md](README.md).
> 새 세션은 **오늘 날짜에 해당하는 파일**을 읽고, 맨 아래 항목의 "다음 AI에게"부터 확인한다.

**이 달 미완료 인계사항 (2026-08-18 11:48 기준)**

GPT(설계 담당)에게 넘긴 G-1~G-5:

- [x] **G-1. 테스트 러너 결정** — 자체 `SceneTree` 러너로 확정 (`D-015`). → 항목 `08-18 11:48`
- [x] **G-2. 출처 정확성 재검수** — 완료. → 항목 `08-18 10:49`
- [x] **G-3. 판단 근거 검수** — 완료. 과도한 해석/상태 분류를 정정. → 항목 `08-18 11:48`
- [x] **G-4. 내용 중복 잔존 검사** — 완료. 4묶음 추가 정리. → 항목 `08-18 11:48`
- [x] **G-5. `NPC-005` LOD 결정성 검수** — 완료. catch-up 외 추가 불변식 확정. → 항목 `08-18 11:48`

오너/구현 담당 쪽:

- [ ] **PHASE 1 착수 지시 대기** — G-1~G-5 선행 검수는 모두 닫힘. 오너 지시 전 구현 시작 금지.
- [ ] docx 개정 시 `tools/docx_to_md.ps1` 재실행 (항목 `08-17 14:07`)
- [ ] 다른 PC 작업 시 `--import` 먼저 (항목 `08-18 10:14`)
- [ ] 배포 렌더링 경로는 PHASE 11 (`B-003`)

---

## 2026-08-17 13:03 · STEP 0 — GitHub 최초 push

**담당**: Claude (구현)

### 요청
> 이 폴더는 아직 원격에 안 올라간 문서 스캐폴딩이다. git 상태를 확인하고, origin이 없으면
> `https://github.com/aa0607aa/Tower-Dev.git`로 연결한 뒤 main으로 push 해라.

작업 전 `COLLABORATION_PROTOCOL.md` → `docs/DEVELOPMENT_GUIDE.md` → `DECISIONS.md` →
`CURRENT_STATE.md` 순으로 읽으라는 지시 포함.

### 작업
- 문서 4종 정독 후 `git init -b main` → 첫 커밋 → origin 연결 → push
- `git ls-remote origin`으로 원격 ref 실제 존재 확인

### 근거
- push 성공을 stdout 메시지가 아니라 **원격 ref 조회로 검증**했다.
  PowerShell이 git stderr를 NativeCommandError로 감싸 성공을 실패처럼 보이게 하므로
  종료 코드나 출력만 믿으면 안 된다.

### 다음 AI에게
- [x] PHASE 0 착수 → 항목 `08-17 13:11`

### 오너 결정 필요
없음

**산출물**: 커밋 `26be95f`

---

## 2026-08-17 13:11 · PHASE 0 — Godot 프로젝트 뼈대

**담당**: Claude (구현)

### 요청
> 개발 가이드의 PHASE 0 정의와 폴더 구조를 따른다. Godot 4.7.1 stable + GDScript 프로젝트
> 뼈대 생성. 실행 가능한 최소 Boot/Main 씬을 만들고 실제로 실행해 오류 없는지 확인한다.
> PHASE 0 완료 조건 충족하면 멈춘다. 내 지시 전에는 PHASE 1로 넘어가지 마라.

### 작업
- `project.godot`(GL Compatibility · nearest 필터 · 1280×720), 개발 가이드 §6 폴더 구조 전체
- `Boot.tscn` → `Main.tscn`, `game_log.gd` autoload, `data/canon/canon.gd`
- `docs/CANON_NOTES.md` · `CODING_STYLE.md` · `TEST_CHECKLIST.md` · `assets/STYLE_GUIDE.md`
- **실행 검증 중 버그 1건 발견·수정**: `Boot._ready()`에서 `change_scene_to_file()` 직접 호출 시
  `Parent node is busy adding/removing children` 에러 → `await get_tree().process_frame` 후 전환

### 근거
- `data/canon/canon.gd`에 **[Resolved] 상수만** 넣었다. TBD 수치를 "일단 기본값"으로 채우면
  canon 위조가 되고, 다음 세션이 그것을 사실로 오해한다.
- 개발 가이드 §6은 `docs/DECISIONS.md`인데 협업 프로토콜 §6은 루트다.
  **협업 프로토콜이 우선**이라 루트를 유지하고 README에 명시했다.
- `.gitattributes`로 LF 정규화. Godot 에디터가 항상 LF로 재저장해 CRLF면 커밋마다 전체 diff가 난다.

### 다음 AI에게
- [x] ESC 종료 검증 — 오너가 직접 확인 (항목 `08-17 13:26`)

### 오너 결정 필요
없음

**산출물**: 커밋 `72c4ff4` `0b69769`

---

## 2026-08-17 13:26 · OpenGL 드라이버 설치와 종료 크래시 규명

**담당**: Claude (구현) · 오너 (UAC 승인 · ESC 검증)

### 요청
> OpenGL 3.3 드라이버도 설치하자

(선행 요청: "Godot 4.7.1 설치 상태 확인하고 없으면 안내해줘" → winget으로 설치 완료)

### 작업
- 진단: 실 하드웨어는 AMD Radeon R5 240(`PCI\VEN_1002&DEV_6611`)인데 드라이버 미설치로
  Microsoft 기본 디스플레이 어댑터로 동작 → OpenGL 3.3 불가
- 관리자 권한 승격(오너 UAC 승인) 후 Windows Update의 AMD 드라이버 설치 → OpenGL 3.3 확보
- **설치 후 새 문제**: 창 실행 시 종료 코드 `0xC0000409` 크래시
- 격리 테스트: headless exit 0 / 네이티브 GL 창 크래시 3/3 /
  **스크립트 없는 빈 프로젝트도 크래시 3/3** / Vulkan·D3D12 미지원 →
  **ANGLE 강제 시 exit 0, 3/3**

### 근거
- 빈 프로젝트에서 재현되므로 **우리 코드가 아니라 드라이버+엔진 문제**임을 확정했다.
- **ANGLE을 `project.godot`에 넣지 않았다.** 개발 PC 한 대의 드라이버 버그로 배포 경로를 정할 수 없다.
  → `docs/BACKLOG.md` B-003.
- 드라이버 설치는 관리자 권한이 필요해 오너 UAC 승인을 받았다.

### 다음 AI에게
- [x] 개발 PC에서 창 실행 시 `--rendering-driver opengl3_angle` 사용 — README·TEST_CHECKLIST에 기록
- [ ] 배포 렌더링 경로 결정은 PHASE 11 (`B-003`)

### 오너 결정 필요
없음 (ESC 종료는 오너가 직접 눌러 검증 완료 → PHASE 0 완료 조건 전부 충족)

**산출물**: `docs/TEST_CHECKLIST.md` 환경 이슈 절 · 커밋 `f518da8`에 반영

---

## 2026-08-17 13:34 · canon 색인 체계 구축 (1차) + 데이터 마이닝 정책 검토

**담당**: Claude (구현)

### 요청
> 실제 게임 개발에 착수하기 전에 설정이 제대로 탄탄하게 잡히고 그렇게 잡힌 설정이 분류가
> 제대로 되서 필요할 때 딱딱 불러올 수 있어야 한다고 생각해.

> 데이터 마이닝 자체를 막는 것이 아니라 일반적인 방법으로는 다 확인할 수 없을 만큼 수 많은
> 데이터를 쌓고 더미 데이터를 섞어서 원하는 내용을 찾지 못하게 만드는 방법을 택하면 어떨까.

### 작업
- `docs/canon/` 신설 — ID 체계, 도메인 분류, 상태(CANON/DESIGN/TBD/PROPOSAL)
- 1차 등재 63항목 / 7도메인 (당시 설정서 원본 미입고)
- `docs/DATAMINING_POLICY.md` 작성 → `D-012` Proposed 등록

### 근거
- JSON 색인을 따로 두지 않았다. 마크다운과 JSON 두 진실이 생기는 것을 방지.
- 데이터 마이닝 원안은 방향은 옳지만 문자 그대로 구현하면 실패한다고 판단.
  코드가 참조하지 않는 더미는 쉽게 걸러지고, 진짜에만 적용되는 검증 규칙도 판별기가 된다.
  → 3 Tier로 분리 제안.

### 다음 AI에게
- [x] 설정서 원본 입고 시 63항목 전수 대조 → 항목 `08-17 13:45`

### 오너 결정 필요
- **D-012 Q1/Q2/Q3** → 항목 `08-17 13:55`에서 확정

**산출물**: 커밋 `5a3f6e8` `c9aac08`

---

## 2026-08-17 13:45 · 설정서 원본 대조 — 63 → 133항목

**담당**: Claude (구현) · 오너 (원본 배치)

### 요청
오너가 `docs/SETTING_BIBLE_v1.1.docx`를 저장소에 배치.

### 작업
- docx 압축 해제 → `word/document.xml` 파싱해 전문(§0~§27) 확보
- 원문에만 있던 도메인 8개 신규 등재: HIS · RAC · MAG · ITM · NPC · KGD · DGN · GOD
- 기존 도메인 보정
- `[CANON CONFLICT]` 0건

### 근거
- 1차 색인은 DECISIONS+개발 가이드 기반이라 종족·신좌·마법·왕국·리프트·메인던전·NPC 규칙이 빠져 있었다.
- 원본 입고 전 임시 색인 단계의 ID 재배정 흔적은 FAC.md 하단에 기록.

### 다음 AI에게
- [x] 절 단위 커버리지 감사 → 항목 `08-17 13:55`

### 오너 결정 필요
없음

**산출물**: 커밋 `a793e6c`

---

## 2026-08-17 13:55 · D-012 확정 + 커버리지 감사 (누락 4건)

**담당**: Claude (구현) · 오너 (D-012 결정)

### 요청
> Q1. 허가 / Q2. 나중에 하자 / Q3. 1층은 괜찮아. 어차피 알더라도 큰 변화는 없어.
>
> 설정 정리는 제대로 한거야?

### 작업
- D-012 Resolved — Tier 1·2 → SYS-009, Q3 → SYS-014, Tier 3 → B-001
- `docs/BACKLOG.md` 신규
- 설정서 절 84개 전수 매핑 → 누락 4건 발견·보완 → 137항목

### 근거
- "제대로 했나"를 기계 감사로 확인했고 누락 4건이 실제로 나왔다.
- 1층 지형 공개는 FLR-015/CHR-014의 지식 기반 성장과 일치.

### 다음 AI에게
- [x] 마크다운 사본 필요성 검토 → 항목 `08-17 14:07`

### 오너 결정 필요
없음

**산출물**: 커밋 `f518da8`

---

## 2026-08-17 14:07 · 설정서 마크다운 사본 + 자기검증 변환기

**담당**: Claude (구현)

### 요청
> 지금 정리한 내용들 GPT가 확인할 수 있게 하려면 git에 연결해주고 확인하라고 하면 돼?
> → 응 변환본도 만들어.

### 작업
- `tools/docx_to_md.ps1` — Word 스타일 매핑 기계 변환
- `docs/SETTING_BIBLE_v1.1.md` 생성 (원문 666문단, 누락 0)
- 저장소 공개 상태 확인

### 근거
- 손으로 옮겨 원문과 사본이 어긋나는 일을 막고 기계 변환/누락 검증 사용.
- docx를 못 읽는 AI가 Canon 없이 검수하는 문제보다 읽기 전용 사본의 위험이 작다고 판단.

### 다음 AI에게
- [ ] **docx 개정 시 `tools/docx_to_md.ps1` 재실행 필수.**
- [x] 테스트 러너 결정 → `D-015`, 항목 `08-18 11:48`

### 오너 결정 필요
없음

**산출물**: 커밋 `1ea4f7f`

---

## 2026-08-18 09:54 · GPT 교차검증 6건 반영 — D-013 · D-014

**담당**: GPT (설계 검수) · Claude (구현) · 오너 (결정)

### 요청
GPT 6건 지적 + 오너의 NPC LOD 원안.

### 작업
- "레벨당 스탯 포인트 1" 원문 재검색 → 설정서에는 없고 개발 가이드 문장임을 확인.
- 경험치도 §8.2 초기화 목록에 없음 확인.
- `D-013` 신설 — 규칙 유지, 출처를 오너 결정으로 정정. `CHR-020` 분리.
- `D-014` 신설 — NPC 시뮬레이션 LOD.
- `KGD-008`, `FAC-004`, `KGD-001`, `RAC-004` 등 반영 → 141항목.

### 근거
- 오너가 "패스"라고 한 항목도 원문과 충돌하면 출처를 바로잡아야 다음 감사에서 반복되지 않는다.
- LOD는 엔진 시뮬레이션 빈도와 AI 호출 예산을 분리하고, 저해상도도 결정적이어야 한다.

### 다음 AI에게
- [x] 판단 근거 기록 체계 → 항목 `08-18 10:04`
- [x] D-013 유형 출처 정확성 재검수 → 항목 `08-18 10:49`
- [x] D-014 결정성 추가 검수 → 항목 `08-18 11:48`

### 오너 결정 필요
없음

**산출물**: 커밋 `e1906d7`

---

## 2026-08-18 10:04 · 판단 근거 필드 신설 + 내용 중복 2건 정리

**담당**: Claude (구현)

### 요청
> 설정집에 내용 추가해서 커밋할 때 나한테 설명하는 것 처럼 해당 내용에 대한 근거도 함께 적고있지?

### 작업
- 출처 141/141, 판단 근거 20/141 확인
- `docs/canon/README.md`에 `판단` 필드와 사용 기준 추가
- DESIGN·혼합 상태 항목에 판단 backfill
- 중복 2건 정리: `WLD-008→ITM-002`, `WLD-009→HIS-002+HIS-004`

### 근거
- 판단 이유가 채팅에만 있으면 협업 프로토콜의 Git 인계 원칙을 위반.
- 원문 §26의 순수 TBD는 판단이 개입하지 않았으므로 억지 backfill하지 않음.

### 다음 AI에게
- [x] GPT G-3 판단 근거 재검수 → 항목 `08-18 11:48`
- [x] 내용 중복 잔존 G-4 → 항목 `08-18 11:48`

### 오너 결정 필요
없음

**산출물**: 커밋 `c348578`

---

## 2026-08-18 10:14 · 작업 로그 체계 도입

**담당**: Claude (구현)

### 요청
> 항상 답변 할때는 md파일 만들어서 다른 AI가 대화 흐름을 놓칠 것을 대비하고, 근거와 요청,
> 답변 시각, 다음 AI에게 전달할 내용, 오너 판단 필요 사항까지 기록하자. 월 단위로 구분한다.

### 작업
- `docs/log/README.md` — 형식·필드 규칙
- `docs/log/2026-08.md` — 기존 세션 소급 작성
- `COLLABORATION_PROTOCOL.md` §3.3 편입

### 근거
- CURRENT_STATE/CHANGELOG/DECISIONS만으로는 "왜"와 직전 AI 인계가 사라지는 구멍이 있었음.
- 시각은 커밋 타임스탬프 사용.

### 다음 AI에게
- [ ] 작업이 끝날 때마다 이 파일에 항목 추가
- [ ] 월이 바뀌면 새 파일로 미완료 인계 이동
- [ ] 처리된 인계는 `[x]` + 처리 항목 표시

### 오너 결정 필요
없음

**산출물**: 커밋 `bc6b8ef`

---

## 2026-08-18 10:14 · 저장소 누락 점검 (다른 PC 작업 대비)

**담당**: Claude (구현)

### 요청
> 다른 컴퓨터에서 작업할 수도 있는데 git에 작업물 전부 올라가있는지 확인하고 누락되면 업로드해줘.

### 작업
- 디스크/추적/ignored 비교 → 실제 작업물 누락 없음.
- `/saves/` ignore를 `/saves/*` + `!/saves/.gitkeep`로 수정.
- README에 다른 PC 이어받기 절 추가.

### 근거
- `.godot/`은 PC별 엔진 캐시라 커밋하지 않음.
- ANGLE은 해당 개발 PC의 로컬 우회이며 전역 프로젝트 설정이 아님.

### 다음 AI에게
- [ ] 다른 PC 시작 시 `--import` 먼저
- [ ] PC별 GPU/드라이버 종료 크래시가 있으면 TEST_CHECKLIST에 기록

### 오너 결정 필요
없음

**산출물**: 커밋 `bc6b8ef`

---

## 2026-08-18 10:22 · GPT 인계 문구 정리 (G-1 ~ G-5)

**담당**: Claude (구현)

### 요청
> 지금까지 얘기한 내용 GPT한테 전달하게 정리해서 말해주고 전달할 부분도 같이 적어줘.

### 작업
- G-1~G-5를 로그 상단 미완료 인계로 번호화.
- 커밋 해시/파일 경로와 우선순위를 명시.

### 근거
- 번호가 있어야 GPT 답변 후 어떤 항목이 닫혔는지 추적 가능.
- D-013 유형의 출처 오류를 최우선 재검수 대상으로 지정.

### 다음 AI에게
- [x] G-1~G-5 처리 → G-2는 10:49, 나머지는 11:48 완료

### 오너 결정 필요
없음 (G-1은 11:48에 오너가 직접 확정)

**산출물**: 커밋 `9832b2a`

---

## 2026-08-18 11:02 · G-2 결과 검수 (구현 담당)

**담당**: Claude (구현) — GPT 작업물 교차검증

### 요청
> GPT가 작업 및 커밋 진행했으니까 잘 되어있나 확인해볼래?

### 작업
- GPT G-2 커밋들을 fetch/pull하고 설정서 원문과 재대조.
- CBT-003/CHR-003/CBT-004/CHR-007/008/FLR-010/RAC-001/CBT-001의 수정이 타당함을 확인.
- 판단 필드 36개로 증가 확인.
- GPT 수정 파일 10개의 끝 개행 누락을 복구.

### 근거
- 구현 담당이 GPT 결과를 원문으로 다시 검증해 리뷰를 겹쳐 수행.
- 출처 감사는 "없는 걸 넣었나"와 "있는 걸 빠뜨렸나" 양방향으로 해야 함.

### 다음 AI에게
- [x] G-3 판단 필드 36건 검수 → 항목 `08-18 11:48`
- [x] G-1 결정 → 항목 `08-18 11:48`
- [x] G-4/G-5 → 항목 `08-18 11:48`
- [ ] Markdown 끝 LF 확인
- [ ] PowerShell 다중행 커밋 메시지는 `git commit -F`

### 오너 결정 필요
없음

### 사고 기록 — 커밋 메시지와 로그 해시 불일치

`git commit -m @'...'@` here-string 실패 후 커밋 성공 확인 전에 `rev-parse HEAD`를 실행해 이전 SHA를
로그에 적는 사고가 있었다. 이미 push된 기록은 히스토리를 다시 쓰지 않고 사고 기록으로 보존.

**교훈**: 커밋 성공 확인 후 SHA 기록.

**산출물**: `79d5c25` · `6f34902`

---

## 2026-08-18 10:49 · G-2 출처 정확성 재검수 및 직접 반영

**담당**: GPT (설계 검수 · GitHub 문서 수정)

### 요청
오너가 G-1~G-5 중 G-2를 우선 처리하도록 요청. `SETTING_BIBLE_v1.1.md`와 `docs/canon/` 역대조.
이어 "답변·근거·시각·다음 AI 인계·커밋까지가 한 세트"라고 재확인.

### 작업
- CANON/혼합 항목의 출처를 색인→원문 역방향 재검수.
- 설정서 정본 외 유래 규칙 추적표 확대.
- FLR-010, CBT-001/003/007, CHR-007/008/009/010/011, SYS-001/007/011,
  FAC-003/004, KGD-008, RAC-001 등 원문 뉘앙스/출처/판단 분리.

### 근거
- D-013에서 개발 가이드 문장이 설정서 문장으로 잘못 승격된 실제 사례가 발생했으므로
  색인의 강한 표현이 원문에 실제 존재하는지를 제3자가 검사할 필요가 있음.

### 다음 AI에게
- [x] G-2 닫힘.
- [x] G-3 판단 검수 → 11:48.
- [x] G-4 내용 중복 → 11:48.
- [x] G-1/G-5 → 11:48.
- [ ] 설정서 밖 새 확정 규칙은 DECISIONS + INDEX 정본 외 유래 표 동시 추적.

### 오너 결정 필요
없음

**산출물**: `3c6f7dd` `a505a79` `3ded02b` `40f097c` `c4097a0` `3bbe5fd` `6605787` `6f61470` `96e77a8` `ec1b3a2`

---

## 2026-08-18 11:23 · Git 인계 상태 확인 및 CURRENT_STATE 동기화

**담당**: GPT (설계 · 인계 검수)

### 요청
> git 인계 확인해봐

### 작업
- 최신 커밋/월간 로그/CURRENT_STATE/DECISIONS/BACKLOG/CHANGELOG 대조.
- G-1 미완료, G-2 완료, G-3~G-5 미완료 상태 확인.
- CURRENT_STATE가 `오너 결정 대기: 없음`으로 뒤처진 불일치 발견 → 최신 인계 기준으로 재작성.

### 근거
- Git 저장소가 AI 간 공유 진실이므로 CURRENT_STATE가 월간 로그보다 뒤처지면 새 AI가 선행조건을 건너뛸 수 있음.

### 다음 AI에게
- [x] G-1 결정 → 항목 `08-18 11:48`
- [x] G-3/G-4/G-5 → 항목 `08-18 11:48`
- [ ] CURRENT_STATE와 로그가 어긋나면 최신 로그와 실제 커밋으로 즉시 동기화

### 오너 결정 필요
없음

**산출물**: `1f19119` · `47e1b5d`

---

## 2026-08-18 11:48 · G-1 확정 + G-3/G-4/G-5 설계 감사 완료

**담당**: GPT (설계 · canon 감사 · GitHub 문서 수정) · 오너 (G-1 최종 결정)

### 요청
> 클로드는 자체 scene tree 사용을 추천하더라. 너도 괜찮다고 하면 그걸로 결정

이어서:
> 그럼 G-1은 scenetree로 결정하고 나머지 G3~G5도 진행해둬

### 작업

#### G-1 — 테스트 러너
- **자체 `SceneTree` 러너를 확정**, `D-015` Resolved 추가.
- GUT은 현재 도입하지 않음.
- 테스트 본문을 **프레임워크 중립**으로 작성: 순수 함수 + 작은 assertion helper.
- 러너는 발견/실행/집계/종료 코드만 담당하고 실패 시 non-zero exit.
- `tests/README.md`, `docs/TEST_CHECKLIST.md`, `docs/BACKLOG.md B-004`, `CURRENT_STATE.md` 반영.
- PHASE 1 착수 시 `tests/runner.gd`를 실제 구현하도록 인계.

#### G-3 — 판단 근거 감사
`SETTING_BIBLE_v1.1.md`와 판단 필드/혼합 상태를 재대조했다.

수정:
- `HIS-003`: §5.5 Event Node 블록 전체가 원문에서 DESIGN인데 일부를 "실질적 CANON"으로 승격한 판단 제거.
- `CBT-007`: 원문 §12.4가 기습의 관통·절삭 방향과 리치 관계를 직접 확정 →
  **DESIGN → CANON(방향) / TBD(수치)**.
- `CBT-013`: "부위 히트박스만 가능"은 원문보다 강함. 랜덤 크리티컬 우회 금지는 유지하되,
  실제 타격 위치/방향/해부학적 조건처럼 재현 가능한 사건 판정이면 허용. 히트박스는 우선 구현안.
- `CHR-011`: "하드 카운터 금지"를 **상성만으로 승패를 확정하는 hard lock 금지**로 좁힘.
  강한 유불리 자체는 수치 TBD가 결정할 영역.
- `RAC-004`: 열린 목록이라는 사실은 CANON, 추가 종족의 이름·계수·특성만 TBD.
- `FLR-011`: `clues[]` 필드와 "비면 테스트 실패"는 설정서 문장이 아니라 Canon을 강제하는 구현 Hard Rule임을 명시.

**판정 방침**: CANON 전 항목에 억지로 판단 필드를 채우지 않는다. 원문을 그대로 옮긴 경우에는
"판단이 없었는데 있었던 척" 만들지 않고, 실제 해석/분류/구현 제약이 들어간 지점에만 기록한다.

#### G-4 — 내용 중복 감사
추가 중복 4묶음을 정리했다.

| 중복 | 정본 | 조치 |
| --- | --- | --- |
| CBT-005 ↔ ITM-003 무기 수명 | ITM-003 | CBT-005 포인터화 |
| CHR-006 ↔ SKL-003 3점 투자→스킬 후보 | SKL-003 | CHR-006은 성장값/하드캡만 유지 |
| FAC-004 ↔ NPC-003 시설 경험 정보 전파 | NPC-003 | FAC-004는 시설 인식만 + 참조 |
| WLD-004 ↔ CHR-012 보존/초기화 상세 | CHR-012 | WLD-004는 죽음=새 회차 의미만 유지 |

`KGD-002↔CHR-011`, `DGN-004↔FLR-011`, `SYS-010`의 데이터 소유권 목록은 서로 다른 추상화/역할이라
중복으로 제거하지 않았다.

#### G-5 — NPC-005 LOD 결정성
**catch-up만으로는 SYS-003 결정성을 보장하지 못한다**고 판정했다.
`D-014`, `NPC-005`, `TEST_CHECKLIST G-7`에 추가한 불변식:

1. LOD는 계산 빈도/표현 해상도만 바꾸고 같은 시드/입력/논리 시간의 Canon 결과는 바꾸지 않는다.
2. RNG 소비 순서를 플레이어 거리와 분리한다. NPC/시스템 독립 RNG 또는 안정 키 사용.
3. 승격 전 `last_sim_tick → current_tick` catch-up 완료 후 관찰/충돌/대화 허용.
4. 같은 논리 틱 사건은 안정적 ID/key 순서로 처리. 월드 영향 사건은 플레이어 접근까지 미루지 않는다.
5. `last_sim_tick`, RNG 상태/이벤트 카운터, 예약 사건/핵심 상태를 세이브에 보존.
6. 저LOD 전용 확률표/별도 규칙으로 "대충 결과를 뽑는" 방식 금지. 등가성이 어려우면 결정적 예약 또는 chunked catch-up.

검증은 **플레이어 경로만 바꿔 LOD 승격/강등 시점을 달리해도 NPC Canon 상태와 외부 사건 로그가 동일한지**로 한다.

### 근거
- 개발 가이드 §B5는 외부 플러그인을 실제로 시간을 줄일 때만 도입하도록 한다.
  PHASE 1~3의 자동 검증은 프레임 독립성·대각선 정규화·고정 지형·시드 결정성·`clues[]` 같은
  데이터/순수 로직 단언이 중심이라 지금 GUT의 추가 계층이 주는 이득보다 자체 러너가 단순하다.
- Godot 4.7 계열에서 `SceneTree`는 기본 MainLoop이고 `--script`로 스크립트를 직접 실행할 수 있어
  headless 자체 러너가 가능하다. 테스트 본문을 러너와 분리하면 나중에 GUT로 갈아탈 비용도 낮다.
- G-3은 원문의 `CANON/DESIGN/TBD` 자체를 우선하고, 판단문이 그 상태를 몰래 승격하거나 구현 하나만
  유일한 정답으로 만들지 않는지를 기준으로 봤다.
- G-4는 "관련 항목이 여러 도메인에 언급되는 것"이 아니라 **같은 사실을 독립적으로 두 번 유지해
  어느 쪽이 정본인지 모호한 경우**만 제거했다.
- G-5는 LOD 빈도가 플레이어 위치에 의해 달라질 수 있으므로 단순 catch-up만 쓰면 RNG 호출 횟수와
  동시 사건 순서가 달라질 수 있다. 따라서 논리 시각과 결정성 상태를 플레이어 관찰과 분리해야 한다.

### 다음 AI에게
- [x] **G-1~G-5는 모두 닫혔다.** 같은 감사를 처음부터 반복하지 말고 `docs/canon/INDEX.md`의 5~7차 감사 이력부터 읽을 것.
- [ ] 다음 실개발은 **PHASE 1**이다. 단 오너의 별도 착수 지시가 있을 때만 시작한다.
- [ ] PHASE 1 첫 작업에 `tests/runner.gd`를 만들고 대각선 정규화/프레임 독립성 테스트를 먼저 붙일 것.
- [ ] SceneTree 러너에 fixture/mock/비동기/리포팅 코드가 계속 늘어나 **러너 유지가 게임 테스트보다 비싸지면** GUT 재검토.
- [ ] `CBT-007` 방향은 Canon이므로 기습이 일반 피해 배율만 올리는 구조로 구현하지 말 것. 정확한 수치는 TBD.
- [ ] `CBT-013` 급소는 랜덤 크리티컬 우회가 되면 안 되지만 부위 히트박스 하나만이 유일한 구현은 아님.
- [ ] NPC LOD는 `D-014`의 G-5 불변식과 `TEST_CHECKLIST G-7`을 반드시 만족할 것.
- [ ] 설정서 밖에서 새 확정 규칙이 생기면 DECISIONS + INDEX의 정본 외 유래 표를 함께 갱신할 것.
- [ ] 문서 수정 시 끝 LF 유지. 구현 담당이 로컬 PowerShell에서 다중행 커밋 시 `git commit -F` 사용.

### 오너 결정 필요
없음. G-1은 이 요청에서 **자체 SceneTree로 오너가 직접 확정**했다. G-3~G-5는 새 세계 Canon을 만들지 않고
원문 상태/중복/결정성 구현 제약을 정리한 감사다.

### 산출물
- `DECISIONS.md` — `8245418` (D-015 + D-014 G-5 보강)
- `docs/BACKLOG.md` — `0f4a7f7` (B-004 종료)
- `tests/README.md` — `498fa3f`
- `docs/TEST_CHECKLIST.md` — `e219111`
- `docs/canon/HIS.md` — `1a5b44c`
- `docs/canon/CBT.md` — `9bc7c94`
- `docs/canon/CHR.md` — `b88442a`
- `docs/canon/RAC.md` — `bc0eeef`
- `docs/canon/FLR.md` — `93ff55f`
- `docs/canon/FAC.md` — `b511994`
- `docs/canon/WLD.md` — `835ac97`
- `docs/canon/NPC.md` — `ee9a6f0`
- `docs/canon/INDEX.md` — `74fae69`
- `CURRENT_STATE.md` — `1877f38`
- `CHANGELOG.md` — `7e4b85e`
- 이 로그 항목 — 본 커밋
