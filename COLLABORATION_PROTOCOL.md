# COLLABORATION_PROTOCOL.md — 「탑」 다중 에이전트 개발 협업 규칙

> **모든 에이전트는 새 작업을 시작하기 전에 이 문서를 가장 먼저 읽는다.**
> 이 문서는 개발 가이드(DEVELOPMENT_GUIDE)와 **독립적으로** 유지된다. 개발 가이드가 v2.1, v2.2로 바뀌어도 협업 규칙은 여기서 따로 관리한다.
>
> 협업 프로토콜 v1.0 · 2026-08-17

---

## 0. 대전제 — 역할은 "모델명"이 아니라 "능력/환경" 기준

이 프로젝트는 여러 AI 에이전트(예: ChatGPT, Claude 등)와 사람(프로젝트 오너)이 함께 개발한다. **어떤 모델이 어떤 역할을 맡는지는 고정된 정체성으로 정하지 않는다.** 대신 그 세션이 실제로 가진 능력과 도구로 역할을 정한다.

- 실제 저장소의 코드·PR·diff를 읽을 수 있는가 (GitHub 연결 여부)
- 코드를 작성·수정·리팩터할 수 있는가
- 쉘/자동 테스트/런타임 검증을 실행할 수 있는가
- Godot GUI를 직접 플레이해 조작감을 검증할 수 있는가 (이건 기본적으로 **사람의 몫**)

같은 모델이라도 세션 환경에 따라 가능/불가능이 달라진다. 따라서 아래 역할표는 "누가"가 아니라 "무엇을 할 수 있는 세션인가"로 읽는다.

---

## 1. 역할과 최종 권한

| 영역 | 최종 권한 / 담당 |
| --- | --- |
| 세계관 Canon | **설정서 v1.1 + DECISIONS.md** (문서가 진실) |
| Canon 최종 승인 | **프로젝트 오너(사람)** |
| Canon 정합성 추적 | 모든 AI 에이전트 (둘 다) |
| 시스템 설계 / 기능 분해 | 주로 설계 담당 에이전트 |
| 실제 저장소 타당성 검토 | 구현 담당 에이전트 |
| 코드 구현 / 대규모 리팩터 | 실제 프로젝트 + 실행 환경을 가진 에이전트 |
| 아키텍처·로직·설정 리뷰 | 설계 담당 + 구현 담당 **교차검증** |
| 자동 테스트 / 런타임 검증 | 실행 가능한 환경을 가진 세션 |
| 플레이 감각 / 도트 가독성 / 조작감 | **오너의 로컬 플레이테스트** |
| 실제 코드의 단일 진실 | **GitHub 저장소** |
| 결정 이력 | `DECISIONS.md` |
| 현재 작업 상태 | `CURRENT_STATE.md` |
| 변경 이력 | `CHANGELOG.md` |

### 핵심 원칙

- **엔진이 진실의 단일 출처(SSOT).** AI가 출력한 텍스트만으로 게임 상태·canon을 바꾸지 않는다.
- **어떤 AI도 canon을 스스로 결정하지 않는다.** AI는 canon의 *관리인/추적자*이지 결정권자가 아니다.
- **리뷰와 검증은 겹쳐서 간다.** 설계 리뷰와 실행 검증은 서로 다른 성격이므로 한쪽만으로 "완료"가 되지 않는다.

---

## 2. Canon 관리 — 관리인/추적자 역할

AI는 새로운 설정을 만들지 않는다. 대신 충돌을 찾아 오너에게 **결정 요청**으로 올린다. 형식:

```
[CANON CONFLICT]
위치: 설정서 v1.1 §<번호> / DECISIONS.md <항목>
현재 Canon(A): ...
이번 구현안/제안(B): ...
충돌 내용: A와 B가 <어떻게> 충돌함
필요한 결정: A 유지 / B로 변경 / 제3안
영향 범위: <어떤 코드·데이터·PHASE에 영향>
```

오너가 결정하면 → 그 결정을 `DECISIONS.md`에 `[Resolved]`로 기록 → 이후 **모든 에이전트가 그 결정을 따른다.** 오너 승인 전에는 어떤 것도 canon으로 굳히지 않는다(`[Proposed]` 상태 유지).

---

## 3. 핸드오프 — 대화가 아니라 저장소로 넘긴다

두 AI는 서로의 기억을 공유하지 않는다. **대화 내용을 인수인계 수단으로 쓰면 반드시 망가진다.** 모든 상태는 저장소의 파일과 커밋으로 넘긴다.

### 3.1 작업 종료 시 — WORK REPORT 강제

모든 작업(구현/리팩터/설계 반영)이 끝나면 아래를 `CURRENT_STATE.md`에 갱신하고 커밋 메시지/보고에 남긴다.

```
[WORK REPORT]
작업 ID:
PHASE:
목표:
변경 파일:
- res://...
- res://...
구현 내용:
-
테스트:
- 실행 명령:
- 성공:
- 실패:
설정 관련 결정:
- 없음 / DECISION 필요
알려진 문제:
-
다음 작업:
-
완료 등급: IMPLEMENTED / VERIFIED / PLAYTESTED
Git commit:
<해시>
```

### 3.2 작업 시작 시 — 읽기 순서

새 세션(어느 에이전트든)은 아래 순서로 컨텍스트를 복원한 뒤 작업을 시작한다.

```
1. COLLABORATION_PROTOCOL.md   (이 문서 — 규칙)
2. 설정서 v1.1                  (canon)
3. DECISIONS.md                (확정된 결정)
4. CURRENT_STATE.md            (지금 어디까지 왔나)
5. 최근 CHANGELOG.md           (무엇이 바뀌었나)
6. 최근 Git diff / 관련 코드    (실제 상태)
```

---

## 4. 작업 분해 루프 — PHASE → 티켓 → 타당성 → 확정

기획서만 보고 구현 순서를 강제로 확정하지 않는다. 실제 코드 구조를 아는 쪽의 피드백을 거쳐 **실행 가능한 티켓**으로 확정한다.

1. **분해 제안**: 설계 담당이 "PHASE N을 이런 티켓들로 나눈다"를 제안한다. (이 시점엔 *제안*이지 명령이 아니다.)
2. **타당성 검토**: 구현 담당이 실제 저장소를 보고 피드백한다. 예) "Ticket 3·4는 합치는 게 낫다", "Ticket 6은 선행 작업이 필요하다".
3. **확정**: 피드백을 반영해 실행 가능한 티켓 목록으로 확정한다.
4. **구현·보고**: 구현 후 §3.1 WORK REPORT로 마감한다.

---

## 5. 완료의 3단계 — IMPLEMENTED / VERIFIED / PLAYTESTED

자동 테스트가 통과해도 게임의 "재미·조작감·가독성"은 코드로 검증할 수 없다. 그래서 완료를 세 단계로 나눈다.

| 등급 | 의미 | 누가 판정 |
| --- | --- | --- |
| **IMPLEMENTED** | 코드가 작성됨 | 구현 담당 |
| **VERIFIED** | 자동/런타임 테스트 통과 | 실행 가능한 세션 |
| **PLAYTESTED** | 오너가 실제 플레이하고 승인 | **오너** |

예) 이동 시스템을 구현 담당이 `VERIFIED`까지 만들어도, 오너가 직접 돌려보고 조작감을 승인해야 **PLAYTESTED**가 된다. 아래 같은 항목은 반드시 PLAYTESTED가 필요하다: 이동감(미끄러움), 공격 선딜, 함정 단서 도트 가독성, 적 공격 회피 난이도, UI 가림, 대시의 손맛 등. 전투 단계로 갈수록 더 중요해진다.

---

## 6. 저장소 문서 구조

```
/docs
  SETTING_BIBLE.docx            # 세계관 canon (v1.1) — 진실
  DEVELOPMENT_GUIDE.docx/.md    # 개발 가이드 (v2.x) — 구현 방식
DECISIONS.md                    # 결정 이력 (Resolved / Proposed / TBD)
CURRENT_STATE.md                # 현재 작업 상태 (WORK REPORT 최신본)
CHANGELOG.md                    # 확정된 변경 이력
COLLABORATION_PROTOCOL.md       # 이 문서 — 협업 규칙 (독립 관리)
```

---

## 7. 요약 — 지켜야 할 5가지

1. 시작 전 이 문서 → canon → DECISIONS → CURRENT_STATE → CHANGELOG → diff 순으로 읽는다.
2. canon은 문서와 오너가 주인이다. AI는 충돌을 찾아 올릴 뿐 결정하지 않는다.
3. 핸드오프는 대화가 아니라 저장소 파일과 커밋으로 한다. WORK REPORT를 남긴다.
4. 분해는 제안 → 타당성 검토 → 확정의 루프를 거친다.
5. 완료는 IMPLEMENTED → VERIFIED → PLAYTESTED 3단계이며, 마지막은 오너만 판정한다.

*협업 프로토콜 v1.0 · 2026-08-17 · 개발 가이드와 독립적으로 관리*
