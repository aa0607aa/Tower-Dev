# CURRENT_STATE.md — 현재 작업 상태

> 새 세션은 이 파일로 "지금 어디까지 왔는지"를 복원한다.
> 상세 시계열과 판단 근거는 `docs/log/2026-08.md`와 최신 보조 로그를 확인한다.

## 현재 위치 — 2026-08-20

- **PHASE 0**: 완료 (`VERIFIED`)
- **PHASE 1 (이동/카메라)**: 완료 (`PLAYTESTED`)
- **PHASE 2 (1층 공간/상태/계단/세이브)**: **완료 (`PLAYTESTED`)** — 2026-08-20 종료
  - [x] `P2-T0` Canon 가드 테스트
  - [x] `P2-T1` 공간 데이터 타입 (`WorldAnchor`, `AccessEnvelope`, 공유 `TerrainMutationState`)
  - [x] `P2-T2` 1층 고정 greybox + loader
  - [x] `P2-T3` 함정/파밍 정적 정의와 동적 상태 분리
  - [x] `P2-T4` AccessEnvelope 실제 Player 이동 연결
  - [x] `P2-T5` 계단 resolver
  - [x] `P2-T6` save/load + immutable definition hash
  - [x] `P2-T7` 자동 회귀/변이 검증
  - [x] `P2-REV-001~003` 수정 + GPT 재검토
  - [x] `P2-REV-004~007` 수정 + `DOC-REV-001~003` 정리
  - [x] **GPT 최종 재검토 통과** — 새 BLOCKER 없음
  - [x] **오너 layout v2 재플레이 완료** (통로 폭 수정 후)
- **PHASE 3 (상호작용·함정·파밍)**: **완료 (`PLAYTESTED`)** — 2026-08-20 종료
  - [x] `P3-T0` PHASE 전환/계약 정리
  - [x] `P3-T1` 상호작용 입력과 대상 선택 (`InteractionService`, `E`/`Enter`)
  - [x] `P3-T2a` `RunState`/`WorldState` 소유권 골격 + 가드 테스트
  - [x] `P3-T2b` `ItemInstance` + 인벤토리
  - [x] `P3-T3` 바닥 아이템 실체 + 줍기/버리기 + `RunSave` 왕복
  - [x] `P3-T4` 함정 trigger mechanism 계약 (`TrapStimulus`/`TrapRuntime`)
  - [x] `P3-T5` 실제 함정 발동 + 저장 + 비플레이어 자극 검증
  - [x] `P3-T6` 단서 표현 (`ClueView`) / 정보 비대칭
  - [x] `P3-T7` 종합 회귀 (자동)
  - [x] **`P3-T7` 오너 PLAYTEST 완료** — `3-4` 판정 "잘 보여" (2026-08-20)
- **PHASE 4 (반실시간 전투)**: **자동 검증 완료, 오너 PLAYTEST 대기**
  - [x] `P4-T1` 피해 계산 골격 (`DamageModel` — 4단계 식, √ 압축, 크리티컬 사건 기반)
  - [x] `P4-T2` 성장곡선/무기 데이터 (`data/curves/`, `data/items/weapons.json`)
  - [x] `P4-T3` 공격 타이밍 (`AttackState` — wind-up/active/recovery, 초 단위)
  - [x] `P4-T4` 공간 타격 판정 (`CombatService` — 굴림 없음)
  - [x] `P4-T5` 적 감지·추적·근접 공격 (`Enemy`)
  - [x] `P4-T6` 대시 · 던지기(`ThrownObject`) · 전술 정지(`TimeScale`)
  - [x] `P4-T7` 전투 상태 소유권 + 세이브 v3 + E2E
  - [ ] **`4-2` 오너 PLAYTEST** — 공격 선딜·대시 손맛, 적 회피 난이도 (`CBT-008` 필수)

### PHASE 4 canon 준수 메모

전투는 canon 밀도가 가장 높아 **하면 안 되는 것**이 명시돼 있다. 지킨 방식:

| canon | 요구 | 구현 |
|---|---|---|
| `CBT-004` | 크리티컬은 확률이 아니다 | `DamageModel`에 난수 없음. 급소·기습·무기수명 **사건**에서만 |
| `CBT-006` | 명중/회피·급소 배율 **TBD** | 명중은 **공간 판정**으로 대체, 급소 배율은 기본 1.0 |
| `CBT-010` | 효율을 그대로 곱하지 않는다 | `√(가중 평균 효율)` |
| `CBT-011` | `BodyResilience` 비노출 | 계산에만 쓰고 결과 사전에 담지 않음 |
| `CBT-012` | 무기 비중은 데이터에 | `weapons.json`. 코드 하드코딩 시 테스트 실패 |
| `CBT-013` | 급소는 별도·비확률 판정 | 궤적선과 대상 중심의 거리 (공간 사건) |
| `CBT-001` | 마인드맵 열면 완전 정지 | `TimeScale` — 엔진 배속 대신 월드 시간만 |

⚠ **DESIGN 수치는 전부 데이터에 있고 canon이 아니다** — 무기 성능·구간 길이·대시·감지
거리·체력. `CBT-006`이 TBD로 못박은 영역은 **비워뒀다.**

### PHASE 3 GPT 재검토 — `P3-REV-001~004` 처리 완료 (2026-08-20)

| ID | 등급 | 내용 | 상태 |
|---|---|---|---|
| `P3-REV-001` | BLOCKER | 반복형 치명 함정의 단서가 첫 발동 뒤 사라짐 | ✅ `armed` 기준으로 수정 |
| `P3-REV-002` | BLOCKER | `terrain`을 저장만 하고 복원하지 않음 + 객체 직렬화 | ✅ JSON-native + `from_save_dict` |
| `P3-REV-003` | HIGH | 바닥 물건 조회가 `cell`만 봐서 region/layer 무시 | ✅ `WorldAnchor` 기준으로 변경 |
| `P3-REV-004` | HIGH | 내구 **등급**이 `int` 변환으로 소실 | ✅ `durability_grade`/`points` 분리 |

**변이 검증 4건 모두 통과.** 부수 정리 3건(TrapRuntime null 계약 · `main.gd` 주석 · 이 문서)도 처리했다.

### PHASE 3 GPT 3차 재검토 — `P3-REV-008` (2026-08-21)

`P3-T5`의 "비플레이어 물리 자극 E2E" 완료 조건이 **엄밀히 닫히지 않았다**는 지적.
기존 검증은 `TrapStimulus`를 손으로 만들어 `TrapRuntime`에 넣는 **runtime 단위**였고,
production 경로에는 플레이어 몸 진입 외에 자극을 만드는 코드가 **아예 없었다.**

- ✅ `scripts/traps/trap_sensor.gd` — 물리 사건 → 자극 **어댑터** 신설
- ✅ 플레이어 진입도 같은 어댑터를 거치게 통합 (경로가 갈라지면 `P3-REV-005`가 재발한다)
- ✅ `test_nonplayer_stimulus_e2e.gd` — 던진 돌이 어댑터를 통해 실제 `wall_bolt` 발동,
  `CausalSource.THROWN` + 실제 envelope 경계, 독립 시뮬레이션 통과까지 검증
- ✅ **테스트 유실 가드** — `TestCase.completed` + 파일별 `MIN_ASSERTIONS` 하한.
  GDScript 런타임 에러가 함수만 중단시켜 **단언이 사라져도 PASS로 보고**되던 문제를 막는다

새 canon 없음. `PRESSURE`/`TOUCH`/`IMPACT`는 계속 DESIGN이다.

### PHASE 3 GPT 2차 재검토 — `P3-REV-005~007` + `DOC-REV-004` (2026-08-21)

| ID | 등급 | 내용 | 상태 |
|---|---|---|---|
| `P3-REV-005` | BLOCKER | **실제 플레이에서 wall_bolt가 절대 발동하지 않았다** | ✅ 진입 자극을 압력+접촉으로 |
| `P3-REV-006` | BLOCKER | `drop()` 실패 시 물건 영구 소실 가능 | ✅ 트랜잭션화 |
| `P3-REV-007` | HIGH | 스키마가 바뀌었는데 `SAVE_VERSION`이 1 | ✅ v2, v1은 `VERSION_MISMATCH` |
| `DOC-REV-004` | — | stale 주석 3건 | ✅ |

**변이 검증 4건 통과.** 실제 게임 경로 E2E(`test_trap_play_path_e2e.gd`)를 새로 만들었다 —
`Main.tscn`을 띄워 플레이어를 실제로 걷게 해서 함정이 터지는지 본다.

### PHASE 3 자동 검증 결과 (2026-08-20)

- 단위 **1209 단언** · 통합 **86 단언**, 실패 0 (998/37 → 1004/61 → **1209/86**)
- 새 테스트 파일 5개 — 소유권 / 상호작용 / 아이템 왕복 / 함정 / 정보 비대칭
- **변이 검증 3건** — 함정을 actor class 전용으로 / 단서를 함정 칸에 정확히 표시 /
  인벤토리를 `FloorState`로 이동. 전부 해당 테스트가 잡았다

### PHASE 3 티켓 타당성 검토 (Claude, 2026-08-20)

GPT 분해안을 현재 Godot 구조와 대조했다. **순서는 그대로 타당하다.** 다만 한 가지를 짚는다.

> **아래는 2026-08-20 당시의 진단 기록이다. `P3-T2a`에서 전부 해소됐다** —
> `scripts/world/run_state.gd`·`world_state.gd`가 실제로 존재하고 소유권 가드 테스트가 지킨다.
> 왜 그런 구조가 되었는지를 남기기 위해 지운다.

- ⚠ (당시) **`RunState`·`WorldState` 컨테이너가 실제로 존재하지 않았다.** `TerrainMutationState`는
  "`WorldState` 소유"라고 문서화돼 있지만 소유자가 없어 **테스트에서만 생성**되고
  `main.gd`는 만들지도 않는다. 3계층 canon이 코드에는 아직 `FloorState` 한 층뿐이다.
- 그래서 `P3-T2`의 "인벤토리를 `FloorState`에 두지 않는다"를 지키려면 **인벤토리가 살 자리를
  먼저 만들어야** 한다. `P3-T2`를 두 단계로 나눈다:
  - `P3-T2a` — `RunState`/`WorldState` 최소 소유권 골격 + 소유권 가드 테스트
  - `P3-T2b` — `ItemInstance` + 인벤토리
- `P3-T1`의 "미발견 정보 누출 금지"는 **함정 발견 상태가 아직 없어도** 검증 가능하다.
  함정을 상호작용 후보에서 아예 제외하면 되고, 새 canon이 필요 없다.
  발견 상태가 실제로 필요해지는 시점은 `P3-T6`이다.

- **저장소**: `aa0607aa/Tower-Dev`, `main`
- **현재 문서 동기화 기준 HEAD**: PHASE 2 종료 시점 `794d1f5` + PHASE 3 인계 병합
- **엔진**: Godot `4.7.1-stable`
- **Canon 색인**: **156개 ID / 15도메인 / 통합 포인터 3개**
- **설정서 원본**: `docs/SETTING_BIBLE_v1.1.docx`
- **AI 검수용 사본**: `docs/SETTING_BIBLE_v1.1.md` — 직접 편집 금지

## 최신 오너 확정 — D-018~D-024

### D-018 · 회차 시작 고정 문구

`[@회차 시작! 행운을 빕니다!]`

- 회차 시작 때 한 번 표시.
- 글자·띄어쓰기·기호 고정.
- 층 진입마다 반복하는 문구가 아니다.

### D-019 · 1층 greybox DESIGN 기준선

최종 Canon 숫자가 아니라 PLAYTEST 시작값:
- 긴 축 **160~220 타일**
- 주요 공간 **14~20개**
- 소형 파밍 포켓/막다른 길 **6~10개**
- 큰 순환 루프 **3~5개**

현재 layout v2는 긴 축 **172타일**, 방 17 + 포켓 8의 범위 안에 있다.

### D-020 · 1층 스폰 무기 품질

1층 스폰 무기는 **대체로 품질이 나쁘지만 초기 대거보다는 낫다.**

### D-021 · 오르골 초기 스탯 정정

- 힘 **8** / 민첩 **11** / 지능 **15**.
- ✅ 설정서 v1.1 §24.1을 2026-08-20에 8/11/15로 갱신 완료 (docx 원본 수정 + md 사본 재생성).

### D-022 · 1층 시작 위치 랜덤화

- 고정 지형과 별개로 시작 위치는 회차 시드로 고정 후보 중 선택.
- 후보는 `FloorDefinition`, 결과는 `FloorState.start_cell`에 저장.

### D-023 · 마인드맵 성장 구조

- 빨강=STR / 초록=AGI / 파랑=INT 고정.
- 숫자 직접 배분이 아니라 연결된 색상 노드에 투자.
- **노드 1개 = 포인트 1 = 대응 스탯 +1**.
- 한 가지의 스탯 노드 3개 완성 → 즉시 랜덤 스킬 개화 → 해당 스킬에서 새 가지 발생.
- 중심에서 멀수록 기존 빌드 연관성 강화, 일정 깊이 이후 완전히 무관한 스킬 제외.
- 임의 respec 기본 불가. 종족 변경 시 부적합 skill subtree만 제거하고 해당 투자 포인트 환원.
- 전문화 임계/연관도 공식/태그/가지 수는 TBD.

### D-024 · 소설 원작 누락 6건 복원

1. **물리 함정 상호작용 — `FLR-028`**
   - 함정은 Player 전용 트리거가 아니다.
   - 실제 메커니즘상 돌·투척물·물체·환경 변화가 트리거를 건드릴 수 있으면 발동/소모/우회 가능.
   - PHASE 3에서 actor class보다 trigger mechanism을 먼저 모델링한다.
2. **랜덤 인카운터 — `SYS-016`**
   - 완전 즉흥 생성이 아니라 **저작된 시나리오 골격 + 허용된 세부 랜덤화**.
3. **언어 통합/바벨탑 — `WLD-011`**
   - 미궁/탑 내부에서는 서로 다른 언어권의 말이 통한다.
   - 이 기능을 담당하는 구조물은 **「바벨탑」**.
   - 위치·외형·작동 메커니즘은 TBD.
4. **NPC/생물 경험 학습 — `NPC-006`**
   - 직접 경험하거나 합법적인 정보 경로로 배운 위험/사건/단서가 이후 행동에 영향을 준다.
5. **왕국 도달 난이도 — `WLD-012`**
   - 왕국의 문에도 도달하지 못하는 유배자가 **대다수**인 체감을 유지.
   - 원작의 70%는 고정 통계가 아니라 DESIGN 밸런스 기준.
6. **NPC 정보 표현 — `NPC-007`**
   - 원작 속 과거 게임의 전지적 마우스오버보다 **소설 속 실제 세계 체험**을 따른다.
   - NPC의 미발견 상세는 대화·관찰·행동 결과·소문으로 파악.

## 최신 소설 source audit

- `docs/reference/NOVEL_SOURCE_GAP_AUDIT.md`가 원작 캡처 ↔ Canon 역대조 정본.
- `NSG-001/003/004/005/006/008`은 **D-024로 해결됨**.
- 새 발췌의 열매 효과 랜덤/외형 비신뢰성은 `ITM-002`와 MATCHED.
- **NSG-017**: 2026-08-20 오너 승인으로 `ITM-007`/`D-025`가 되었다. **경향만 canon**이고
  스폰 확률·집단 규모·거리 반경은 TBD다. 구현 상수로 만들지 않는다.
- 같은 발췌의 특정 사냥꾼 독성 저항/레벨은 SCENE-ONLY.

## PHASE 2 현재 구현 요약

### 공간/월드 모델

- `WorldAnchor`: 실제 월드 공간 주소.
- `AccessEnvelope`: 유배자별 허용 영역/인과 경계.
- `TerrainMutationState`: 월드가 공유하는 지형 변경 상태.
- `FloorDefinition`: 1층 고정 초기 정의.
- `FloorState`: 전리품/스폰/함정 상태/계단/발견/시작점 등 동적 결과.

### layout v2

- 서쪽 회랑: 열주형
- 입구 광장: 열린 공간
- 서쪽 납골: 분할된 좁은 셀
- 북쪽: 좁은 미로/회랑
- 중앙 대청: 큰 공간 + 내부 구조물
- 남쪽: 붕괴된 사원 하부 공동
- 동쪽: 넓은 통로
- 최동단: 열린 큰 홀

오너 1차 평가 **"지형이 훨씬 낫다"** → 통로 폭 수정 후 재플레이로 **최종 판정 완료 (2026-08-20)**.
PHASE 2는 `PLAYTESTED`로 닫혔다.

### 계단 resolver

```text
Engine candidate
→ Hard Constraint
→ Engine score
→ Optional AI ranking
→ Validator
→ 상위 band seeded selection
→ WorldAnchor 저장
```

- BFS 실제 경로 비용 사용.
- AI 없이도 정상 동작.
- D-022 실제 `start_cell` 사용.
- P2-REV-001에서 BFS/fallback을 AccessEnvelope-aware로 수정 완료.

### 세이브 / definition hash

- 실체화 결과를 저장하고 로드시 재추첨하지 않는다.
- `definition_hash`는 **immutable floor-definition hash** 계약으로 정리 완료.
- 함정 성질, 시작 후보, 층 정체성 등 불변 의미를 포함.

## PHASE 2 테스트 상태 — Claude 기록

`P2-REV-004~007` 처리 후 (2026-08-20):
- 단위 **652 단언** 통과
- 통합 **20 단언** 통과
- 반복 3회 안정성 확인
- 변이 3건(`velocity *= delta`, envelope 무시, 함정 성질 hash 제외) 모두 테스트가 실패하는 것을 확인

GPT는 이 실행을 현재 환경에서 직접 재실행한 것이 아니므로 최종 종료 시 diff/테스트 구조를 독립 재검토한다.

## PHASE 3 전에 할 일

1. ✅ P2-REV-001 — AccessEnvelope-aware route/fallback.
2. ✅ P2-REV-002 — `or true` 제거, party ID RNG 참여 표본 검증.
3. ✅ P2-REV-003 — immutable definition hash.
4. ✅ 오래된 주석 정리 및 layout 태그 중립화.
5. ✅ Claude 전체 단위/통합/변이 테스트.
6. ✅ **[GPT] P2-REV-001~003 최종 재검토 완료** — `P2-REV-004~007` + `DOC-REV-001~003` 지적.
7. ✅ **[Claude] P2-REV-004** — 통로 폭 정확화 (`width=2`가 3칸이던 버그). 변이 검증.
8. ✅ **[Claude] P2-REV-005** — AI 신뢰 경계: 읽기전용 사본 + 사후 재검증 + 악성 어댑터 5종. 변이 검증.
9. ✅ **[Claude] P2-REV-006** — 행동 반경을 물리 이동 **전에** 적용 + 몸체 전체 판정. 변이 검증.
10. ✅ **[Claude] P2-REV-007** — 공간 태그를 정의 해시에 포함.
11. ✅ **[Claude] DOC-REV-001~003** — D-025 위치·문서 충돌·테스트 수치 정리. `D-026` 신설.
12. ⏳ **[GPT] P2-REV-004~007 diff 재검토**.
7. ✅ **[오너] layout v2 최종 PLAYTEST 판정 완료** (2026-08-20, 통로 폭 수정본 재플레이).
8. 이후 PHASE 2 종료 → PHASE 3.

## ⏸ 보류 항목 — 잊지 말 것

오너가 **나중에 보기로 명시한** 것들이다. 지금 손대지 않는다.

| 항목 | 미루는 이유 | 다시 볼 시점 |
|---|---|---|
| 함정 단서 흔적의 밀도·진하기·크기 (`ClueView` 상수) | greybox 사각형 위에서 튜닝해봐야 도트가 들어오면 다시 해야 한다 | **PHASE 8** (도트 맵 완성 후) |
| 단서 개수 → 흔적 개수 규칙 (`clues.size() + 1`) | 위와 같음. 단서 1개짜리 함정이 가장 알아채기 어렵다 | **PHASE 8** |
| **던지기 자원 소모** (지금은 무한) | 인벤토리·무게 체계와 함께 정리하는 편이 낫다. 오너 결정 2026-08-21 | **PHASE 6** |
| **"돌이 안 통하는 함정" 피드백** | 지금은 반응이 없어 고장처럼 보인다. 튕김·구름 연출은 도트와 함께 봐야 한다. 오너 결정 2026-08-21 | **PHASE 8** |

⚠ **"돌이 안 통하는 함정"은 버그가 아니다.** `min_mass` 미달로 발동하지 않는 것이며
`FLR-028`("모든 함정이 모든 물체에 반응하지 않는다")이 그대로 작동한 결과다.
**고치려 들지 말 것.** 남은 문제는 *플레이어에게 어떻게 알릴 것인가*이고
`SYS-005`상 함정 종류를 직접 알려줄 수는 없다.

⚠ **던지기 자원(`PHASE 6`)의 위험도를 낮게 보지 말 것.** 지금은 `F`를 누르면
**무한히** 돌이 나가므로 모든 함정을 공짜로 무력화할 수 있다.
`FLR-011`(단서를 보고 판단하는 긴장)이 통째로 사라지고 `ITM-001`
("물건은 실제로 들고 다닌다")과도 어긋난다. **밸런스 판정을 그 전에 확정하지 말 것.**

`DebugOverlay`는 **삭제하지 않고 개발 전용으로 남긴다.**
원래 PHASE 3에서 삭제할 예정이었지만, 위 튜닝을 PHASE 8에서 할 때
**흔적 위치와 실제 함정 위치를 비교할 도구**가 필요하다.
이미 3중 게이트(`OS.has_feature("editor")` · `OS.is_debug_build()`)로 배포 빌드에서는 켜지지 않으므로
`SYS-005` 위험은 없다. 인계서 §3.4의 "삭제 **또는** 개발 전용 경로로 격리" 중 후자를 택했다.

## 구현 주의 / 장기 인계

- **PHASE 3 함정**: 반드시 `FLR-028`을 읽는다. Player collision 전용 트리거로 구현 금지.
- **2층+ 인카운터**: `SYS-016` authored template 구조를 따른다.
- **PHASE 7 NPC**: `NPC-006` 경험 학습 + `NPC-007` 정보 비공개를 함께 적용.
- **언어/왕국 사회**: `WLD-011` 바벨탑 존재는 Canon, 세부 메커니즘은 TBD.
- **왕국 밸런스**: `WLD-012`; 70%를 하드 상수로 만들지 않는다.
- **NSG-017** → `ITM-007` 승격 완료 (2026-08-20). 확률·규모·반경만 TBD.
- `D-021`: 다음 설정서 개정 때 오르골 8/11/15 반영.
- `D-023`: `CHR-020`의 레벨당 포인트 DESIGN과 가지 3노드 개화 구조를 PHASE 6 전에 정합 확인.
- 마인드맵 전문화 임계/연관도/태그/가지 수는 TBD.
- `BASE_SPEED`는 PHASE 6에서 민첩 보정과 함께 재확정.
- PHASE 3에서 실제 함정/아이템 상호작용이 붙으면 `debug_overlay.gd` 삭제 예정.
- 다른 PC 작업 시 Godot `--import` 먼저.
- 배포 렌더링 경로는 PHASE 11 (`B-003`).
